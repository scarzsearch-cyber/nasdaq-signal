# 전략_v72 — 월간 스냅샷 갱신 + 비교표 3전략 체제 + 화면 정리

> 2026-08-29. 소유자 요청 4건 이행. **규칙(-16/-16)·freeze.json 무변경.**

## 1. 월간 성과 스냅샷 갱신 (신규 자동화)

지평표·비교표가 2026-08-26 에 멈춰 있던 문제. 매일 갱신은 이득(하루 ±1~2% 변동)보다
비용(데이터 의존성 6종의 일일 장애 모드, 문서-화면 드리프트)이 커서 **월 1회**로 결정.

- `deploy/refresh_hist.py` (신규): 원자료 11종 연장.
  - **기존 행 무수정** (동결된 연구 스냅샷 보존, append-only).
  - 수정주가는 **비율 이음**: 붙임 구간 = 야후 adjclose × k (k = 저장 마지막 종가 /
    같은 날 야후 값). 수정주가의 정보는 수익률이고 이 방식은 이음새 이후 수익률을
    야후 조정 수익률과 정확히 일치시킨다. 배당으로 야후가 과거를 재조정해도
    다음 실행의 k 가 흡수 (자가 치유).
  - 장중 가드 = update_signal 과 동일 판별. **실전 검증**: 장중 실행에선 08-28 제외,
    마감(20:00:00 UTC) 2분 뒤 실행에선 08-28 확정 종가 포함 — 양 분기 모두 확인.
  - 파일 형식 분기: kr A형(Date,Open,Close,AdjClose)과 B형(…,Volume,Raw — Close 가
    이미 조정본) 을 각각 처리. LBMA 금은 야후에 없어 GLD 수익률로 이음(연 0.4% 보수
    만큼 현물 대비 불리 — 방어 20% 다리라 영향 미미).
  - FX(FRED DEXKOUS)는 공식 CSV 전체 교체, 실패 시 기존 유지(ffill 로 견딤).
- `.github/workflows/monthly-stats.yml` (신규): 매월 1일 07:17 UTC(미·한 모두 휴장
  시각) — 연장 → build_stats 재계산 → **verify_all 통과 시에만** 커밋.
- 이번에 1회 수동 실행: 곡선 끝 2026-08-26 → **2026-08-28**. us_2000 B 165.0 → 167.3배.

## 2. 비교표 3전략 체제

| 줄 | 정의 | 다른 점 |
|---|---|---|
| ① −16/−16 · 방어 40/40/20 | 채택안 | — |
| ② −16/−16 · 방어 배당100 | 기존 비교안 | 방어만 다름 |
| ③ **헤지 60/40** · 방어 40/40/20 | 상승장에도 QLD60/SCHD40 (월 1회 재조정) | 공격만 다름 |

- ③의 방어를 40/40/20 으로 굳힌 실측: us_1972 에서 mix Calmar 0.453 / MDD −46.1%
  vs 배당100 0.327 / −64.8% (배당100 방어는 60/40 완충으로도 GFC 형 낙폭을 못 막음),
  kr_1997 은 동률(0.556 vs 0.572). → mix.
- ③ 실측 (08-28 기준): us_1972 최종 31,588배·MDD −46.2%·Calmar 0.453 (채택안 214,076
  ·−60.5%·0.417) — **수익 계단을 낮추고 낙폭을 깎는 저레버리지 지점**. kr_real 3.2년
  MDD −33.6% vs 채택안 −44.9%.
- 벤치마크(2배 보유·방어 단독)는 비교표에 유지하되 **mix 기준으로 고정** (전략 3줄이
  방어 변형을 이미 보여주므로 벤치까지 칩에 따라 바뀌면 혼란).
- build_stats.py 에 `strategies_hedge` 추가 (4개 시나리오 전부, 동일 신호·비용).

## 3. 화면 정리

- **카드·지표 풀이에서 벤치마크 하위값 제거** — 비교는 성과 비교표 한 곳에서만.
  (v61 의 회색 눈금 설계를 소유자 요청으로 철회. verify I5 검사도 교체.)
- 지표 방향 표시(↑ 클수록 / ↓ 얕을수록 …)를 강조색(파랑)으로.
- 실행 가이드 6블록 → 4블록 요약 (같은 날 체결 + 09:05~15:20 / 바스켓 셋 다 + 선물형
  국채 / 세금>규칙 / 환노출 2배 — 환위험 방향 명시).

## 4. 검증

- verify_all 실패 0건 (I5 를 새 설계에 맞게 교체: "비교표에 벤치마크와 전략 3줄").
- 화면 렌더 검증 (로컬 HTTP): 콘솔 오류 0, 비교표 20행(3전략+2벤치 ×4기준) DOM 확인,
  방향 글자 색 rgb(11,95,214), 카드·풀이의 벤치 잔재 0개.
