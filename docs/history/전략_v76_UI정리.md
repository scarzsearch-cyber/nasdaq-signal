# 전략_v76 — UI 정리 · 전환 기준가 · 전환일 알림

> 2026-08-29. 소유자 요청: "가독성 최적화 + 불필요 삭제 + 전략 관점의 기능 추가".
> 규칙·백테스트·신호 계산 무변경. verify 실패 0건.

## 삭제·정리 (불필요 제거)

1. **방어자산 칩 제거** — 카드·풀이·지평은 채택안(방어 40/40/20) 고정.
   배당100 변형은 비교표의 줄로 이미 항상 보이므로 토글은 중복이었다.
2. **죽은 코드 제거** — BENCH/benchLabel/benchOf (v73 비교표 재편 후 미사용).
3. **수동 종가 입력 접기** — `<details>` 로 접힘. 자동 갱신 실패 시의 비상 도구라
   평시엔 화면만 차지했다. 기능은 그대로.
4. **섹션 순서 재배치** — 행동 → 참고 순서로:
   상태/게이지 → (접힌)수동입력 → **실행 가이드 → 계산기** → 성과 비교 → 세금 → 궤적 → T4.

## 추가 (전략 관점 — "규칙 실행을 돕는 것"만)

1. **전환 기준가** — facts 에 `252일 고점 × 0.84` (지금 626.09). 게이지에도
   "QQQ 626.09 아래로 마감하면 전환 / 위로 마감하면 복귀"로 병기.
   %p 보다 가격이 행동으로 직결된다. 진입선=복귀선이라 양방향 같은 값.
2. **전환일 폰 알림** (`deploy/signal_alert.py`) — changed_today 인 날 아침,
   신호 갱신 직후 "오늘 한국장 09:05~15:20 에 무엇을 사고 팔지"를 알림.
   1년에 두어 번뿐인, 놓치면 안 되는 유일한 날이다. notify.py 재사용(🔔),
   secret 미설정 시 조용히 생략, continue-on-error 로 본 파이프라인 무영향.

## 추가하지 않기로 한 것 (이유 포함)

- 실시간 국내 ETF 가격 연동 — 데이터 의존성·장애 모드 증가 대비 가치 낮음 (주문은 어차피 증권사 앱에서)
- 자동 매매 — 이 시스템의 가치는 "사람이 규칙을 지키게 하는 것"이지 사람을 빼는 게 아님
- 위기 유사도 경고 — v58·v59 에서 신호로서 기각, UI 로는 재량 개입 유혹 (v70 §결론)
- 지표 추가(샤프 등) — v60 에서 정한 6개 세트 유지, 더 넣으면 안 읽힘
