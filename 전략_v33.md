# 전략 v33 — 적립 시뮬레이터의 2일 지연 버그. v29 ISA 수치 정정

작성 2026-08-27. 재현 코드 `verify_volguard.py`, 원본 출력 `전략_v33_raw.txt`.

## 0. 답

> **사용자 요구: "완벽한지 검증해줘. 이건 절대 틀리면 안 된다."**
>
> **검증했더니 틀린 게 나왔다. 적립 시뮬레이터 4개 전부가 신호를 하루 더 늦게
> 반영하고 있었다.** 고쳤고, v29(ISA)·v26(적립식)·v32(변동성 가드) 수치를
> 다시 냈다. **결론은 전부 유지되고 수치만 바뀐다.**

| | |
|---|---|
| 버그 | 적립 함수가 `그날 수익 → 전환` 순서라 **실질 2일 지연** (규약은 1일) |
| 영향 파일 | `axis_lib.accumulate` / `axis_accum2.accum` / `axis_isa.accum_tax` / `axis_volguard` |
| 영향 문서 | v22 축3 · **v26** · **v29(라이브 사이트)** · v32 |
| 결론 변화 | **없음.** 순위·판정 전부 그대로 |
| 수치 변화 | ISA 이득 **+43.2% → +49.7%**(20년), 15년은 +28.5% → +20.6% |
| 재발 방지 | `axis_lib.check_accum()` 신설 — 납입 1회면 거치식과 오차 0이어야 통과 |

---

## 1. 무엇이 틀렸나

프로젝트 규약은 **`pos = w.shift(1)`** — 전일 종가 신호로 당일 체결이다.
`reentry_lib.run()` 과 `axis_lib.sim()` 이 그렇게 돈다.

그런데 적립 시뮬레이터들은 이 순서였다:

```python
for i in range(lo, hi):
    R *= (1 + rr[i])          # ① 그날 수익을 먼저 적용
    C *= (1 + dfr[i])
    pos = w[i - 1]            # ② 그 다음 포지션 결정
    if pos != prev:           # ③ 그 다음 전환
        ...
```

**i일의 수익이 i-2일 신호로 정해진 배분에 적용된다.** `prev` 가 직전 반복에서
`w[i-2]` 로 설정됐기 때문이다. 즉 1일이 아니라 **2일 지연**이다.

정정:

```python
for i in range(lo, hi):
    pos = w[i - 1]            # ① 포지션 먼저
    if pos != prev:           # ② 전환 먼저
        ...
    R *= (1 + rr[i])          # ③ 그 다음 그날 수익
    C *= (1 + dfr[i])
```

### 검산

**납입을 1회로 두면 그 시점부터의 거치식과 정확히 같아야 한다.**

| | 결과 |
|---|---|
| 기존 (2일 지연) | 499.011 vs 거치식 833.327 — **오차 −40.1%** |
| 정정 (1일 지연) | 833.327 vs 833.327 — **오차 1.1e-16** |

미래훔쳐보기는 아니었다(보수적 방향). 그래도 규약 위반이고 40% 오차다.

> **이 검산을 `axis_lib.check_accum()` 으로 상설화했다.** `check(D)` 가 자동으로
> 부른다. 새 적립 함수를 만들면 반드시 통과시켜라.

---

## 2. v29 (ISA) 정정 — 라이브 사이트 반영 완료

### 납입 1억(연 2천만 × 5년) 대비 세후 평가액, 롤링 창 중앙값

| 계좌 | 10년 | 15년 | 20년 | 실효세율 |
|---|---:|---:|---:|---:|
| 세전 (참고) | 6.84배 | 31.5배 | 159.0배 | — |
| **④ ISA · 만기 연장** | **6.27배** | **28.5배** | **143.3배** | **9.9%** |
| ③ 이연 + 9.9% | 6.26배 | 28.5배 | 143.3배 | 9.9% |
| ② 이연만 (15.4%) | 5.94배 | 26.8배 | 134.7배 | 15.4% |
| ⑤ ISA · 3년마다 해지 | 5.96배 | 25.2배 | 120.5배 | 10.7% |
| ① 일반계좌 | 5.98배 | 23.6배 | 95.7배 | 16.9% |

**ISA vs 일반계좌: +4.8%(10년) / +20.6%(15년) / +49.7%(20년)**

### 분해 (20년 창)

| 요소 | 정정 전 | **정정 후** | 비중 |
|---|---:|---:|---:|
| 과세이연 | +34.5% | **+40.7%** | **82%** |
| 세율 15.4%→9.9% | +6.4% | +6.5% | 13% |
| 400만원 비과세 | +0.0% | **+0.0%** | **0%** |
| 합계 | +43.2% | **+49.7%** | 100% |

**질적 결론은 그대로다** — ISA 가 훨씬 낫고, 이득의 대부분이 과세이연이며,
400만원 비과세는 이 규모에서 0 이다. 3년 만기에는 연장이 낫다
(정정 후 143.3배 vs 120.5배 = **+19%**, 정정 전 +15%).

**주의: 10년 창에서는 ②(이연만)가 ①(일반계좌)보다 나쁘다**(5.94 vs 5.98).
전환 비용이 이연 이득을 상쇄하는 구간이 짧은 창에 있다. 세율 인하(③)까지
가야 뒤집힌다. 짧게 굴릴 거면 ISA 의 이점이 작다는 뜻이다.

---

## 3. v26 (적립식) — 결론 불변

납입 규약 6종별 20년 창 중앙값:

| 정책 | 거르기 | 고정 | 몰아넣기 | 불규칙 | 역추격 | 추격 |
|---|---:|---:|---:|---:|---:|---:|
| QQQ 적립(1배) | 4.45 | 4.52 | 4.57 | 4.40 | 4.48 | 4.56 |
| QLD 그냥 적립(2배) | 5.15 | 5.14 | 5.13 | 5.08 | 5.35 | 4.97 |
| 전략A −16/−11 | 35.12 | 36.81 | 35.26 | 35.96 | 36.62 | 36.62 |
| **전략B −16/−16** | **45.62** | **46.96** | **45.06** | **46.85** | **47.26** | **44.87** |

**순위 불변.** 6종 어디서도 뒤집히지 않는다. 최소 월납입 15만원도 그대로.

---

## 4. v32 (변동성 가드) — 결론 불변, 수치 이동

적립식 20년 창 · 원화 · 월 정액 60개월 · 납입 60단위 대비:

| | 중앙 | 차이 | 승률 | 5분위 | 최악 | 납입후MDD | 원금대비 |
|---|---:|---:|---:|---:|---:|---:|---:|
| **A 기준** | **133.82** | — | — | 18.56 | 13.15 | −51.7% | −20.8% |
| A +(14,p90,−2%) | 90.51 | −32.4% | 33.3% | 22.62 | 17.71 | −46.0% | −16.6% |
| A +(10,p92.5,−3%) | 85.07 | −36.4% | 29.4% | 19.17 | 15.36 | −51.2% | −17.1% |
| **B 기준** | **158.98** | — | — | 26.23 | 18.53 | −53.3% | −23.4% |
| B +(14,p90,−2%) | 98.13 | −38.3% | 23.5% | 19.98 | 16.51 | −52.1% | −19.1% |
| B +(10,p92.5,−3%) | 96.44 | −39.3% | 29.4% | 23.14 | 19.71 | −53.3% | −20.9% |

**가드는 여전히 적립식에서 진다** — 승률 24~33%, 중앙값 −32~39%.
다만 정정 후에는 **좌측꼬리가 개선되는 게 더 뚜렷해졌다**
(A: 5분위 18.56 → 22.62, 최악 13.15 → 17.71). 판정은 v32 그대로 유지한다.

---

## 5. 내가 이번 세션에서 틀린 것 전부

기록으로 남긴다. 같은 실수를 반복하지 않기 위해서다.

| # | 무엇 | 어디서 잡혔나 |
|---|---|---|
| 1 | v31 이 격자의 엉뚱한 단면만 보고 '파라미터 첨탑' 오판 | v32 §1-1 |
| 2 | v31 워크포워드를 **수익 기준**으로 골라 실패 | v32 §1-2 |
| 3 | 적립식 MDD 를 **초기 잔고 1단위** 등락까지 세어 −69~75% 로 부풀림 | 사용자 질문 |
| 4 | 중앙값 대신 **최악 창** 값을 인용 | 사용자 질문 |
| 5 | **적립 시뮬레이터 2일 지연** (프로젝트 파일 4개) | v33 §1 |
| 6 | 검증 하네스가 QLD 합성연장·SCHD 현금대용을 빼먹음 | v33 §6 |

**공통 원인: 결과를 내기 전에 검산을 안 했다.** 5번은 "납입 1회 = 거치식"
한 줄이면 즉시 잡혔다. 그래서 그 검산을 상설화했다.

---

## 6. 검증 관문 (verify_volguard.py)

| 관문 | 내용 | 결과 |
|---|---|---|
| G1 | 내 `sim()` == `reentry_lib.run()` | 오차 0 |
| G2 | `verify.py` 공표치 재현 (140.0배 / −46.6%) | 140.3배 / −46.6% |
| G3 | 가드 신호를 하루 더 늦춰도 MDD 개선 유지 (미래참조 점검) | 4/4 통과 |
| G4 | 수익률0·전환0 → 최종=납입액 / 납입 1회 → 거치식 오차 0 | 통과 |
| G5 | MDD 정의별 수치 (초기허수 / 납입후 / 원금대비) | — |
| G6 | 최종 결론표 독립 재산출 | — |

> G2 는 두 번 실패한 뒤 통과했다. `verify.py` 는 (a) QLD 2006년 상장 이전을
> 2×QQQ−합성비용으로 **연장**하고 (b) SCHD 2011년 상장 이전을 **현금 2%** 로
> 대용한다. 둘 다 빼먹으면 93.6배, (b)만 고치면 96.4배가 나온다.
> **공표 수치를 재현할 때는 데이터 구성 규약부터 맞춰라.**

---

## 7. 갱신한 것

| 파일 | 내용 |
|---|---|
| `axis_lib.py` | `accumulate()` 정정 + **`check_accum()` 신설**(회귀 방지) |
| `axis_accum2.py` | `accum()` 정정 |
| `axis_isa.py` | `accum_tax()` 정정 |
| `axis_volguard.py` | 내부 `accum()` 정정 |
| `data/isa_stats.json` | 재생성 |
| `signal.html` | **계좌 패널 수치 전면 교체** (+43.2%→+49.7%, 표 4행, 3년해지 비교) |
| `전략_v29.md` / `전략_v26_raw.txt` / `전략_v29_raw.txt` | 재생성·정정 |

---

## 8. 재현

```bash
python verify_volguard.py     # 관문 6개 전부
python axis_isa.py --emit     # ISA 재산출 + isa_stats.json
python axis_accum2.py         # 적립식 재산출
```
