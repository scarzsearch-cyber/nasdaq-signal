# Claude Code 인수인계 지시서

## 0. 시작하기

zip을 원하는 폴더에 풀고, 터미널에서:

```bash
cd <압축푼폴더>
claude
```

Claude Code가 설치돼 있지 않으면 먼저:

```bash
npm install -g @anthropic-ai/claude-code
```

(Node.js 18 이상 필요. 설치 확인은 `node -v`)

작업 시작 전 환경 확인:

```bash
python3 -m pip install --user pandas numpy requests
python3 verify.py          # 기준 수치가 재현되는지 먼저 확인
```

`verify.py`가 **최종배수 138.2배 / CAGR 20.3% / MDD −46.6%**를 출력하면 정상이다.
(`--cash 0` 을 붙이면 125.8배 — 2011년 이전 피신처를 무이자로 볼 때의 보수적 수치)

---

## 1. 첫 프롬프트 — 아래를 그대로 붙여넣기

**작업 ⓪(복귀 로직 연구)은 2026-08-26에 완료됐다. 다시 하지 마라.**
결과는 `전략_v19.md`에 있고, 판정은 **기존 −16/−11 유지**다. A~H 계열 294개 +
평탄성 표면 576개 + 쿨다운·V·부분복귀 계열을 연속상태 WFA·톱니 스트레스까지 걸어
전부 기각했다. 핵심 결론 두 줄:

- 복귀를 빠르게 하는 변형은 실측에서 거의 전부 개선처럼 보이지만(138.5→151~185배),
  그 원천은 예외 없이 "복귀선이 사실상 −16%로 올라간 것"이다.
- 반등률·회복속도·단기수익률·이동평균 같은 **복귀 확인 지표는 기여가 0이다.**
  이 계열은 탐색 종료. 재실행 코드는 `reentry_*.py`.

**작업 ②(1970년대 데이터 확장)도 2026-08-26에 완료됐다. 다시 하지 마라.**
결과는 `전략_v20.md`에 있고, 데이터를 **1972-02까지(54.5년, 13,859거래일)** 늘려
`-16/-11`(A)과 `-16/-16`(B)을 정면 비교했다. 판정은 다시 **A 유지**지만, **v19와 근거가
완전히 다르다. v19의 기각 사유는 폐기됐다:**

- v19는 합성 톱니 경로로 "히스테리시스 폐지는 톱니형 약세장에서 파괴적"이라고 했다.
  **실제 역사에는 대응물이 없다.** 톱니는 27개 하락 에피소드 중 19개에서 총 43회 실재했지만,
  그 구간에서 B−A 평균은 **+0.04%p(사실상 0)**이고 19번 중 12번은 B가 이겼다.
  **−11%선은 톱니를 막아주지 못한다.** 합성 스트레스로 위험을 추정하는 방식은 신뢰도를 잃었다.
- B를 채택하지 않는 진짜 이유는 전부 실측이다: **t=1.51(54년으로도 A와 구분 불가)**,
  28개 기여연도 중 **상위 5개가 우위의 88%**(그 5년 빼면 1.08배), 손익 **왜도 −1.42**,
  **체결 하루 지연 시 1.97→1.06배**, 진입선 −20%에서 역전, MDD는 6개 하위구간 전부 A 우세.
- **1973–74는 톱니 시장이 아니다**(−16% 아래 2년 체류, 두 전략 차이 2.0%p).
  진짜 톱니는 **1987-10~1988-12**이고, 두 전략 모두의 54년 최대낙폭이 거기서 나온다
  (A −60.86% / B −64.23%, 회복까지 4년). 재실행 코드는 `hyst_*.py` / `hist_data.py`.

이제 아래 데이터 확장(①③④)으로 간다. **②는 건너뛴다.** 아래를 붙여넣는다.

```
이 폴더의 전략_v18.md를 먼저 읽어라. 나스닥 레버리지 ETF 전환전략이고,
현재 채택안은 "QQQ 252일 고점대비 낙폭 ≤ -16% → SCHD 전량, > -11% → QLD 전량"이다.

중요: 같은 데이터로 문턱값·확인일·필터를 더 조합하는 탐색은 하지 마라.
이미 45개 변형을 전부 검증했고 전부 기각됐다(문서 4절). 표본이 얇아서
탐색을 늘리면 스파이크만 더 나온다.

네가 할 일은 데이터를 늘리는 것이다. 문서 6절의 ①~④ 중 ①부터 시작한다.

[작업 ①] 배당/가치 지수 장기 시계열 확보
- Kenneth French Data Library에서 HML 팩터 일별 데이터를 받아라
  (https://mba.tuck.dartmouth.edu/pages/faculty/ken.french/data_library.html)
- 가능하면 Russell 1000 Value 또는 S&P 500 Value 지수 일별 총수익도 확보하라
- 목표: 1970년대~현재. 최소한 1995년~현재는 반드시 확보
- 이 데이터로 "닷컴(2000-2002)과 GFC(2007-2009) 구간에서 배당/가치주가
  현금 대비 연 몇 %를 벌었는가"를 실측하라

이게 왜 중요한가: 현재 전략의 가장 큰 미해결 질문이 "위기 중 SCHD류가
연 0~5% 이상 버느냐"인데, SCHD가 2011년 상장이라 닷컴·GFC 구간이 공백이다.
연 +5% 이상이면 현 채택안(SCHD 피신)이 확정되고, 연 0% 이하면 현금 피신을
재검토해야 한다.

작업 순서:
1. 데이터를 받아 data/ 폴더에 저장하고, 형식·결측·기간을 먼저 보고하라
2. verify.py의 SCHD 자리에 이 대용 시계열을 넣어 1995~2026 전체를 재계산하라
3. "SCHD 피신 vs 현금 피신"을 닷컴·GFC 포함해서 비교하고 결론을 내라
4. 결론을 전략_v19.md로 문서화하라

검증 원칙(반드시 지켜라):
- 파라미터는 반드시 촘촘한 그리드 + 이웃평균(3점/5점 이동평균)으로 평지인지
  스파이크인지 확인할 것. 원값 최고치를 그냥 채택하지 말 것
- 훈련/검증 분리, 퍼지드 워크포워드, 롤링윈도우 승률(1~15년)을 항상 같이 볼 것
- 특정 사건 하나(코로나 등)에 의존한 개선인지 반드시 구간 분할로 확인할 것
- 거래비용 민감도(편도 0.05~0.5%)를 항상 같이 볼 것
- 개선처럼 보이는 결과가 나오면 먼저 의심하고, 위 관문을 통과하지 못하면 기각하라
```

---

## 2. 작업 ① 완료 후 이어서 쓸 프롬프트

```
[작업 ②] 나스닥/S&P를 1970년대까지 확장
- 나스닥 종합 또는 S&P 500 일별 데이터를 1970년대부터 확보하라 (FRED, Stooq 등)
- QLD는 지수 일별수익 × 2 − 일일비용으로 합성하라
- 일일비용은 고정값(연 3.30%)이 아니라 당시 단기금리를 반영한 시변 값으로 하라
  (FRED의 DFF 또는 TB3MS 사용, 비용 ≈ 단기금리 + 운용보수 0.95%)
- 목표: 1973-74 오일쇼크, 1987 블랙먼데이를 표본에 추가 (독립 위기 5개 → 8개)
- 확장된 표본에서 -16%/-11%가 여전히 평지의 정상인지 재검증하라
```

```
[작업 ③] 환노출 효과 실측
- 원달러 환율(USD/KRW) 일별 데이터 확보 (FRED: DEXKOUS)
- TIGER 미국나스닥100레버리지(합성)는 환노출형이고 환변동도 2배로 반영된다
  → 원화 기준 일별수익 = (지수수익 + 환수익)의 2배 − 비용
- 이 기준으로 MDD가 달러 기준(-46.6%)보다 실제로 몇 %p 얕아지는지 계산하라
- 가능하면 실제 TIGER ETF 가격도 받아 괴리율까지 확인하라
```

```
[작업 ④] 현금 수익률 실측
- 미국 단기금리(FRED: TB3MS 또는 DGS3MO)와 한국 CD 91일물 금리를 확보하라
- 현재 "현금 연 2% 고정" 가정을 실제 시변 금리로 바꿔 재계산하라
- 금리가 2000년 6%대 → 2021년 0%대로 크게 변했으므로 결론이 달라질 수 있다
```

---

## 2-B. 폰에서 보기 + 종가 자동 갱신 (독립 작업, 언제 해도 됨)

`deploy/README.md`에 전체 절차와 붙여넣기용 프롬프트가 있다. 요약하면:

- GitHub Pages로 `signal.html`을 배포 → 폰에서 URL로 열고 홈 화면에 추가
- GitHub Actions가 매일 KST 07:30에 QQQ 종가를 받아 신호를 갱신하고 커밋
- 무료, PC를 켜둘 필요 없음

준비된 파일: `deploy/update_signal.py`, `deploy/workflows/daily-signal.yml`,
`deploy/workflows/pages.yml`. 사전에 GitHub 계정과 `gh` CLI 로그인이 필요하다.

---

## 3. 매일 신호 자동화 — 터미널 방식 (2-B 대신 쓸 때)

데이터 확장과 별개로, 매일 아침 신호를 자동 판정하게 만들 수 있다.

```
signal.html을 쓰지 않고 터미널에서 매일 신호를 확인하고 싶다.
stooq에서 QQQ 최신 종가를 자동으로 받아 252일 낙폭을 계산하고,
현재 상태(QLD 보유 / SCHD 피신)와 다음 전환선까지 남은 여유를 출력하는
daily_signal.py를 만들어라. 상태는 파일에 저장해 히스테리시스를 유지해야 한다.
macOS면 launchd, 리눅스면 cron으로 매일 오전 7시(KST)에 돌게 설정하는 방법도 알려달라.
```

---

## 4. 절대 하지 말 것

- 같은 3개 CSV(qqq/qld/schd)로 문턱값·gap·확인일·필터를 더 조합하기
- 롤링윈도우 승률이나 워크포워드 없이 전체구간 수익만 보고 채택하기
- 이웃평균 확인 없이 그리드 최고값을 채택하기
- 거래비용을 0.1% 이하로만 가정하고 결론 내기
- "이번엔 다르다"며 위 검증 원칙을 건너뛰기

이 다섯 가지가 이 프로젝트에서 실제로 여러 번 잘못된 결론을 만들었던 경로다.

---

## 5. 현재 상태 요약 (Claude Code가 알아야 할 것)

- **채택안**: QQQ 낙폭 ≤ −16% → SCHD / > −11% → QLD. 평상시 QLD 100%
- **성적**: 2000~2026(26.6년) 138.2배 / CAGR 20.3% / MDD −46.6% / 전환 연 0.8회
- **대조**: QLD 계속보유 4.9배(MDD −98.8%), QQQ 계속보유 8.8배
- **핵심 근거**: 닷컴을 −3.3%(사실상 본전)로 통과해 이후 20년 상승을 온전히 태운 것
- **가장 큰 취약점**: **1987-10~1988-12형 시장** — V자 급락 뒤 8개월간 낙폭이 −15~−22%로
  진동하는 국면. 54년 전체 최대낙폭(−60.86%)이 여기서 나왔고 회복에 4년 걸렸다.
  이 구간은 **QLD 단순보유(−31.3%)에도 진다**(전략 −45.8%). 히스테리시스와 무관한
  낙폭 기반 신호 자체의 구조적 약점이다 (`전략_v20.md` §6)
- **가장 큰 미해결 질문**: 위기 중 배당/가치주가 현금보다 나은가 (작업 ①이 이걸 푼다)
- **종결된 과제 1**: SCHD 피신 후 QLD 급반등을 놓치는 문제(작업 ⓪) — `전략_v19.md`에서
  복귀 지표 계열 전부 기각. 반등률·회복속도·MA 확인의 기여는 0이다
- **종결된 과제 2**: `-16/-11` vs `-16/-16`(작업 ②) — `전략_v20.md`에서 1972년까지
  확장해 판정. **A 유지.** B는 54년 최종배수 1.97배로 앞서지만 t=1.51로 통계적 구분이
  안 되고, 우위의 88%가 28개 기여연도 중 상위 5개에 몰려 있으며, 체결을 하루만 늦춰도
  1.06배로 사라진다. 반면 MDD는 6개 하위구간 전부 A가 우세하다.
  **v19가 근거로 삼았던 합성 톱니 스트레스는 실제 역사에 대응물이 없어 폐기했다**
- **사용자 전제**: ISA 서민형, 국내 TIGER ETF로 매매, 판정은 QQQ 미국장 종가 기준,
  목표는 수익 최대화(MDD −60%까지 감내), 배당 생활은 나중 국면의 별도 문제

---

## [2026-08-26 추가] v21 완료 — 남은 데이터 과제 전부 종결 + 폴더 정리

**남은 데이터 과제 ①③④ 전부 완료.** 다시 하지 마라. 결과는 `전략_v21.md`.

| 과제 | 상태 |
|---|---|
| ① 배당/가치 지수 장기 시계열 | ✅ Kenneth French BE/ME·D/P + 실물 DVY/VYM/SDY. `data/hist/` |
| ② 1970년대 확장 | ✅ v20 에서 완료 |
| ③ TIGER ETF + 원달러 | ✅ TIGER 3종 실물 + KRX 달력 + DEXKOUS |
| ④ 단기금리 실제 시계열 | ✅ DTB3 로 SCHD 피신 vs 현금 피신 재평가 완료 |

### 이번에 뒤집힌 전제 3가지

1. **「연 2% 현금」 방어자산 규약이 결과를 크게 왜곡하고 있었다.** 실측 배당주로 바꾸면
   B/A 가 1.969 → 1.369(54년), 1.092 → **1.001**(2000-2026)로 줄어든다.
2. **전략 MDD 는 −46.6% 가 아니라 −68.7%다.** 실물 DVY 가 2008년에 −59.7% 빠졌기 때문이다
   (SPY −54.2% 보다 나쁘다). MDD 시점도 2020-03 → **2009-03-09**.
3. **TIGER 레버리지(합성)는 환노출도 2배다.** 실측 회귀 beta = 1.982 vs TIGER 미국나스닥100(원화),
   R²=0.90. 기존 원화 모형 `(1+r_QLD)(1+r_FX)−1` 은 과소모형이었다.

### 진입/복귀 규칙 — 결론이 바뀌었다

`전략_v21.md` §11 에서 **−16/−16, −16/−15, −16/−11 3자 비교**를 마쳤다.

- **−16/−15 는 기각.** 양쪽에 지면서 전환만 1.7배 많다.
- **−16/−16 과 −16/−11 은 통계적으로 구별되지 않는다.** 위험선호가 결정한다.
  - 수익·단순성 → **−16/−16** (원화 2,705배, 10Y 승률 60.4%, WFA 선택 31/50)
  - 꼬리위험·전환횟수 → **−16/−11** (톱니 최악 −18.3%, 닷컴 −21.5%, 전환 28회)
  - 보험료는 54년 원화 기준 **최종배수 27% / 연 CAGR 0.87%p**
- 핵심 근거: 낙폭 −16% 이하 구간의 최적 레버리지 f* = **0.50** → **도피 기전이 가치의 전부**이고
  두 규칙 모두 그것을 동일하게 갖는다. −11% 밴드는 방어 추가가 아니라 **회복 참여 지연**이다.
- **규칙을 자주 바꾸는 것이 최악이다.** §11.8 워크포워드(OOS 69.15)가 고정(160.7/160.9)에 크게 진다.

### 폴더 정리

`FILES.md` 에 전체 파일을 4분류해 두었다. `archive/` 로 27개 파일을 옮겼고 2개를 삭제했다.
**루트에 남은 것은 「지금 돌릴 것」과 「지금 읽을 것」뿐이다.**

### 남은 것 (전부 선택 사항)

1. **괴리율(NAV 대비 시장가) 정량화** — KRX/발행사 iNAV 데이터 필요. 현재는 시초가 갭 대용치만 있음
2. ~~**세금 반영**~~ — **완료(2026-08-26, v21 §13 / `hist_krtax.py`).** 예상과 달리 순위는 안 바뀐다.
   B/A 우위 +21.7%(세전) → +14.6%(15.4%+분배금) → +11.4%(22%). 전환은 2.07배인데 납부세액은
   1.37배에 그친다 — 레버리지→방어 매도가 정의상 −16% 낙폭 직후라 실현이익이 이미 깎여 있고,
   추가 전환분은 대부분 톱니 구간의 소액 손익이라 과세가 잘 안 된다.
3. ~~**라이브 사이트 반영 여부**~~ — **완료(2026-08-26).** `signal.html` 에서 −16/−16 · −16/−11 을
   버튼으로 고르게 했고(기본값 −16/−16), 각 선택지에 MDD·Calmar·Sortino 를 띄운다.
   `deploy/update_signal.py` 는 두 규칙을 모두 판정하고, 지표는 새 스크립트
   `deploy/build_stats.py` 가 `data/strategy_stats.json` 으로 미리 굳혀둔다(로컬 수동 실행).

---

## [2026-08-26 추가 2] 한국 실전 맞춤 + 라이브 화면 개편

### 새 파일
- `hist_krtax.py` — 세금·계좌 반영 A vs B (전략_v21 §13)
- `deploy/build_stats.py` — 화면용 성과지표 사전계산 → `data/strategy_stats.json` (로컬 수동)

### 결론 요약
1. **세후에도 −16/−16 우위 유지.** 세전 +21.7% → 일반계좌 15.4% +14.3% → 분배금 포함 +14.6%
   → 22% 가정 +11.4%. 손익통산이 허용된다고 가정하면 +26.1% 로 오히려 커진다.
2. **계좌 선택이 규칙 선택보다 4배 크다.** 과세이연 → 일반계좌 15.4% 는 최종배수 −61%
   (2,705 → 1,062). 규칙 차이는 15~22%.
3. **체결 시각은 고민할 가치가 없다.** 시가→종가 기대수익 +0.008%, 변동성 1.36% 추가.
   시초가 갭 표준편차가 2.42% 라 몇 시간 차이는 묻힌다. LP 호가 의무 공백만 피해 09:05~15:20.
4. 2025-04 실물 구간에서 −16/−16 이 진 이유는 전액 **관세 쇼크 톱니 1건**. 차이 −1.22%p 중
   65%가 순수 거래비용(6회 × 0.2%). 4/4~5/9 이외 구간에서 두 곡선은 완전히 동일하다.

### signal.html 개편
- 전략 선택 버튼(−16/−16 기본), 선택지별 MDD·Calmar·Sortino, 4개 기준 전체 비교표
- **시장 시계** — 미국장 종가 확정까지 남은 시간 + 한국장 상태. 전부 **한국시간(KST)** 표기.
  서머타임은 IANA 타임존(`Intl`)이 처리하고, NYSE 휴장일·조기마감(13:00 ET)은 규칙으로 계산.
  성금요일은 Meeus 부활절 알고리즘으로 산출. **한국 공휴일은 미반영**(설·추석이 음력이라 보류).
- `data/signal.json` 에 `updated_at_iso` 추가 → 브라우저가 KST 로 렌더

### 아직 안 한 것
- 괴리율(NAV 프리미엄/디스카운트) 실측 — KRX/발행사 iNAV 필요
- 한국 공휴일 달력을 화면 시계에 반영 (음력 휴일 테이블 필요)
- **커밋 없음.** 두 세션 연속으로 아무것도 커밋하지 않았다. `data/strategy_stats.json` 은
  신규 파일이라 첫 커밋 전에는 화면 성과 패널이 뜨지 않는다.

---

## [2026-08-26 추가 3] v22 — 새 축 3개 탐색 완료

**질문**: "−16/−16 · −16/−11 말고 아예 다른 관점에서 더 나은 전략이 있는가?
QLD Dip Alert 같은 앱처럼 완전히 새로운 시각이 있는가?"

**답**: 있었다. 다만 그것은 "언제 바꾸느냐"가 아니라 **"얼마나 태우느냐"**였다.
v18~v21 의 4개 버전 + 900여 개 변형이 전부 **「전환 타이밍」 축 하나** 위에 있었다.

결과는 `전략_v22.md`, 재현 코드 `axis_lib.py` / `axis_lev.py` / `axis_ens.py` /
`axis_accum.py`, 원본 출력 `전략_v22_raw.txt`.

| 축 | 지금까지 | 판정 |
|---|---|---|
| **축1 리스크온 배수 k** | 항상 2배 고정 | **최대 미탐색 축.** 세전 2.51배 / 세후 26년 1.25배 · 54년 2.85배. **조건부** |
| 축2 파라미터 앙상블 | 항상 하나를 고름 | **기각** — 항상 멤버 산술평균에 앉고 전환만 3.6~14배 |
| 축3 적립식 목적함수 | 항상 거치식 | **현행 강화.** dip 매수 타이밍 기여 = 0 |

### 반드시 알아야 할 것 3가지

1. **v21 §11.3 이 답을 이미 적어 놓고 있었다.** 조건부 f\*: 평상시 5.00 / 회색 4.25 /
   도피 0.50. **도피 쪽 0.5 는 SCHD 전환으로 구현돼 있는데 리스크온 쪽 f\*=5 는 구현이 없다.**
   −16/−11 vs −16/−16 은 561일짜리 논쟁이지만 배수는 13,859일 전부에 걸린다.
   2000-2026 기준 규칙 차이 1.00배 vs 배수 차이(x2→x3) **2.51배**.

2. **그런데 계좌가 대부분을 먹는다.** 국내 레버리지 ETF 는 2배 상한이고 ISA 는 해외 상장
   종목을 못 담는다 → x2 초과는 해외계좌(22% 양도세)뿐이다.
   세후: **x2 ISA 133.3 vs x3 해외 166.9 (1.25배, 26년)** / 200,538 vs 571,529 (2.85배, 54년).
   **투자기간이 결정한다.** 그리고 **같은 x2 인데 계좌만 ISA→해외로 옮기면 0.53배(26년)다
   — ISA 자금을 x3 쓰려고 옮기는 것은 절대 금지.**

3. **QLD Dip Alert 형(낙폭에 맞춰 사는 도구)의 기여는 실측상 정확히 0 이다.**
   적립식 15년 창 160개: dip −16% 대기 중앙 6.75 = 그냥 적립 6.75, 좌측꼬리는 오히려 악화
   (최악 0.39 vs 0.48). −25% 대기는 4.64 로 확실한 손해.
   이유는 f\*(DD ≤ −16%) = 0.5 다 — **낙폭 구간은 "싸게 사는 자리"가 아니라 "안 들고
   있어야 하는 자리"다.** 같은 −16% 신호라도 "그때 사라"가 아니라 "그때 팔아라"가 맞다.

### 부수 발견 — 적립식에서 이 전략은 거치식보다 더 강하다

15년 창 160개 **전부**에서 QQQ 적립을 이긴다. 결정적인 건 좌측꼬리:
전략 최악 **3.18배** vs QLD 그냥 적립 **0.48배**(납입 원금의 절반 손실).
25년 창은 중앙 76배 vs 9배. **v18~v21 이 못 본 강점이다.**
A vs B 는 적립식에서도 안 갈린다(15년 A 7.91 / B 7.56, 25년 A 72.29 / B 76.47 — 엇갈림).

### 채택안 변경 없음

**QQQ 252일 낙폭 ≤ −16% → SCHD, > −16% → QLD, x2, ISA.** 그대로다.
x2.5~x3 은 다음이 **전부** 성립할 때만 고려한다(`전략_v22.md` §2.6):
ISA 한도 초과분 + 20년 이상 방치 + MDD −76% / 하루 −45% / 수중 6.1년 감내 +
2000-2009 형 10년(그 구간은 x3 이 진다)에도 규칙 유지.
참고로 **현재 전제인 "MDD −60% 감내"는 실측 방어자산에서 x2 조차 이미 −68.0% 로 넘겼다.**

---

## [2026-08-26 추가 5] v24 — 남은 과제 3건 종결

결과는 `전략_v24.md`, 재현 코드 `axis_krspec.py` / `axis_krspread.py` / `deploy/kr_holidays.py`,
원본 출력 `전략_v24_raw.txt`.

| 과제 | 상태 | 결과 |
|---|---|---|
| ③ 미국채 환노출형 재판정 | 완료 | **이미 있었다.** 305080/308620 이 환노출 -> v23 채택안 변경 |
| ② 한국 공휴일 시계 반영 | 완료 | 음력 직접 계산. **22년 실측과 거짓양성 0 / 거짓음성 0** |
| ① 괴리율 실측 | 부분 | iNAV 없이는 **상한만** 가능. 상한 + 분해까지 문서화 |

### [가장 중요] 과제 ① 을 하다 발견한 것 — 레버리지 실물 이탈

| 상품 | 실물 CAGR | 모형 CAGR | 연 이탈 | 전략 영향 |
|---|---:|---:|---:|---:|
| **TIGER 미국나스닥100레버리지** | **36.95%** | **41.45%** | **-4.50%p** | **-3.69%p/yr** |
| TIGER 미국배당다우존스 | 14.66% | 17.66% | -3.01%p | -0.22%p/yr |
| TIGER 미국채10년선물 | 2.77% | 4.65% | -1.87%p | -0.13%p/yr |
| ACE KRX금현물 | 25.69% | 26.78% | -1.10%p | -0.04%p/yr |

**방어자산 3종을 다 합쳐도 -0.39%p/yr 인데 레버리지 한 종목이 그 9배다.**
v21 §4.2 가 이미 alpha -3.57%/yr 를 실측했으니 새 사실은 아니고, 다른 기간·기준으로
크기를 재확인한 것이다.

**「순위」가 아니라 「수준」의 문제다.** 모든 방어자산 후보에 동일하게 걸리므로 v23 의
배합 판정은 안 바뀐다. 그러나 **모든 절대 수익 전망은 연 3~4%p 낮춰 읽어야 한다.**
원화 177.5배는 이 이탈이 빠진 값이고, 26.6년 복리로 적용하면 대략 1/2.6 수준이 된다.

### 과제 ② — 공휴일 (완료, 화면 반영됨)

`deploy/kr_holidays.py` 가 Meeus 알고리즘으로 삭·태양황경을 계산해 음력을 만들고
(동지 든 달 = 11월, 중기 없는 달 = 윤달), 증시 휴장일 표를 굳힌다.
KOSPI 실거래일 역산과 **2005~2026 전 구간 대조: 거짓양성 0 / 거짓음성 0.**
9개 연도의 설·추석·부처님오신날 27개 날짜 전부 일치.

- `data/kr_holidays.json` (2026~2032, 110일) — `python deploy/kr_holidays.py --emit`
- `signal.html` 이 부팅 때 읽고, **없으면 주말만 반영하는 기존 동작으로 조용히 되돌아간다**
- 휴장일에는 `한국장 휴장 · 설날` 처럼 사유까지 뜨고 다음 개장일까지 카운트한다
- **임시공휴일·선거일은 알고리즘이 못 맞힌다** -> `SPECIAL` 딕셔너리에 손으로 넣는다.
  화면이 하루 틀려도 신호 판정에는 영향이 없다(판정은 미국 종가 기준).
- **2032년까지다.** 그 이후는 `--emit` 을 다시 돌려라.

### 과제 ① — 괴리율 (부분 완료)

진짜 괴리율(시장가 - iNAV)은 KRX/발행사 iNAV 가 있어야 하고 못 구했다. 대신:
- **상한**: 이론가 대비 잔차 표준편차. 배당 0.91% / 국채 0.69~0.74% / **금현물 2.24%**
- **분해**: 시초가 갭이 일중에 되돌아오는 정도로 "마찰 vs 정보"를 가른다
  - 미국채 2종 **되돌림 45~46%** — 거래대금 9~17억으로 얇아서다. 회당 0.2%p 안팎 추가 마찰.
    §4.2 비용 민감도(편도 0.5% 까지 우위 불변)를 통과하므로 결론은 안 바뀐다
  - **ACE KRX금현물은 되돌림 0** — 잔차 2.24% 는 괴리가 아니라 **KRX 금시장 가격이
    국제 금가격과 실제로 다르게 형성되는 것**이다(국내 수급 프리미엄, 범위 0.911~1.174)

---

## [2026-08-26 추가 6] v25 — 남은 과제 3건 완결 + v24 정정 2건

결과는 `전략_v25.md`, 원본 출력 `전략_v25_raw.txt`.

### ⚠️ 먼저 — v24 §3.3 / §3.4 는 무효다

두 가지 오류가 있었다.
1. `hist_defasset.kr()` 이 **분배금 미반영 종가**를 읽었다. 초기 3종(133690/418660/458730)만
   Close(미조정)와 AdjClose 를 따로 갖는데 로더가 Close 를 집었다.
   배당 ETF 는 3.2년 누적 10.9%(연 3.2%p) 짜리 가짜 이탈이 생겼다.
2. 레버리지 이론가에 `c_daily` 를 빼지 않아 **비용 0인 합성**과 비교했다.

고치면 **레버리지 이탈은 -4.50%p 가 아니라 +0.19%p** 다.
**"절대 수익 전망을 연 3~4%p 낮춰 읽어라"는 v24 지시는 철회한다.**

### 과제 ① 진짜 괴리율 — 완결

**KRX 는 로그인 필수로 바뀌었다**(getJsonData.cmd 가 본문 "LOGOUT" + 400).
발행사 ajax·ETF체크도 막혔다. 대신 **네이버 금융 ETF 전종목 목록**이 열려 있다:

    https://finance.naver.com/api/sise/etfItemList.nhn   # nowVal 과 nav 를 함께 준다

`deploy/nav_collect.py` 를 만들어 `daily-signal.yml` 에 물렸다(실패해도 신호 갱신은 살린다).
**과거는 여전히 복원 불가지만 오늘부터 매일 쌓인다.**

실측 (2026-08-26): 305080 **+0.032%** / 458730 **+0.156%** / 411060 **+0.314%**
대조군 국내 전체 1,158종목 중앙 -0.023%, 표준편차 0.380%.
-> **v24 §3.1 의 상한(0.69~2.24%)이 과했던 것이 확인됐다. 잔차 대부분은 시차였다.**
   **괴리율은 이 전략에서 걱정할 항목이 아니다.**

### 과제 ② 레버리지 이탈 — 완결: 반영할 것이 없다

원화 엔진이 실제로 쓰는 모형(`2x(지수+환) - c_daily`)과 대조하면:

| 상품 | 실물 CAGR | 엔진 모형 | 차이 |
|---|---:|---:|---:|
| **TIGER 레버리지 418660** | **37.28%** | **37.08%** | **+0.19%p/yr** |
| 1배 대조군 133690 | 20.09% | 20.60% | -0.52%p/yr |
| (참고) 미국 QLD | 25.05% | 24.91% | +0.15%p/yr |

**모형이 정확하다.** 1배 대조군의 -0.52%p 가 시차·추적오차의 바닥이고 레버리지는 그보다 낫다.
이중차분(환율·시차 완전 제거)으로도 배당 ETF 는 -2.64%p -> **+0.41%p** 로 뒤집혔다.

남은 것은 국채 다리의 선물 롤오버 비용(전략 영향 -0.13~-0.19%p/yr)뿐이고,
**v23 §4.2 비용 민감도(편도 0.5%)를 통과하므로 배합 판정 불변.**

주의 — `c_daily` 는 `mean(2*r_QQQ - r_QLD)` 라 **beta 미달까지 포함한 평균 미달분**이다.
그리고 **달러 백테스트는 2006-06-22 이후 실물 QLD 를 그대로 쓴다**(hist_data.py:122).
합성은 그 이전에만, 원화 엔진만 전 구간 합성이다.

### 과제 ③ 임시공휴일 — 완결: 제헌절이었다

v24 가 KOSPI 데이터에서 역산해 SPECIAL 에 '임시공휴일'로 넣었던 2026-07-17 은
**제헌절 복원**이었다. 1949~2007 공휴일 -> 2008~2025 제외 -> **2026 복원**
(공휴일법 개정 2026-01-29 본회의 통과). **대체공휴일은 2027년부터.**

`ERA` 를 복수 구간으로 바꿔 반영했고 **검증은 여전히 거짓양성 0 / 거짓음성 0**.
2027 산출이 언론 연휴 목록과 일치한다(7/17 토 -> **7/19 월 대체** 포함).
`data/kr_holidays.json` 을 2026~2032 (116일) 로 재생성했다.

---

## [2026-08-26 추가 7] 화면 정리 — 종목은 다리마다 딱 하나

사용자 지시: **"무조건 1개씩만 추천해줘. 전략은 단순해야 가치가 있으니까. 채권 1개 금 1개."**

| 바뀐 것 | 내용 |
|---|---|
| `data/signal.json` | `defensive.legs` 에서 **`alt` 필드를 제거**했다. 다리당 종목 하나 |
| `hist_defasset.MIX_LEGS` | 같이 제거 |
| `signal.html` | **공격 상태에서도 "전환되면 살 것" 으로 바스켓 티커를 미리 띄운다.** 위험자산에도 코드(418660)를 붙였다 |
| `전략_v23.md` §7 / HANDOFF | 권고 표에서 대안 열 삭제. 왜 이 셋인지는 산문으로만 남겼다 |

### [중요] Pages 배포 누락 버그를 같이 고쳤다

**`.github/workflows/pages.yml` 이 `signal.json` 과 `strategy_stats.json` 만 복사하고 있었다.**
그래서 `data/kr_holidays.json` 이 라이브에서 **404** 였고 시계가 주말만 반영하고 있었다
(로컬에서는 정상이라 안 보였다). `cp` 한 줄을 추가했다.

**교훈: 새 데이터 파일을 만들면 pages.yml 의 「사이트 파일 모으기」에도 넣어야 한다.**
지금 복사 대상: `signal.json` / `strategy_stats.json` / `kr_holidays.json`.

### 화면 최종 모습

공격 상태
```
나스닥100 2배 · 100% TIGER 미국나스닥100레버리지(합성) 418660
─────────────────────────────────────────
전환되면 살 것
40% TIGER 미국배당다우존스 458730
40% TIGER 미국채10년선물   305080
20% ACE KRX금현물          411060
```

방어 상태
```
40% TIGER 미국배당다우존스 458730
40% TIGER 미국채10년선물   305080
20% ACE KRX금현물          411060
배당 40 / 미국채 40 / 금 20 · 도피 구간 안에서 월 1회
```

---

## [기각 기록] VR 밴드 리밸런싱 하이브리드 (`axis_vrhybrid.py`)

이전 세션에 만들어져 커밋되지 않은 채 남아 있던 파일이다. 2026-08-26 에 실행·검증하고
정식으로 기록한다. **결과는 완전한 기각이다.**

**가설**: 리스크온 구간을 100% 이진으로 두지 말고 VR 5.0 식 밴드 리밸런싱(목표평가액 V 대비
±band 벗어나면 조정)을 얹으면, 변동성과 MDD 가 줄고 특히 이 전략 최대 약점인
톱니장(1987-10~1988-12, v20 §6)에서 덜 다칠 것이다.

**결과 — 전부 반대로 나왔다.**

| 검증 | 결과 |
|---|---|
| 2000-2026 최종배수 | 기존안 **151.3** vs 하이브리드 117~142 (G·band 42조합 **전부 열위**) |
| 롤링 승률 | 1년 36.8% / 5년 21.7% / 10년 5.7% / **15년 0.2%** |
| 초과 CAGR 중앙값 | -1.1 ~ -2.4%p (창이 길수록 나빠진다) |
| **87 톱니 MDD 개선** | **0.00%p** — 가설의 핵심 시험대에서 실패 |
| 위기 6종 MDD 개선 | 2022 베어만 +1.25%p, 나머지 전부 0.00 |
| 하위구간 4개 | 3개에서 열위 (2000-2009 만 1.089배로 우세) |

**왜 위기 개선이 0 인가 — 구조적이다.** 도피 구간에서는 하이브리드 = 기존안이고,
위기의 대부분은 도피 상태로 보낸다. VR 밴드는 **리스크온 구간에서만** 작동하므로
위기 MDD 를 구조적으로 건드릴 수 없다. 가설 자체가 기전과 맞지 않았다.

**MDD 는 5%p 안팎 낮아지지만 수익이 20~40% 깎인다.** Calmar 는 0.42 -> 0.43~0.44 로
거의 그대로다. 즉 위험조정 개선도 없다.

> 이 축은 소진됐다. **리스크온 구간을 잘게 쪼개는 방향은 다시 열지 마라.**
> v22 §3 의 앙상블 기각, v18 §4-2 의 낙폭 사다리 기각과 같은 결론이다 —
> **부분비중은 레버리지에 안 맞는다.**

---

## [2026-08-26 추가 8] v26 — 적립식 재검증 (사용자 질문: "적립식이어도 상관없는 전략인거지?")

결과는 `전략_v26.md`, 재현 코드 `axis_accum2.py`, 원본 출력 `전략_v26_raw.txt`.

### 답: 된다. 거치식보다 오히려 낫다. 불규칙해도 상관없다.

v22 축3 이 적립식을 봤지만 (1) 방어자산이 **배당100** 이었고 (2) **고정 납입**만 봤다.
둘 다 지금과 다르므로 다시 쟀다. 납입 규약 6종을 새로 정의했다:
고정 / 불규칙(로그정규 σ=0.8) / 거르기(30% skip) / 몰아넣기(20% 확률 5배)
/ **추격**(전월 상승 시 2배) / **역추격**(전월 하락 시 2배).

**원화 20년 롤링, 납입액 대비 배수 (고정 납입)**

| 정책 | 중앙값 | 10%분위 | 최악 | 경로MDD중앙 |
|---|---:|---:|---:|---:|
| QQQ 적립(1배) | 4.52 | 3.14 | 2.83 | -77.04% |
| QLD 그냥 적립(2배) | 5.14 | 2.36 | **1.85** | **-97.89%** |
| 전략A -16/-11 | 29.98 | 9.85 | 6.25 | -51.01% |
| **전략B -16/-16** | **37.87** | **13.19** | **8.99** | **-51.81%** |

**전략B 가 QLD 그냥 적립의 중앙 7.4배 / 최악 4.9배.** 경로 MDD 는 -97.9% -> -51.8%.
격차가 창 길이에 비례해 벌어진다: 10년 1.07배 -> 15년 2.45배 -> 20년 7.4배.
(적립식은 시간이 갈수록 잔고가 커지므로 도피 기전의 값어치가 커진다)

**납입 규약별 중앙값 (20년, 전략B)**
고정 37.87 / 불규칙 37.14 / 거르기 36.23 / 몰아넣기 36.73 / 추격 37.65 / 역추격 38.12
-> **편차 5.2%, 순위는 6개 규약 전부 동일.**
   납입을 어떻게 하느냐보다 무엇을 사느냐가 100배 이상 중요하다.

몰아넣기만 좌측꼬리를 갉는다(20년 최악 8.99 -> 7.67, -15%). 그래도 QLD 그냥 적립의 5.5배다.
추격 vs 역추격은 1~16% 차이로 역추격이 낫지만 **못 지켜도 전략은 안 무너진다.**

### 실무 — 규칙은 하나다

**돈을 넣는 날, 그 시점 신호가 지시하는 쪽에 넣는다.** 공격이면 418660, 방어면 40/40/20.

**최소 월납입 15만원** — 국내 ETF 는 1주 단위이고 비중이 가장 작은 금(20%)이 병목이다.
ACE KRX금현물 29,050원 / 0.20 = 145,250원. 그 미만이면 3종을 쪼개지 말고
**비중이 가장 미달한 것 하나만** 사라 — 결과는 같고, **납입 자체가 리밸런싱을 대신한다**
(v23 §3.1 의 월 1회 리밸런싱 규약을 적립식이 자동으로 충족한다).

`axis_accum2.py` 의 `min_contribution()` 이 `data/nav_history.csv` 의 현재가로 다시 계산한다.

### 이 검증에서 새로 정한 규약

**원화 롤링 창의 시작을 1981-04-13 이후로 제한한다**(DEXKOUS 시작).
그 이전은 환율 데이터가 없어 forward-fill 되므로 환효과가 0 이 되고, 창이 오염된다.
`axis_accum2.FX_START` 로 박아 뒀다. **원화 기준 롤링을 새로 짤 때 반드시 지켜라.**

---

## [다음 세션] 남은 과제

### ① 위기 국면 괴리율 — 수집기가 쌓기를 기다린다

`data/nav_history.csv` 가 매일 한 줄씩 쌓인다. **다음 큰 조정(낙폭 -10% 이상) 때**
`python deploy/nav_collect.py --report` 로 괴리가 벌어지는지 확인해라.
평시 ±0.3% 가 위기에 얼마나 벌어지는지가 유일하게 안 본 것이다.

### ② 국채 다리 롤오버 비용을 백테스트에 넣을 것인가

전략 영향 -0.13~-0.19%p/yr. 작아서 판정을 안 바꾸지만 절대 수치는 그만큼 낙관이다.
넣으려면 `hist_defasset.ust_tr()` 에 연 1.4~2.1%p 를 빼는 옵션을 두면 된다.
**단, 표본이 8년(305080 상장 이후)뿐이다.**

### ③ 임시공휴일 — 정부 발표가 나오면 `SPECIAL` 에 추가

`deploy/kr_holidays.py` 의 `SPECIAL` 딕셔너리. 선거일도 여기다.
화면이 하루 틀려도 신호 판정에는 영향이 없다(판정은 미국 종가 기준).

### 하지 마라

- 방어자산 가중치를 더 촘촘히 훑기 — 평지 확인 완료(v23 §4.1)
- 환헤지 상품을 방어자산에 넣기 — 원화 기준으로 확실히 진다(177.5 vs 148.3)
- **원화 MDD 를 방어자산으로 줄이려는 시도 — 원인이 환율이라 불가능하다**
- **상품 표기만 보고 환헤지/환노출을 단정하기** — 그것 때문에 결론이 한 번 뒤집혔다
- **`실물 CAGR - 무비용 합성 CAGR` 로 이탈을 재기** — v24 가 그래서 틀렸다.
  반드시 **엔진이 실제로 쓰는 모형**과 대조하고, 1배 대조군으로 시차 바닥을 빼라
- **분배금 반영 여부를 확인 안 하고 국내 ETF 시계열 쓰기** — `DA.kr()` 을 써라
- **리스크온 구간을 부분비중으로 쪼개기** — VR 하이브리드 기각(위 참조), 앙상블 기각(v22 §3),
  낙폭 사다리 기각(v18 §4-2). 세 번 같은 답이 나왔다
- **낙폭을 기다리며 납입금을 현금으로 쌓기** — v22 축3 기여 0, v26 에서 재확인
- **원화 롤링 창을 1981-04 이전부터 잡기** — 환율 데이터가 없어 창이 오염된다
- **방어자산을 신호(모멘텀·낙폭·MA)로 골라 담기** — v27 에서 관문 5개 전부 탈락.
  이긴 규칙은 동전던지기 분포의 82백분위였고 워크포워드 3전 3패였다
- **방어를 현금으로 대체하기** — 시험한 모든 정책 중 꼴찌(-33%), MDD 도 더 나쁘다
- **일간수익 비율을 만들 때 분모를 비용 차감 뒤에 재기** — v23~v26 이 그래서
  월간 재조정 수수료를 한 번도 안 물렸다(v27 §7 정정)
- **신호를 국내 ETF 종가나 원화환산 가격으로 재기** — v28 에서 기각.
  환율이 도피 판정일의 낙폭을 3.1%p 얕게 만든다. 1997- 원화 -92%, MDD +21%p
- **VIX·하이일드·공포탐욕 등 매크로/심리 지표를 신호에 더하기** — v30 에서 전부 기각.
  **변화량** 기준 시차상관이 동시점 0.58~0.70, 모든 시차 |0.04| 미만이다(완전한 동행).
  새 정보 없이 전환만 12->74 회. ※ v30 초판은 이걸 **수준**끼리 재서 증거가 무효였다(v31 §1-1)
- **시차상관을 수준(level)끼리 재기** — 자기상관 큰 계열끼리는 참·거짓과 무관하게
  **항상 0일에서 최대**가 나온다. 반드시 1차차분(변화량)으로 재라 (v31 §1-1)
- **분위 문턱을 전표본(np.percentile)으로 잡기** — 그날 몰랐을 정보로 문턱을 정하게 된다.
  `expanding().quantile().shift(1)` 을 써라 (v31 §1-3)
- **'두 지표가 닮았다'를 점수 상관으로 주장하기** — 상관 0.918 이어도 **신호일 자카드
  겹침은 61.7%** 였다. 전략을 좌우하는 건 '언제 켜지는가'다 (v31 §1-5)
- **판정 문구를 하드코딩하기** — v30 초판이 그래서 관문 4개를 통과한 후보를
  통째로 놓쳤다. 판정문은 계산 결과에서 생성해라 (v31 §4)
- **매크로를 복귀(재진입) 필터로 쓰기** — 5종 전부 악화(-8.5%~-89.9%).
  반등은 공포가 가장 클 때 시작된다 (v31 §5)
- **장단기 금리차로 타이밍 잡기** — 진짜 선행한다(11/11, 중앙값 174일). 그러나
  산포가 7~533일이라 못 쓴다. MDD 개선은 0 이다 (v31 §3)
- **변동성 조건부 조기방어** — 최종 **기각**. v31 의 근거 2개는 틀렸지만(v32 가 취소),
  v32 가 **적립식**으로 재판정해 다시 기각했다. 20년 창 승률 2~29%, 중앙값 19~50% 하락.
  판정은 `전략_v32.md` §6, 코드 `axis_volguard.py`
- **거치식 결과를 적립식에 그대로 적용하기** — 이 축에서 결론이 뒤집혔다.
  적립 중에는 낙폭이 매수 기회다 (v32 §6)
- **격자를 좁게 보고 '첨탑' 이라 판정하기** — v31 이 그래서 틀렸다. 최적점 주변
  **전 방향**으로 넓히고, 경계에 붙어 있으면 격자 밖까지 늘려라 (v32 §1-1)
- **워크포워드에서 수익 기준으로 파라미터 고르기** — 수익 표면은 울퉁불퉁해서
  잡음을 고르게 된다. Calmar 로 골라라. **더 나은 건 아예 안 고르는 것**이다:
  고정 7~8/9 vs 선택 5/9 (v32 §4)
- **효과가 평지 위에 있을 때 최적화하기** — 정보가 아니라 잡음을 더한다 (v32 §4)
- **짧은 창(2011-)에서 통과한 신호를 믿기** — v30 §4 의 역발상은 2011- 에서 관문 3개를
  통과했지만 1972- 로 늘리자 -43%, 닷컴 구간 -51%p. 2011-2026 은 레버리지에 유난히 관대하다
- **매크로 지표를 쓸 때 `pos = w.shift(1)` 검산을 건너뛰기** — v30 §1. 빠른 지표는
  체결규약을 어기면 결론의 **부호가 뒤집힌다**(VIX 조기경보 +37.6% -> -9.2%)
- **`signal.json` 의 `state` 문자열 이름 바꾸기** — 'SCHD' 는 v18 잔재지만
  히스토리 파일·화면이 같이 쓴다. 가독성밖에 못 얻고 과거 기록이 어긋난다
- **ISA 를 '400만원 비과세 계좌'로 이해하기** — 이 전략 규모에서 비과세 기여는 **0%** 다.
  값어치는 전부 **과세이연**(이득의 80%)에서 나온다 (v29)
- **ISA 3년 만기에 해지하기** — 20년 기준 -15%. 게다가 불어난 돈은 재납입 한도(연 2천)에 걸려
  다시 못 넣는다. 만기를 연장해라
- **화면에 새 수치를 넣을 때 JSON 을 새로 만들기** — 정적 수치는 HTML 에 인라인해라.
  새 data 파일은 `pages.yml` 복사 목록에 반드시 추가해야 하고, v25 에서 그걸 빠뜨려 404 가 났다


---

## [2026-08-27 추가 9] v27 — 방어 안에서도 고를 것인가 (기각)

사용자 질문: *"QLD 에서 방어로 들어갈 때 배당·국채·금의 장 상황에 따라 배당100 으로
갈지 40/40/20 으로 나눌지 정하면 좋지 않나? QQQ 의 DD 처럼."*
*"벌 생각 없으면 현금으로 빼면 되잖아? 그럼에도 방어자산을 산다는 건 방어 안에서도
투자한다는 방향성이잖아."*

결과는 `전략_v27.md`, 재현 코드 `axis_defsel.py`, 원본 출력 `전략_v27_raw.txt`.

### 판정: 기각. 채택안 변경 없음

선택규칙 11종(절대/상대모멘텀·낙폭배제·MA200·역변동성·모멘텀가중·동일가중·현금·배당100)을
관문 5개에 걸어 전부 떨어뜨렸다.

| 관문 | 결과 |
|---|---|
| ① 예측력 | 배당·국채 t≈0. 금만 12M 모멘텀 t=3.86 이나 전략 지분 0.20x0.18=3.6% |
| ② 적중 | 모멘텀 1등이 다음 도피의 실제 1등을 맞힌 비율 **23% < 무작위 33%** |
| ③ 첨탑 | 유일 승자(상대모멘텀 2등까지)는 252일만 +16%, 189일 -6%, 378일 -15% |
| ④ 플라시보 | 그 +16% 는 무작위 2-of-3 규칙 분포의 **82백분위**. 95% 미달 |
| ⑤ 워크포워드 | 학습 1등이 OOS 에서 **3전 3패** (-32.7% / -36.1% / -2.9%) |

### 반드시 기억할 것 — 왜 낙폭 규칙이 방어자산엔 안 통하는가

**낙폭 규칙은 레버리지를 끊는 도구지 자산을 고르는 도구가 아니다.**
현행 -16% 규칙조차 QQQ 수익 예측에서는 t=1.44 로 유의하지 않다(v27 §2 대조군).
그게 사는 이유는 2배 상품의 변동성 손실을 끊기 때문이다.
**1배 방어자산에는 끊을 변동성 손실이 없으므로 같은 유비가 성립하지 않는다.**
다음 세션에서 "○○에도 낙폭 신호를 걸어보자"는 아이디어가 나오면 먼저 이걸 확인하라 —
**그 자산이 레버리지 상품인가?** 아니면 남는 건 수익 예측력뿐이고, 그건 대개 0 이다.

### 뒤집힌 소득 — 분산 자체가 알파다

도피 구간 날짜만 이어붙인 연율수익(도피 = 전체의 18%, 10.0년):

| 성분 | 도피중 CAGR |
|---|---|
| 배당 | 8.57% |
| 금 | 8.55% |
| 국채5Y | 7.92% |
| 현금 | 4.99% |
| **바스켓 40/40/20** | **9.32%** |

**바스켓이 자기 성분 셋을 전부 이긴다.** 재조정 프리미엄이고, 고르는 순간 버리는 몫이다.
현금 방어는 최종 -33%, MDD 도 -63.1% 로 바스켓(-59.7%)보다 나쁘다 —
위기에 버는 자산이 있으면 낙폭 바닥 자체가 얕아지기 때문이다.

### 부수: 비중은 고원이다

동일가중 1/3 이 현행과 +1.8%/+2.0% 로 사실상 동률. **40/40/20 을 소수점까지 지킬 필요 없다.**
v26 의 "월 15만원 미만이면 한 다리만 사라"가 안전한 근거가 여기 있다.

### 정정 — 월간 재조정 비용이 안 물려 있었다 (v23~v26 전체)

`mix_monthly_parts` / `mix_monthly_from` 이 일간수익 비율의 **분모를 비용 차감 뒤에**
재고 있었다. 분자·분모에서 비용이 약분돼 5bp 가 한 번도 안 물렸다.
`hist_defasset.py` · `axis_defmix.py` · `axis_defsel.py` 세 곳 수정.

영향: 54년 최종배수 263,557 -> **263,062 (-0.19%)**. 결론 불변.
`data/strategy_stats.json` 재생성 완료(사이트 수치 동기화).

**이 버그가 이 축에서는 컸다** — 선택규칙은 연 회전율이 0.4~1.8회라 공짜로 돌리고 있었다.

### 원화는 순위를 바꾸지 않는다

원화 1981- 에서 배당100 이 +12.2% 로 1등처럼 보이지만 **달러 동일창에서도 +12.2%** 다.
이중차분 0.0%p — 통화가 아니라 창 효과다. 앞으로도 원화 결과가 달러와 달라 보이면
**반드시 달러 동일창 대조군을 먼저 붙여라**(v24 에서 쓴 방법과 같다).


---

## [2026-08-27 추가 10] v28 — 신호를 어느 종가로 잴 것인가

사용자 질문: *"한국 추종 ETF 종가로 잡는 게 맞나 미국으로 보는 게 맞나? QQQ, SCHD 두 종목.
TIGER 미국채10년선물·ACE KRX금현물은 괴리가 있으니 그 ETF를 기준으로 잡잖아?
두 개는 미국, 두 개는 한국으로 하는 게 맞는 걸까?"*

결과는 `전략_v28.md`, 재현 코드 `axis_sigsrc.py`, 원본 출력 `전략_v28_raw.txt`.

### 판정: 미국 QQQ 종가 유지. 변경 없음

### 먼저 층을 나눠라 (다음 세션에서 같은 질문이 또 나올 것이다)

| 층 | 무엇 | 가격 출처 |
|---|---|---|
| **신호원** | 공격/방어 판정 | **미국 QQQ 종가 하나뿐** |
| **체결 자산** | 실제로 사고파는 물건 | 국내 ETF 4종(418660 / 458730 / 305080 / 411060) |

- **SCHD 는 신호가 아니다.** `update_signal.py` 는 QQQ 하나만 받는다.
  코드의 `"SCHD"` 는 v18 때 방어자산이 SCHD 였던 시절의 **상태 이름**이고,
  지금은 40/40/20 바스켓을 뜻한다. 화면에는 '방어'로 나온다.
  **`state == "SCHD"` 를 "SCHD 를 사라"로 읽지 마라.**
- **국채·금에도 신호가 없다.** v27 에서 방어자산 선택을 기각했다. 판정 없이 그냥 산다.
  그 ETF 가격은 (a) 월간 재조정 주수, (b) 최소 월납입 계산에만 쓴다 — 둘 다 **실행**이다.
- 즉 **"미국 2 / 한국 2"가 아니라 "신호원 1(미국) + 체결자산 4(한국)"** 이다.

### 실측 (체결 조건 고정, 신호원만 교체)

원화 1997- · 규칙 B:

| 신호원 | 최종배수 | MDD | 전환 |
|---|---|---|---|
| **A 미국 QQQ 종가 (현행)** | **2,937.9** | **-55.94%** | 62 |
| B QQQ x 환율 | 246.4 (-92%) | -77.33% | 130 |
| D 미국 종가 1일지연 | 1,864.7 (-37%) | -52.46% | 62 |

원화 2011-11- 공통창(133690 상장 후):

| 신호원 | 최종배수 | MDD |
|---|---|---|
| **A 미국 QQQ 종가** | **81.6** | **-43.55%** |
| C 국내 133690 종가 | 69.3 (-15%) | -48.73% |
| D 미국 종가 1일지연 | 60.8 (-25%) | -48.03% |
| B QQQ x 환율 | 40.2 (-51%) | -62.53% |

### 기전 — 한 줄로

**QQQ 최악 5%일(= 도피가 실제로 걸리는 날)에 원화 기준 신호원은 낙폭을 3.1~3.8%p
얕게 본다.** 문턱 -16% 가 실질 -19.8% 가 되는 셈이다. 필요한 순간에만 무뎌진다.

주의: **체계적 편향이 아니라 노이즈다.** GFC 에는 6일 늦고 2011 유럽에는 9일 빨랐다.
낙폭이 현재가/252일최고가의 **비율**이라 환율이 분자·분모를 같이 흔들기 때문이다.
그래서 문턱 조정으로 상쇄할 수 없다. 2015 차이나에서는 아예 발동조차 안 했다.

### 부수 관찰

국내 ETF 종가(C)가 원화환산(B)보다 나은 건, 15:30 KST 마감가가 **당일 나스닥 선물**을
이미 반영해 전일 미국 종가보다 반나절 앞서기 때문이다. 그래도 A 는 못 이긴다.


---

## [2026-08-27 추가 11] v29 — ISA 서민형 세후 판정 + 화면 반영

사용자 조건: ISA 중개형·서민형. 연 2,000만원 / 5년 누적 1억(이월 가능) /
순이익 400만원 비과세 / 초과분 9.9% 분리과세.

결과는 `전략_v29.md`, 재현 코드 `axis_isa.py`, 원본 출력 `전략_v29_raw.txt`,
산출물 `data/isa_stats.json`.

### 결론

**ISA 로 굴린다. 일반계좌 대비 세후 +43.2%(20년 창 중앙값).**

납입 1억(연 2천 x 5년) 대비 세후 평가액, 롤링 창 중앙값:

| 계좌 | 10년 | 15년 | 20년 | 실효세율 |
|---|---|---|---|---|
| 세전(참고) | 6.31배 | 28.2배 | 133.0배 | — |
| **ISA 만기 연장** | **5.79배** | **25.5배** | **120.0배** | **9.9%** |
| ISA 3년마다 해지 | 5.53배 | 22.3배 | 101.5배 | 10.7% |
| 일반계좌 | 5.24배 | 19.8배 | 83.8배 | 16.5% |

### 반드시 기억할 것 — 값어치는 비과세가 아니라 이연이다

20년 창 분해: **과세이연 +34.5%(전체의 80%)** / 세율 15.4->9.9% +6.4%(15%) /
**400만원 비과세 +0.0%(0%)**.

이유: 이 전략은 원화 기준 **연 2.1회 전량 전환**한다. 일반계좌는 전환할 때마다
복리의 원금이 깎인다. 순이익이 수억이 되면 400만원은 반올림 오차다.

### 화면(signal.html) 변경 요약

추가: 계좌 패널(3칸 요약 + 계좌 4종 비교표 + 선택지 3개 + 놓치기 쉬운 것),
상단 고정 요약바(신호 패널이 스크롤 밖으로 나가면 상태·낙폭 고정), v27/v28 한 줄 반영.

고침: 기존 세금 문단이 **배당100 시절 수치**(2,705->1,062배)였다. 40/40/20 기준으로 갱신.

UI: iOS 입력창 확대 방지(16px), 모바일 탭 타깃 44px, 지표 그리드 5칸->2칸,
표 가로스크롤 안내, 모바일 여백 축소, 601~820px 중간 폭 대응, `color-scheme:light`.

**ISA 수치는 HTML 에 인라인이다.** 새 JSON 을 fetch 하지 않으므로 `pages.yml` 수정 불필요.
갱신하려면 `python axis_isa.py --emit` 후 `signal.html` 의 `#taxPanel` 표를 손으로 맞춰라
(그 자리에 주석으로 적어 뒀다).

### 검증

375px / 1280px 양쪽에서 가로 넘침 0, 클리핑 0, 콘솔 오류 0.
고정 요약바 토글: 스크롤 0/700 off -> 1500/3000 on -> 복귀 off.

주의: 이 환경의 브라우저 창은 프레임을 합성하지 않아 **스크롤 이벤트와
IntersectionObserver 콜백이 아예 안 뜬다.** 스크린샷도 안 된다.
그래서 토글 로직은 `sync()` 를 직접 호출해 검증했다. 다음에 같은 증상이 나오면
코드 버그로 오해하지 마라 — 기하값(getBoundingClientRect)으로 검증하면 된다.


---

## [2026-08-27 추가 12] v30 — 매크로·심리 지표는 신호가 될 수 없다

문서 `전략_v30.md`, 코드 `axis_macro.py`(2011-) / `axis_macro2.py`(1972-),
원본 출력 `전략_v30_raw.txt`. 새 원자료 `data/hist/yahoo_VIX.csv`(1990-),
`data/hist/yahoo_HYG.csv`(2007-).

질문: MD6(md6.today) 같은 대시보드의 경제지표·금리전망·소비방향·시장폭·VIX·
하이일드·공포탐욕지수를 모으면 전략이 정교해지나?

**답: 아니다. 전부 기각. 추가 데이터를 모을 필요가 없다.**

### 핵심 이유 — 동행지표다

QQQ 낙폭 대비 시차별 상관의 **최대점이 셋 다 정확히 0일**이다.

| 지표 | -10일 | -5일 | **0일** | +5일 | +10일 |
|---|---:|---:|---:|---:|---:|
| VIX z점수 | +0.331 | +0.365 | **+0.407** | +0.305 | +0.236 |
| HY스프레드 | +0.239 | +0.284 | **+0.338** | +0.315 | +0.290 |
| 공포탐욕 | +0.330 | +0.368 | **+0.419** | +0.342 | +0.274 |

선행지표가 아니다. 더하면 전환만 12회 -> 74회 로 늘고 톱니·비용만 얻는다.

### 함정 1 — 내 백테스트의 미래훔쳐보기 (반드시 기억할 것)

처음 짠 `bt()` 가 당일 신호로 당일 수익을 계산했다(규약은 `pos = w.shift(1)`).
VIX 는 폭락 **당일** 튀므로 이 위반의 영향이 절대적이다:

| VIX 조기경보 | 위반 시 | 규약 준수 |
|---|---:|---:|
| 최종배수 | 309.36배 | 37.10배 |
| 기존 대비 | **+37.6%** | **-9.2%** |
| 플라시보 | 0.0%(통과) | 82.8%(실패) |

**부호가 뒤집혔다.** 느린 252일 낙폭에서는 안 드러나던 함정이다.
검산: 고친 `bt()` 의 2011- 기준선 51.10배 = 프로젝트 엔진 `sim()` 51.2배.

### 함정 2 — 짧은 창에서 통과한 신호

"공포탐욕 극단(<20)이면 QQQ 신호 무시하고 공격 유지"가 2011-2026 에서
배수 +54.5%, 블록 플라시보 0.2%, OOS +4.47%p 로 **관문 3개를 통과했다.**
여기서 멈췄으면 채택했을 것이다. 1972- 로 늘리자:

| 구간 | 기존 | 역발상 |
|---|---:|---:|
| 전구간 1972- | 179,355배 | **-43.2%** |
| 2000-(닷컴) | 162.9배 | **-58.9%** |
| 2007-(금융위기) | 66.7배 | -14.5% |
| 2011-(원래 창) | 51.2배 | +50.6% |

MDD 는 긴 창 전부에서 -68.72% -> **-84.51%**. 닷컴 구간 기여 **-51.32%p**
(기존 +3.03% -> 역발상 -48.29%). 문턱 스윕은 첨탑(`<15`=+35.6%, `<20`=-43.2%),
블록 플라시보 73.4%, 워크포워드 OOS -58.9%.

### 함정 3 — 상관 0.918 인데 부호가 반대

같은 1990- 구간에서 변동성 성분만 바꿨더니: **VIX 판 +87.0% vs 실현변동성판 -27.9%.**
두 점수 상관은 +0.918. 거의 같은 지표가 반대 답을 내면 둘 다 잡음이다.

### 검증 없이 기각한 것

FOMC 금리전망(확정값이 아니라 매일 바뀌는 예상 = 그 자체가 잡음원),
CPI·실업률(월 1회, 발표 시점에 이미 반영), 소비방향 XLY/XLP·시장폭(동행지표 계열),
섹터 히트맵(정량화하면 파라미터 폭증).

**공통 이유: 이 전략은 위기를 예측할 필요가 없다.** 시작된 뒤 빠져나오기만 하면 된다.
252일 낙폭이 느리기 때문에 오히려 톱니에 강하다.

### 화면

`signal.html` 에 한 줄만 넣었다(v27/v28 과 같은 분량). 전략·수치 변경 없음.


---

## [2026-08-27 추가 13] v31 — v30 감사 + 남은 3축 종결

문서 `전략_v31.md`, 코드 `axis_macro3.py`, 원본 출력 `전략_v31_raw.txt`.
`전략_v30.md` 는 정정 배너를 달았고 `axis_macro.py` / `axis_macro2.py` 는
결함을 고쳐 재생성했다(`전략_v30_raw.txt`).

**v30 의 결론(매크로 전부 기각)은 유지. 근거는 절반쯤 다시 썼다.**

### v30 이 틀렸던 곳 (전부 정정 완료)

| 결함 | 영향 |
|---|---|
| 시차상관을 **수준**끼리 쟀다 | 자기상관 탓에 항상 0일 최대. 변화량으로 재니 증거가 더 강해짐 |
| 위기창 **안에서** 분위 계산 | 순환논리. 2020 COVID 선행일수 -11일 -> +11일 |
| 문턱에 **전표본 분위** | 미래 참조. HY 조기경보 -1.2% -> +2.1% (그래도 플라시보 11.6% 기각) |
| 공포탐욕만 **이틀 지연** | z() 와 sim() 이 각각 shift(1). 전구간 -43.2% -> -53.3% |
| "상관 0.918" 근거 | 신호일 **자카드 겹침 61.7%** 가 진짜 이유. +87.0/-27.9 -> +62.0/-23.0 |

### 새로 닫은 축 3개

**① 장단기 금리차(10Y-3M, 1962-)** — v30 이 'FOMC 전망'과 뭉뚱그려 안 쟀다.
**진짜 선행한다: 평시 역전 11건 전부 낙폭에 앞섰고 중앙값 174일.**
그런데 산포가 **7~533일**(76배)이라 못 쓴다. 규칙 4종 전부 MDD 개선 0.

**② 복귀 필터** — 5종 전부 악화(-8.5% ~ -89.9%). 반등은 공포가 가장 클 때 시작된다.

**③ 변동성 조건부 조기방어** — 가장 아까웠다. **관문 4개 통과:**
결정적 대조(낙폭 -5% 이하 날 중 10.0% 만 발동 = 문턱 낮추기와 다름),
전구간 +62.3% 에 MDD -68.72%->-65.93%, 위기별 손해 0건(1973 +11.28%p /
1987 +14.44%p / 2020 +9.43%p), 블록 플라시보 2.4%.

**탈락 이유 4개:** 파라미터 첨탑(룩백 42일에서 **부호 반전**),
워크포워드 -7.3%, 20년 창 승률 43.3%, 그리고 결정적으로 —
**이득이 1972-1985 나스닥 종합지수 합성 구간에 몰려 있다**(+45.6%).
실물 QQQ 구간(1999-)은 **+2.6%**, 1990- 는 **-20.7%**.

> 이 후보를 v30 이 놓친 이유: 초판 코드의 판정 문구가 하드코딩돼
> 출력은 "이긴다"인데 "진다"고 인쇄하고 있었다. **판정문을 하드코딩하지 마라.**

### 남는 사실 하나

변동성 조기방어의 **MDD 개선 2.8%p 는 모든 구간에서 일관됐다.**
"급락장에서는 변동성이 낙폭보다 먼저 벌어진다"는 메커니즘 자체는 실재한다
(1973·1987·2020 전부 급락). 다만 느린 하락장(2000·2022)에서는 기여가 정확히 0 이다.
나중에 MDD 를 줄이는 축을 다시 열면 여기서 출발하되, 파라미터 첨탑과
데이터 구간 편중을 먼저 해결해야 한다.

### 화면

변경 없음. v30 에서 넣은 한 줄이 그대로 유효하다.


---

## [2026-08-27 추가 14] v32 — 변동성 조기방어 재심. v31 기각 취소, 현재 미결정

문서 `전략_v32.md`, 코드 `axis_volguard.py`(본판정) / `axis_macro4.py`(격자 감사),
원본 출력 `전략_v32_raw.txt`. `전략_v31.md` §4 에 취소 배너를 달았다.

사용자 질문: "파라미터 첨탑과 데이터 편중을 해결하면 일관적으로 개선할 수 있나?"

**답: 첨탑은 애초에 없었다. MDD 는 개선되고 수익은 안 는다. 단 최적화하면 안 된다.**

### v31 이 틀린 곳

- **'첨탑' 은 격자의 엉뚱한 단면.** v31 은 룩백 21일 x 게이트 -5~-12% 만 봤다.
  워크포워드가 실제로 고르는 영역(룩백 5~14일 x 게이트 -1~-4%)까지 넓히면
  **42칸 전부가 MDD 를 개선한다.** 수익도 40/42 양수. 평지다.
- **워크포워드 실패는 수익 기준으로 골랐기 때문.** Calmar 로 고르면 수렴한다.

### 실전 조건 본판정 (실제 40/40/20 바스켓)

규칙: 실현변동성(10일) z >= 확장창 p92.5 AND 낙폭 <= -3% -> 즉시 방어. 발동 6.3%.

| | 배수 | MDD | Calmar | 전환 |
|---|---|---|---|---|
| 달러 A 기준 | 164,493 | -56.53% | 0.432 | 54회 |
| 달러 A + 가드 | 386,594 | **-45.85% (+10.69p)** | 0.575 | 182회 |
| **원화 A 기준** | 90,186 | **-64.21%** | 0.444 | |
| **원화 A + 가드** | 97,867 (+8.5%) | **-51.63% (+12.59p)** | **0.557** | |
| 원화 B 기준 | 180,562 | -67.40% | 0.452 | |
| 원화 B + 가드 | 170,964 (**-5.3%**) | **-53.14% (+14.26p)** | 0.571 | |

원화 B 는 **수익 -5.3% 인데 MDD 14.26%p 개선.** 이게 성격을 정확히 보여준다 —
**수익을 사는 도구가 아니라 위험을 파는 도구다.**

### 수익 이득은 가짜, MDD 개선은 진짜

| 구간 | 수익 | MDD |
|---|---|---|
| 1972-1985 합성 | +101.4% | +0.00p |
| 1985-1999 NDX | +33.9% | +10.69p |
| **1999- QQQ 실물** | **-12.9%** | **+7.08p** |
| 2011- | -0.7% | +0.00p |

롤링 창 **MDD 개선률 10/15/20년 전부 100.0%.** 수익 승률은 20년 창에서 43.3%.
블록 플라시보 수익 0/500, MDD 0/500.

### 가장 중요한 발견 — 파라미터를 고르면 안 된다

겹치지 않는 5년 창 9개, MDD 개선 창 수:

| | 달러 | 원화 |
|---|---|---|
| 고정 (10,p92.5,-3%) | 7/9 (+4.41p) | 7/9 (+4.65p) |
| 고정 (14,p90,-2%) | **8/9** (+3.98p) | **8/9** (+5.66p) |
| 고정 (10,p90,-4%) | **8/9** (+2.21p) | **8/9** (+5.66p) |
| **선택 (워크포워드)** | **5/9 (+0.66p)** | 7/9 (+5.66p) |

**최적화가 손해다.** 효과가 넓은 평지 위에 있을 때 최적화는 잡음을 더한다.

### 대가

수익 실물구간 -12.9% / 전환 54->182회(연 1->3.4회) / 편도비용 1% 넘으면 붕괴
(0.2% 에서는 +107% 로 견딤) / 2022-2026 창 기여 0.00p / 신호가 1개에서 2개로.

### 결정적 — 적립식에서 뒤집힌다 (v32 §6)

1~5 는 전부 **거치식**이다. 사용자는 ISA 에 **월 정액 5년 납입 후 보유**한다.
**적립 중에는 낙폭이 손실이 아니라 싸게 사는 기회다.** 조기방어가 그걸 없앤다.

원화 · 월 정액 60개월 · 20년 창 · 납입 60단위 대비:

| 규칙 | 방식 | 중앙 | 승률 | MDD최악 |
|---|---|---|---|---|
| A | 기준 | **106.86** | - | -69.8% |
| A | (14,p90,-2%) | **65.55** | **29.4%** | **-48.4%** |
| A | (10,p92.5,-3%) | 79.14 | **2.0%** | -59.2% |
| B | 기준 | **133.04** | - | -75.4% |
| B | (14,p90,-2%) | **66.27** | 23.5% | -58.0% |
| B | (10,p92.5,-3%) | 107.55 | **3.9%** | -66.8% |

**20년 창 승률 2~29%, 중앙값 19~50% 하락.** 규칙 B 는 절반이 날아간다.

### 현재 상태

**기각.** 거치식이면 살 만한 맞바꾸기지만 사용자는 적립식이다.
화면·라이브 파이프라인 변경 없음. 전략 그대로.

**교훈: 거치식 결과를 적립식에 그대로 적용하지 마라.** 이 축에서 결론이 뒤집혔다.


---

## [2026-08-27 추가 15] v33 — 적립 시뮬레이터 2일 지연 버그. v29 ISA 수치 정정

문서 `전략_v33.md`, 코드 `verify_volguard.py`, 원본 출력 `전략_v33_raw.txt`.

사용자 요구: "완벽한지 검증해줘. 절대 틀리면 안 된다." -> **검증했더니 틀린 게 나왔다.**

### 버그

적립 시뮬레이터 **4개 전부**가 이 순서였다:

    R *= (1 + rr[i])        # 그날 수익 먼저
    pos = w[i - 1]          # 그 다음 포지션
    if pos != prev: 전환    # 그 다음 전환

이러면 i일 수익이 **w[i-2]** 로 정해진 배분에 적용된다. 규약은 `pos = w.shift(1)`
= 1일 지연인데 **실질 2일 지연**이었다. 미래훔쳐보기는 아니지만(보수적) 규약 위반이다.

**정정: 포지션·전환을 그날 수익 적용 앞으로 옮겼다.**

| 파일 | 영향 문서 |
|---|---|
| `axis_lib.accumulate` | v22 축3 |
| `axis_accum2.accum` | v26 |
| **`axis_isa.accum_tax`** | **v29 — 라이브 사이트** |
| `axis_volguard` 내부 accum | v32 |

### 검산 (이제 상설화)

**납입 1회로 두면 그 시점부터의 거치식과 오차 0 이어야 한다.**

| | |
|---|---|
| 기존(2일 지연) | 499.011 vs 833.327 — **오차 -40.1%** |
| 정정(1일 지연) | 833.327 vs 833.327 — 오차 1.1e-16 |

`axis_lib.check_accum()` 신설. `check(D)` 가 자동으로 부른다.
**새 적립 함수를 만들면 반드시 통과시켜라.**

### v29 정정 수치 (화면 반영 완료)

| 계좌 | 10년 | 15년 | 20년 | 실효세율 |
|---|---|---|---|---|
| 세전 | 6.84배 | 31.5배 | 159.0배 | - |
| **ISA 만기연장** | **6.27배** | **28.5배** | **143.3배** | 9.9% |
| ISA 3년해지 | 5.96배 | 25.2배 | 120.5배 | 10.7% |
| 일반계좌 | 5.98배 | 23.6배 | 95.7배 | 16.9% |

**ISA vs 일반: +4.8%(10년) / +20.6%(15년) / +49.7%(20년)** (기존 +10.4/+28.5/+43.2)
분해: 과세이연 +40.7%(82%) / 세율 +6.5%(13%) / 400만 비과세 +0.0%(0%)

**주의: 10년 창에서 ②이연만(5.94)이 ①일반계좌(5.98)보다 나쁘다.**
전환비용이 이연 이득을 상쇄한다. 세율 인하까지 가야 뒤집힌다.

### v26 / v32 — 결론 불변

v26: 납입규약 6종 순위 그대로(전략B > A > QLD > QQQ), 최소 월납입 15만원 그대로.
v32: 가드는 적립식에서 여전히 패배(승률 24~33%, 중앙 -32~39%). 판정 유지.

### 이번 세션에서 내가 틀린 것 6가지 (기록)

1. v31 격자 단면 오판('첨탑') -> v32 §1-1
2. v31 워크포워드를 수익 기준으로 골라 실패 -> v32 §1-2
3. 적립식 MDD 를 초기 잔고 1단위 등락까지 세어 -69~75% 로 부풀림
4. 중앙값 대신 최악 창 값을 인용
5. 적립 시뮬레이터 2일 지연 (프로젝트 파일 4개)
6. 검증 하네스가 QLD 합성연장·SCHD 현금대용을 빼먹음(93.6 -> 96.4 -> 140.3배)

**공통 원인: 결과를 내기 전에 검산을 안 했다.** 5번은 "납입 1회 = 거치식"
한 줄이면 즉시 잡혔다.

### 하지 마라 (추가)

- **새 적립 함수를 검산 없이 쓰기** — `axis_lib.check_accum()` 을 통과시켜라
- **공표 수치를 재현할 때 데이터 구성 규약을 건너뛰기** — `verify.py` 는
  QLD 를 2006년 이전 2xQQQ 로 연장하고 SCHD 를 2011년 이전 현금 2% 로 대용한다.
  이걸 빼면 140.3배가 93.6배로 나온다
- **적립식 계좌 MDD 를 초기 구간 포함해서 재기** — 잔고가 1단위일 때의 등락이
  낙폭으로 잡힌다. 납입 종료 후 기준이나 원금대비로 재라
