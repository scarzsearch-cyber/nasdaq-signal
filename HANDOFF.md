# HANDOFF — 작업 시작 전에 이것부터

전체 이력은 `docs/HANDOFF_전체이력.md`(1,383줄). 이 파일은 **지금 알아야 할 것만** 담는다.

---

## 0. 먼저 실행할 것

```bash
python verify_all.py
```

**실패 0건이 아니면 아무것도 고치지 마라.** 먼저 왜 실패하는지 밝혀라.
(3초. `--fast` 는 1초, CI 가 매 push 마다 부른다.)

전략·자동화·파일 지도는 `README.md` 에 있다. **중복해서 적지 않는다.**

---

## 1. 현재 상태 (2026-08-27)

| | |
|---|---|
| 전략 | QQQ 252일 낙폭 **−16/−16 단일** · 방어 40/40/20 고정 (−16/−11 은 참조, 고를 수 없음) |
| 신호 | 자동. 예약 3슬롯 + 화면 신선도 경고 |
| 검증 | 자동. `verify.yml` 이 push/매일 실행, 실패하면 **이슈 자동 생성** |
| 현행 문서 | **루트 `01~04_*.md`** ([v65] 통폐합 — 버전 문서 43개는 `docs/history/` 보관) |
| 최신 판정 | `docs/history/전략_v88_최종검증.md` (최종 검증 — **J1 비용 ×3 생존 · J2 그림자 대기 · J3 실측 4/60세션**. 지연은 비선형: 1987 lag=2 MDD −72% → 당일 실행 엄수) |
| **동결** | **규칙을 바꾸지 마라.** `data/freeze.json` · `verify_all.py` I11 · `data/oos_log.csv` (v57) |
| T4 그림자 | 장부에 **평가 전용** 병기 (v69). **판정은 `docs/history/전략_v80` §6·§7 부속서가 우선** — 사건 단위 M1·M2 기전 관문, 한도 −29% 고정, 혼합 0.25B 병기. 파라미터·부속서 수정 금지. 개선 시도는 전멸 (04 §5-1) |
| 미결 | 아래 §4 |

---

## 2. 하지 마라 (전부 시험하고 기각된 것)

> [v82] 기각은 **시험된 맥락 안**에서만 유효하다. 구조가 다른 전략에 적용하려면
> 그 맥락에서 재시험하라 (예: 시간 잠금 — B 기각(v55) 후 T4 에서 재시험, 별도 기각(v82)).

### 전략 축

- **방어자산을 신호로 골라 담기** — 관문 5개 전부 탈락(v27).
  스윕 첨탑 · 플라시보 82백분위 · 워크포워드 3전 3패
- **방어를 현금으로 대체** — 바스켓에 −17.85% 로 진다. MDD 도 나쁘다(v27/v36)
- **리스크온을 부분비중으로 쪼개기** — VR 하이브리드·앙상블·낙폭사다리 세 번 기각
- **B+T4 구조 결합 / T4 부품 적출** — 23변형 전멸 (v87, 관문 N1~N8 사전 고정).
  괴리 비대칭: "T4만 경고" 900일의 앞 63일 꼬리는 무조건부보다 **얕다**(−21.8 vs
  −46.1%) — 감속 이식은 평시 보험료만 낸다. "T4만 복귀"는 곰랠리 꼬리(−40.8%).
  분해 실측: 게이트만 MDD −82% · 타깃만 −85% — **알파는 결합에만 있다**
- **낙폭 기다리며 납입금을 현금으로 쌓기** — 기여 0 (v22 축3, v26 재확인)
- **VIX·하이일드·공포탐욕 등 매크로 지표 추가** — 전부 동행지표.
  **변화량** 시차상관이 동시점 0.58~0.70, 모든 시차 |0.04| 미만 (v30/v31)
- **장단기 금리차로 타이밍** — 진짜 선행한다(11/11, 중앙값 174일). 그러나
  산포가 7~533일이라 못 쓴다. MDD 개선 0 (v31)
- **매크로를 복귀 필터로** — 5종 전부 악화. 반등은 공포가 클 때 시작된다 (v31)
- **변동성 조기방어** — **기각 확정.** 적립식 20년창 승률 24~33%, 중앙값 −32~39%.
  이유는 "싸게 사는 기회를 뺏겨서"가 **아니다**(매수시 평균낙폭 −4.85 vs −4.83 동일).
  **오경보 54%** 로 레버리지 노출이 78%→73% 로 주는 것이다. 비용은 차이의 2%뿐 (v32/v40)
- **신호를 국내 ETF 종가/원화환산으로 재기** — 1997- 원화 −92%, MDD +21%p (v28)
- **A(−16/−11)를 메인으로 되돌리기** — v21 이 권했지만 **근거 두 개가 틀렸다**(v43).
  한국 체결·슬리피지 0.1% 반영 후에도 **B 가 1.28배** 앞선다. 갭 분산(2.58%)을
  전환마다 물려도 **1.26배, B 승률 84%**. A 가 이기려면 슬리피지가 **편도 1.0%**
  (실제의 10배)여야 한다
- **다른 시장 데이터로 표본 늘리기** — 규칙이 시장마다 다르다. 같은 −16/−16 을
  S&P500·코스피에 적용하면 **그냥 보유에 진다**(69.3 vs 161.7 / 4.4 vs 5.9).
  최적 진입선도 S&P500 −19%, 코스피 −13%/−23% 로 다르다 (v44)
- **목적함수를 바꿔 다시 뒤지기** — 6종(최종금액·좌측꼬리·언더워터·원금손실·
  CRRA γ=2,5)으로 훑었다. 현행이 CRRA γ=2 **1위**, 좌측꼬리·CRRA γ=5 **3위**.
  최고 후보 −15/−15 도 3/6 이고 겹치지 않는 4블록에서 **파레토 밖**이다 (v44)
- **보조전략을 새로 넣기** — 낙폭 문턱 변형은 전부 B 와 **상관 0.92~1.00** 이라
  선택지가 아니라 중복이다. 유일한 저상관 −11/−11 은 **2000년 이후 0승 27패** (v43)
- **진입선을 −12~−13% 로 얕게** — Calmar 는 +11~17% 오르고 MDD 도 −60%→−44%.
  **그런데 20년창 5분위가 35.9 → 21.2 로 무너진다.** 자주 도피하면 회복을 놓친다.
  231개 조합 중 문턱을 넘은 8개가 전부 여기서 죽었다 (v41)
- **쿨다운 복귀 / V자 조기복귀 / 룩백 변경 / 방어 재조정 주기** — 전부 문턱 미달 (v41)
- **낙폭을 매수 기회로 쓰기(납입금만 레버리지로)** — 6종 전부 기각.
  73% 창에서 이기는데 평균 −1.5% (지는 27% 가 5배 크다). 좌측꼬리는 전부 악화
  (20년 최악 18.00 → 14.79). **2배는 −50%에서 −75%가 된다** (v40)

### 방법론 (여기서 12건이 났다)

- **엔진을 재구현하기** — `axis_lib.sim` / `sim_def` / `sim_hold` 를 써라.
  새로 짜면 기존 엔진과 **오차 0** 검산이 먼저다
- **새 적립 함수를 검산 없이 쓰기** — `axis_lib.check_accum()` 을 통과시켜라
  (납입 1회면 거치식과 오차 0). 이게 없어서 4곳이 2일 지연이었다
- **일간수익 비율의 분모를 비용 차감 뒤에 잡기** — v23~v26 이 재조정 수수료를
  한 번도 안 물렸다
- **공용 모형을 바꾸고 사용처를 안 돌리기** — `verify_all.py` I8 이 목록을 뽑는다.
  안 돌렸더니 v27 의 근거 문장이 뒤집힌 채 남아 있었다
- **판정문에 숫자를 하드코딩하기** — `audit_full.py` 스캐너도 서술형은 못 잡는다
- **격자를 좁게 보고 '첨탑'이라 판정하기** — 전 방향으로 넓혀라. 경계면 격자 밖까지
- **워크포워드를 수익 기준으로 고르기** — Calmar 로 골라라.
  **더 나은 건 아예 안 고르고 못박는 것**(고정 8/9 vs 선택 5/9)
- **판정 게이트를 '최종배수'로 걸기** — v23 근거는 좌측꼬리·위기방어다
- **적립식 MDD 를 초기 구간 포함해 재기** — 잔고 1단위 등락이 낙폭으로 잡힌다
  (−23% 가 −70% 로 부풀었다). 납입 종료 후 또는 원금 대비로 재라
- **중앙값과 최악값을 섞어 인용하기**
- **시차상관을 수준(level)끼리 재기** — 자기상관 탓에 항상 0일 최대가 나온다
- **분위 문턱을 전표본으로 잡기** — `expanding().quantile().shift(1)` 을 써라
- **한국 ETF 를 일간 상관으로 검증하기** — 세션 시차. 주간/월간 또는 shift+1
- **`pct_change` 를 교집합 전에 계산하기** — 드리프트가 부풀려진다(+3.06%→+0.29%)
- **달러 결과로 결론 내기** — 실제로 사는 건 원화 상품이다
- **금의 1970년대 성과를 일반화하기** — 1971 금 자유화, 한 번뿐인 제도 변경
- **상품 이름을 안 읽고 모형 만들기** — 「미국채10년**선물**」이다.
  v25 가 괴리를 측정하고도 원인을 롤 비용으로 잘못 짚었다
- **공표 수치를 재현할 때 데이터 구성 규약 건너뛰기** — `verify.py` 는 QLD 를
  2006 이전 2xQQQ 로 연장하고 SCHD 를 2011 이전 현금 2% 로 대용한다
- **방어자산에 '1일 지연 민감도'로 미래참조를 재기** — 진입일이 폭락 당일이라
  −81% 가 나온다. 실물과의 시차상관으로 재라(SCHD r=0.9999)
- **`data/qqq.csv` `data/signal.json` 을 직접 고치기** — 워크플로 소유다
- **수치를 바꾸고 문서를 안 고치기** — v33·v36 에서 ISA 수치가 두 번 바뀌었는데
  라이브만 고치고 `docs/history/전략_v29.md` 에 v36 이전 값이 남아 있었다.
  **수치를 폐기하면 `data/retired_numbers.json` 에 등록하라.**
  `verify_all.py` I9 가 현행 문서(README·HANDOFF·FILES·요약·signal.html)에
  남아 있으면 **실패**시키고, 버전 문서에 정정 배너가 없으면 경고한다

---

## 1-1. ★ 전략의 전제 — 나스닥 고유 성질에 의존한다 (v44)

이 전략은 **「강한 장기 상승 + 극단적 레버리지 붕괴」** 라는 나스닥의 성질에서
값어치가 나온다. S&P500(2배 MDD −86%)·코스피(29년 4.4배)에서는 **그냥 보유에 진다.**

`verify_all.py` **I10** 이 세 가지를 감시한다:
P1 2배 보유 MDD ≤ −90% · P2 20년 연평균 상승 > 3% · P3 전략 > 그냥 보유

**하나라도 깨지면 전략을 다시 검토해야 한다.**

---

## 2-0. ★ 새 아이디어 판단 기준 (v41 확정)

**독립 위기가 19회뿐이다.** 그래서 지표별 판별 문턱(2σ)이 정해진다:

| 지표 | 2σ 문턱 | 비고 |
|---|---:|---|
| 최종배수 | 37.2%p | 복리로 증폭돼 **둔하다** |
| **Calmar** | **10.2%p** | 가장 예민 — 1차 관문 |
| 20년창 중앙 | 43.2%p | |
| **20년창 5분위** | 24.7%p | **진짜 관문** |

순서: ① Calmar +10.2% 초과 → ② **20년창 5분위가 현행 이상** →
③ 적립식·원화 유지 → ④ 워크포워드 통과.

**②에서 대부분 죽는다.** Calmar 는 MDD 만 줄여도 오르므로 속기 쉽다.
v41 의 8개 후보가 전부 여기서 탈락했다.

---

## 2-1. 새 분석은 `research_kit.py` 를 써라

직접 짜지 마라. 실제로 났던 설계 오류를 API 에서 막는다.

```python
from research_kit import sweep, walkforward, mdd, mdd_vs_paid, dist, verdict
```

| 함수 | 막는 것 |
|---|---|
| `sweep()` | 격자 경계 최적점을 **예외로 거부**. 고원 판정 포함 |
| `walkforward()` | `fixed=` 고정 대조군 없이 **거부**. 지표 명시 필수 |
| `mdd(kind='accum')` | `since=` 없이 **거부** (초기 잔고 허수) |
| `dist()` | 중앙·5분위·최악을 **항상 함께**. 표본 3개 미만 거부 |
| `verdict()` | 판정문을 계산값에서 **생성** |

`python research_kit.py` 자기검사. CI 가 매 push 마다 부른다.

**새 연구 스크립트는 `research/` 에 만들고 상단에 경로 보정 3줄을 넣어라**
(`research/` 의 아무 파일에서 복사). 그러면 어디서 실행하든 루트 엔진·데이터를 찾는다.

**한계**: 이 함수를 **안 쓰면 무력하다.** 급해도 직접 짜지 마라.

---

## 3. 환경 주의

- 이 환경의 브라우저 창은 프레임을 합성하지 않아 **스크롤 이벤트와
  IntersectionObserver 콜백이 안 뜬다.** 스크린샷도 안 된다.
  `getBoundingClientRect()` 로 기하 검증하라. 코드 버그로 오해하지 마라.
- Bash 히어독에 한글+`'` 가 섞이면 깨진다. `Write` 툴을 써라.
- 콘솔이 cp949 라 `PYTHONIOENCODING=utf-8` 을 붙여라.

---

## 4. 미결 (사용자 결정 사항)

| | 내용 | 영향 |
|---|---|---|
| ① 원시종가 vs 수정주가 | 라이브는 `quote[0].close`(원시), 연구는 수정주가 | 상태 불일치 0.16%, MDD 동일, 현재 낙폭 차 0.000%p |
| ② 국채 보수 0.29% | 회귀 절편에서 나온 값 | 실제 상품 보수와 대조하면 더 정확해진다 |
| ③ 국채 표본 7.4년 | 선물형 정정의 근거 구간이 짧다 | 실물이 더 쌓이면 재측정 |

---

## 5. 2026-08-27 에 12건이 난 이유 (요약)

| 원인 | 건수 | 대책 |
|---|---:|---|
| 검산 함수가 없던 곳 | 2 | `verify_all.py` I1 + `check_accum()` ✅ |
| 엔진을 안 쓰고 새로 짬 | 2 | I1 이 동치 검사 ✅ |
| 모형이 실제 상품과 다름 | 1 | I4 실물 드리프트 ±1.5%p ✅ |
| 검증 설계 오류 | 5 | **`research_kit.py`** — API 에서 4/5 차단 ✅ |
| 의존성 추적 실패 | 1 | I8 사용처 목록 ✅ |
| 플랫폼 | 1 | 예약 3슬롯 + 신선도 경고 ✅ |

> **검산 함수가 있던 코드는 12건 중 0건이 틀렸다.**
> `run`/`sim`/`sim_hold`/`after_tax` 는 `check()` 가 지켰다.
> `accumulate`/`mix_monthly` 는 검산이 없었고 둘 다 틀렸다.
>
> **파일이 많아서가 아니다.** 불변식을 코드로 못박지 않은 게 원인이다.
> 상세는 `README.md` §5.

### 도구로 못 막는 나머지 — 사용자의 되묻기가 잡았다

12건을 실제로 찾아낸 건 도구가 아니라 사용자가 계속 되물은 것이다.
"폴더 전체를 훑은 거야?" → 라이브 4개 미확인 발각.
"다 검증한 거 맞지?" → 하위 6개 미재실행 발각(v27 근거 뒤집힘).
"완벽한 거지?" → 적립 2일 지연 발각.

**되묻기 4종** (설계 오류 5종에 대응):
1. "그 숫자 중앙값이야 최악값이야?"
2. "어느 구간에서 나온 이득이야?"
3. "안 하면 어떻게 되는데?"
4. "그 문장 계산에서 나온 거야, 네가 쓴 거야?"

**"내가 알아서 다 보겠다"고 답하지 마라.** 오늘 12번 틀렸다.
