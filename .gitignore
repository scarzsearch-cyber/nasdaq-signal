__pycache__/
*.pyc
.venv/
venv/
.DS_Store
Thumbs.db
.claude/

# French 원자료 추출본 — data/hist/ff_*.zip 에서 hist_defensive._ensure_ff() 가 자동 생성
data/hist/ff_tmp/

# [v176] 시세 스냅샷은 main 이력에 안 쌓는다 — price-data 브랜치가 원본이다.
#   로컬에 있어도 되지만(있으면 로컬 서버에서 배지가 보인다) 커밋하지 않는다.
data/price.json

# 공유용_별도전략: _ 로 시작하는 것은 전부 임시 (중간 산출물 + 1회용 스크립트).
# 확장자 무관 — 소유자 지시 2026-09-02: 1회용 코드도 저 폴더 안에 만들되 커밋하지 않는다.
공유용_별도전략/_*

# [v206] 외부 세션(Codex) 첨부 이미지 — 저장소 자산이 아니다
.codex-remote-attachments/
