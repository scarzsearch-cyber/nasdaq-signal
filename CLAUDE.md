# CLAUDE.md — 이 저장소에서 작업하는 모든 AI 세션의 규칙 (v186)

> 어떤 세션이든 이 파일이 먼저 로드된다. 여기와 파일이 어긋나면 **파일이 옳다** —
> 추측하지 말고 열어서 확인하라. 상세 인수인계는 `HANDOFF.md`.

---

# ⛔ §-1. 무엇보다 먼저 — 오류가 이 전략을 죽이는 방식

> **이 저장소에서 오류의 비용은 극단적으로 비대칭이다.**
> 좋은 아이디어를 기각하면 **기회비용**으로 끝난다.
> 잘못된 근거로 동결 규칙을 흔들면 **실제 돈이 사라진다.**
> **따라서 「바꾸자」의 증거 기준은 「그대로 두자」보다 훨씬 높다.
> 의심스러우면 아무것도 바꾸지 마라.**

## ⚡ 실행 체크리스트 — 아래 서술을 안 읽어도 이것만은 기계적으로 (2026-08-31 소유자 지시)

**소유자 지적: 「너무 자주 일어나서 더 이상 두고 볼 수 없다.」** 규칙이 길어서 실행이 안
되는 게 문제였다. **방아쇠는 전부 눈에 보이는 상황이다 — 「내가 편향됐나」를 묻지 마라.**

| 내가 지금 하려는 것 | 하기 **전에** 반드시 |
|---|---|
| 무언가를 **지운다** | repo 전체 grep — 사용처 확인 (①) |
| **「완료」·「기각」·「권고」**를 쓴다 | 내가 세운 항목을 **파일에서 하나씩 눈으로** (②) · 반증 검사 (ⓒ) |
| 무언가를 **정정했다** | 철회한 주장을 **repo 전체에서 사냥** + 같은 파일 나머지 절 재독 (③) |
| **숫자를 인용**한다 | 그 값이 **무엇에 대해 잰 것인지** 원문 확인 (④) |
| **관문**을 세운다 | 「실패하면 뭐가 참? 통과하면 뭐가 참?」 — 같으면 관문이 아니다 (⑤) |
| **표를 만들었다** | 그 표가 낳은 **다음 질문**을 적어라 → 04 §7 대장 (⑥) |
| **판정문**을 쓴다 | **이번 세션에서 내가 이미 말한 중간 결론**을 다시 읽어라 (⑦) |
| **「A 때문에 B」**를 쓴다 | 표에 **A 만 바꾼 열**이 있는가 — 2개 이상 다르면 주장을 내려라 (⑧) |
| 후보가 **관문을 통과**했다 | 무조건 반증 (ⓐ) |
| 후보가 **B 밖의 계열·구조**를 쓴다 | 그 입력을 **연 블록 뒤섞기 200회**로 플라시보 — 상위 5% 밖만 인정 (ⓔ) |
| **조합·구간을 손으로 골랐다** | 무작위로 다시 뽑아 **분포**로 판정 (ⓑ) — **기각할 때도** (ⓓ) |
| 결론이 **기존 규칙과 잘 맞는다** | **더 의심하라.** 저장소 규칙이 확증편향의 도구가 된 실사례 (⑦) |

> **①~⑤ 는 2026-08-31 오전, ⑥~⑧ 은 같은 날 오후에 실제로 난 오류에서 나왔다.
> 전부 소유자가 잡았다.** 아래는 각 항목의 실사례와 근거다 — 처음이면 다 읽어라.

## 내가 실제로 넘어진 방식 (2026-08-31 하루에 5번, 전부 소유자가 잡음)

**공통 뿌리 하나뿐**: **결과가 내 예상대로 나오면 나는 검증을 멈춘다.**
반대 결과가 나올 때는 스스로 더 판다. **위험은 언제나 「맞았다고 느낄 때」에 있다.**

## 🔒 절대 멈춤 5개 — 판단 말고 기계적으로 실행

1. **무언가를 지우기 전 → 사용처를 repo 전체 grep.**
   (`.clock` 옆에 있다고 `.rules` 를 같이 지워 하루 동안 깨진 채 배포)
2. **「완료」·「기각」·「권고」를 쓰기 전 → 내가 세운 항목을 하나씩 파일에서 눈으로 확인.**
   (문서에 권고를 적어놓고 「구현했다」고 보고 — 8개 중 2개 미구현)
3. **무언가를 정정한 뒤 → 철회한 주장을 repo 전체에서 사냥.**
   문서만 고치고 코드가 옛 판정을 계속 출력한 사례 2회. **같은 파일의 나머지 절도 다시 읽어라.**
4. **숫자를 인용하기 전 → 그 값이 「무엇에 대해 잰 것인지」 원문에서 확인.**
   (PBO 0.437·0.53 은 **T4 파라미터·혼합 x** 에 잰 값인데 **−16 문턱**의 근거로 썼다.
   없는 근거로 소유자의 전략을 의심했다.)
5. **관문을 세울 때 → 「실패하면 무엇이 참인가? 통과하면 무엇이 참인가?」를 먼저 적어라.
   양쪽 답이 같으면 그것은 관문이 아니다.**
   (B 의 타 시장 이식: 「특화」와 「과적합」이 같은 결과를 내는데 ★실패로 찍었다)

## 🔬 후보를 재는 순간의 규칙

- **사전 등록**: 통과 기준을 **결과 보기 전에 커밋**한다. 결과를 본 뒤 기준을 정하면 반드시 통과한다.
- **사전 등록도 만능이 아니다** — 오늘 3번 실패했다: 잘못된 **통계량**(꼬리 위험에 중앙값) ·
  잘못된 **창**(둥근 시작일 하나) · **변별력 없는 검사**. 골대 이동만 막을 뿐 **엉뚱한 골대를
  세우는 것은 못 막는다.**
- **반증을 먼저 설계하라.** 아래 셋 중 하나라도 해당하면 **무조건** 실행:
  ⓐ 후보가 관문을 **통과**했을 때 · ⓑ 조합·집합·구간을 **내가 손으로 골랐을 때**
  (→ 무작위로 다시 뽑아 **분포**로 판정) · ⓒ **「권고/채택/기각」을 쓰기 직전**
- **ⓑ 는 기각할 때도 적용한다.** 기각도 편향된다 — T4 를 둥근 날짜 하나로 잘못 기각했다.
- **앙상블·혼합류는 「내가 고른 조합」이 아니라 「무작위 조합 분포」로 판정.**
- **ⓔ 입력 뒤섞기 플라시보** (2026-09-03, 04 §5-29 b): 후보가 **B 밖의 계열**(금리·환율·밸류에이션·변동성·타 자산)이나 **구조**(비율·오버레이·
  슬리브)를 쓰면 그 계열을 **연 블록으로 뒤섞어 200회** 재고 **상위 5% 밖**일 때만 인정한다. 실사례: C3(÷T-bill 낙폭)는 사전 등록·관문 ①② 통과 뒤
  이 검사에서 닫혔다(뒤섞은 가짜의 27% 가 ①② 통과). ⓑ 는 파라미터·조합만 흔들고 **시점**은 안 흔든다 — ⓔ 는 그 빈칸이다.
  같은 날 잰 관문의 잡음 폭(04 §5-29 a): **①② 만은 파라미터 잡음의 2~4.5% 가 넘는다 · ①②③ 은 800개 중 0** — ③ 은 필수다.
- **구간은 특정 날짜 하나가 아니라 「모든 시작일 분포」로 판정** (`research/slice_scan.py`).
  단 승률 옆에 **비중첩 창 수**를 always 함께 — 20년 창은 비중첩 2.7개뿐이다.

## 🔗 절대 멈춤 6·7 — 연구는 끊기지 말아야 한다 (2026-08-31 소유자 지적)

소유자 지적: **「왜 내가 물어봐야 비로소 인식해? 파생되는 연구흐름까지 연속적으로
사고해야 하지 않나」.** 그날 두 가지가 났고 **성격이 다르다.**

6. **표를 만들었으면 그 표가 낳은 질문을 적어라.**
   `def_bond.py` 가 「배당은 방어 중 QQQ 상관 **+0.775** · 최악5%일 **−1.77%**」를
   출력했는데 나는 「그럼 배당은 왜 방어에 있나」를 **묻지 않았다.** 그 표가 그 질문을
   만들었는데 물은 것은 소유자였다.
   → **연구 파일은 판정만 내지 말고 「이 측정이 낳은 다음 질문」을 반드시 출력한다.**
   답이 저장소에 있으면 절 번호를 달고, 없으면 **04 §7 파생 질문 대장**에 미결로 올린다.
   판정이 「기각」이어도 마찬가지다 — 기각은 측정을 무효로 만들지 않는다.

7. **판정문을 쓰기 전, 이번 세션에서 내가 이미 말한 중간 결론을 다시 읽어라.**
   같은 날 나는 세션 중간에 **「달러도 같은 창이면 원화와 같다 — 통화 효과가 아니라
   표본 창 효과」**라고 직접 쓰고, 최종 판정문에 **「통화에 따라 뒤집힌다」**고 적어
   04·HANDOFF·코드 3곳에 박았다. **몰라서가 아니라 알았다가 놓친 것이다.**
   → 원인: 「통화 때문」이라는 이야기가 저장소의 기존 규칙(v36 「달러로 결론 내지
   마라」)과 **맞아떨어져서 확인을 멈췄다. 저장소의 규칙이 확증편향의 도구가 됐다.**
   **기존 규칙과 일치하는 결론일수록 더 의심하라** — §-1 의 「맞았다고 느낄 때가
   가장 위험하다」가 여기에도 그대로 적용된다.

8. **「A 때문에 B」를 쓰려면 A 만 바꾼 열이 표 안에 있어야 한다.**
   04 §5-17 초판은 **「원화 1981~」과 「달러 1972~」**를 비교해 놓고 **「통화 때문에
   뒤집힌다」**고 썼다. **통화와 표본 창을 동시에 바꿔 놓고 하나를 원인으로 지목**한
   것이다. 「달러 1981~」 열을 넣자마자 통화 효과는 사라졌다(−10.2% vs −10.8%).
   → **기계적 검사**: 비교하는 두 열이 **2개 이상** 다르면 그 표로 인과를 말할 수 없다.
   **열을 추가하거나 주장을 내려라.** 「~때문에」·「~라서」·「~의 산물」을 쓰기 직전에
   이 검사를 한다. 판단이 필요 없다 — 열이 몇 개 다른지는 눈으로 센다.

> **6·7·8 의 관계**: 6 은 「안 물어본 것」, 7 은 「물어서 답까지 얻고 잃어버린 것」,
> 8 은 「두 개를 같이 바꿔 놓고 하나를 범인으로 지목한 것」이다.
> **7 이 가장 나쁘다** — 6 은 시간이 해결하지만 7·8 은 틀린 결론을 문서에 박는다.
> 세 개 다 **2026-08-31 하루에 실제로 일어났고, 전부 소유자가 잡았다.**

## 🧭 그럼에도 남는 사실

**위 규칙을 다 지켜도 나는 또 틀린다.** 오늘 5번 중 5번을 잡은 것은 규칙이 아니라
소유자의 **「정말 다 됐어?」·「확증편향 아니야?」·「그 근거가 맞아?」** 였다.
**그 질문이 최종 안전망이라는 사실을 부인하지 마라.** 소유자가 되물으면
방어하지 말고 **먼저 내 근거의 출처부터 다시 확인하라.**

> **소유자용 진입점**: **웹 화면이 1순위다** (오늘의 신호 · 설명서 — §3 웹사이트 자족 원칙).
> 문서가 필요하면 `내가_보는_것/운영_점검표.md` (한국어, 「내가 언제 뭘 해야 하나」).
> **소유자에게 `research/*.py` 경로를 직접 시키지 마라** — 파일이 많아 파악이 안 된다는
> 실제 피드백(2026-08-31). 정기 점검은 **v140 부터 자동 파수꾼이 돌린다**
> (`내가_보는_것/점검.py` 는 직접 보고 싶을 때만). 새 정기 작업이 생기면
> **먼저 자동화하고 결과를 화면에 띄워라** — 새 수동 의무를 만들지 마라.

## 0. 먼저 읽을 파일 (이 순서로)

1. `HANDOFF.md` — §2 "하지 마라"가 이 저장소의 법이다.
2. `01_Strategy_Logic.md` ~ `04_Rejected_Research.md` — 현행 전략·기각 기록.
3. `signal.html` / `guide.html` / `notes.html` — 웹 화면 3탭 (각각 단일 파일, 바닐라 JS/CSS).
4. `verify_all.py` — 작업 완료 게이트. **화면 문자열까지 검사**하므로 문구 수정 전 확인.
   ⚠ **「배포 게이트」가 아니다** (2026-09-01 정정): `verify.yml` 과 `pages.yml` 은 둘 다
   push 트리거로 **독립 실행**된다 — 검증이 실패해도 배포는 나가고, 실패는 **이슈(메일)**로만
   알린다. 그게 맞다: 문서 검사가 신호 화면을 얼릴 수 있으면 v137 fail-open 원칙과 어긋난다.
   **소유자 화면에 「배포가 멈춘다」고 쓰지 마라** — 실제로 그렇게 썼다가 소유자가 잡았다.
5. `FILES.md` — 전체 파일 지도.

### ★ 저장소 이력을 볼 때는 시세 커밋을 걸러라 (v175)

`chore(price): 시세 스냅샷` 이 **한국장 중 30분마다** 쌓인다(하루 14건 · 연 3,500건).
`git log` 를 그냥 찍으면 최근 20건 중 14건이 그것이라 **파악 수단이 사실상 안 먹는다.**

```
git log --oneline -15 --perl-regexp --invert-grep --grep='^chore\(price\)'
```

- **기계가 읽는 쪽은 영향 없다** — 화면의 「전략 반영 vNN」 도장은
  `git log -1 -- signal.html` 로 **그 파일을 건드린 커밋만** 보므로 시세 커밋을 안 탄다
  (`deploy/stamp_rev.py`, 2026-09-01 실측 확인).
- **주기를 늦추는 것은 답이 아니다** (2026-09-01 소유자와 검토 후 **30분 유지 확정**):
  비용은 250일 내내 쌓이는데 값어치는 전환·재조정 **10일쯤에 몰려 있어**, 균일하게
  늦추면 소음은 절반만 줄고 **정작 필요한 날의 값도 같이 나빠진다.**
- **⛔ 이력을 지워서(rebase·filter-repo·force-push) 정리하지 마라.** 기술적으로는 되지만
  이 저장소에서는 **하면 안 된다**: 이 커밋 이력이 「동결 이후 규칙을 안 건드렸다」의
  **증거**다(notes.html 히어로가 그 주장을 한다). 한 번 다시 쓰면 감사하는 쪽에서
  **「시세만 지운 것」과 「규칙 변경을 지운 것」을 구별할 수 없다.** 소음을 없애자고
  증거를 없애는 거래다. 게다가 이후 커밋 해시가 전부 바뀌어 기존 이슈·실행 링크가 끊긴다.
- 소음을 **정말** 없애야 할 상황이 오면(예: 주기를 10분 이하로), 지우지 말고
  **애초에 안 쌓는 쪽**으로 간다 — 시세를 시세 전용 브랜치에 **항상 커밋 1개로 덮어쓰고**
  (force-push) 배포 때 그 브랜치에서 파일만 가져온다. main 이력은 그대로 깨끗하고
  저장소도 안 커진다. 대가는 **움직이는 부품이 하나 늘어난다는 것**(브랜치를 못 가져올 때
  main 사본으로 물러서는 처리가 필요하고, 그게 조용히 실패하면 옛 시세를 보게 된다).

## 1. 시스템 사실 요약

- **전략 B (동결 2026-08-27, 변경 금지)**: QQQ 252거래일 고점 대비 낙폭
  −16% 이하 마감 → 전량 방어 / −16% 위 회복 마감 → 전량 공격. 연 2~3회 전환.
- 공격 = TIGER 미국나스닥100레버리지(합성) 418660 **100%** /
  방어 = 배당 458730 **40%** + 국채 305080 **40%** + 금 411060 **20%**
  (⚠ 방어에 레버리지는 0% — 전량 전환이다. 외부 AI 가 자주 틀리는 지점).
- T4 그림자 = 평가 전용 기록 (승격 아님, 판정은 v80 부속서 규약).
- 신호는 새벽 자동 갱신(GitHub Actions, 실패 시 카톡), 체결은 한국장
  09:05~15:20 수동. **자동매매 금지. 투자 판단 문구 자동 생성 금지.**
- 주 사용 환경: **갤럭시 412px 세로 모바일**. 가로 스크롤 금지, 세로 최소화
  강한 선호(폰트 축소보다 요소 제거). 사용자 데이터는 localStorage 만.

## 2. ★ 절대 수정 금지 (읽기만 허용)

- `data/freeze.json` · `data/oos_log.csv` · `data/nav_history.csv` — 동결 규칙·실측 장부.
- 전략 파라미터·판정 로직: signal.html 의 `STRAT`/`compute` 등, `deploy/*`,
  `hist_*.py` · `axis_*.py`, `verify_all.py` 의 전략 검사(I1~I12), `docs/history/*`.
- 전략 규칙·비중·지표·백테스트·수익률 개선 제안 전부 금지 — 500개 후보의 무덤이
  `04_Rejected_Research.md` 에 있다. 이름만 바꾼 재탐색도 금지.

### ⛔ `공유용_별도전략/` 은 이 전략이 아니다 (2026-09-02 격리)

소유자가 **지인에게 줄 용도로 만든 별개 데모**(SCHD+QQQ 비레버리지 배합, 신호·방어 없음)다.
**발단**: 소유자가 본 전략을 작업하던 다른 세션에서 그 `.py` 들이 `research/` 안에 섞여 있는
것을 발견하고 「이건 뭔지」 분석하기 시작했다 — **연구 자산으로 오인되어 본 전략을 건드릴
위험**이 실제로 생겼다. 그래서 폴더로 격리했다.

- **그 폴더의 산출물·수치·결론을 이 전략의 근거로 쓰지 마라.** 자산·신호·방어가 전부 다르다.
- 그 폴더는 저장소 엔진(`hist_*.py`·`research/eng_common.py`·`data/hist/*`)을 **읽기만** 한다.
  거기 작업 때문에 엔진을 고치는 일은 없어야 한다.
- `research/` 밖에 둔 것은 **의도**다 — verify_all 의 FILES.md 관문(v172) 감시 범위 밖이며,
  전략 문서 체계의 일부가 아니다. 안으로 도로 옮기지 마라.
- 그 폴더에서 작업하라는 지시를 받았을 때만 그 안의 `README.md` 를 먼저 읽는다.

**★ 소유자 규정 (2026-09-02)**: **「저 스크립트는 우리 전략을 빌려 써도 된다.
우리 게 이상한 걸 흡수해서 손상되지 않기만 하면 된다.」** 즉 **방향이 하나**다 —
**읽기(빌려쓰기)는 자유 · 쓰기(오염)는 차단.**
- 따라서 **엔진을 고쳐서 그 폴더가 깨져도 고쳐줄 의무는 없다.** 산출물은 이미 나갔고
  그 폴더는 저장소 운영과 무관하다. **본 전략 쪽을 그 폴더에 맞춰 바꾸지 마라.**
- 이 규정은 **관문으로 강제된다**(`verify_all.py`, 자각이 아니라 관문으로 — v148):
  그 폴더의 `.py` 가 **폴더 밖 경로에 쓰거나** `deploy/*` 를 임포트하면 **실패**한다.
  경로가 실행 중 정해지는 쓰기는 **경고**로만 둔다(그 폴더는 다른 세션이 파일을 계속
  늘리는 자리라 딱딱하게 실패시키면 빨간불이 상시가 되고 관문이 무시당한다 — v172 교훈).
  ⚠ 그 폴더는 FILES.md 관문 밖이라 **이 검사가 유일한 감시**다. 약화시키지 마라.
  검증: 정상 PASS(쓰기 8곳 전부 폴더 안) · `data/signal.json` 리터럴 주입 시 **FAIL 확인** ·
  동적 경로는 WARN — **세 갈래를 실제로 재현**했다(통과만 보면 검사가 아니다, §-1 ⑤).

## 3. 작업 규약

- **자율 진행이 기본** (소유자 지시 2026-08-31 — 옛 「승인 후 구현」 규약 폐기).
  웹/UX 작업은 묻지 말고 **구현 → 검증 → 커밋 → push → 배포 확인**까지 한 흐름으로
  끝내고 결과만 보고한다. "진행할까요?" 류의 승인 요청을 넣지 않는다.
  **여전히 먼저 물어야 하는 것**: §2 절대 수정 금지 대상을 건드리는 변경 · 전략 덮어쓰기 ·
  파일/데이터 삭제 등 되돌리기 어려운 작업.
- **★ 웹사이트 자족 원칙** (소유자 지시 2026-08-31, v140): **소유자가 알아야 할 정보와
  눌러야 할 기능은 전부 `signal.html`·`guide.html`·`notes.html`(v142) 안에 있어야 한다.**
  분업은 — 신호는 얇게 · 상세는 설명서 · **바뀐 내역은 업데이트 노트**. 파일을 열어야만
  알 수 있는 운영 정보를 만들지 마라. 새 정기 의무가 생기면 순서가 정해져 있다 —
  **① 자동화한다(GitHub Actions) → ② 결과를 화면 한 줄로 띄운다 → ③ 이상일 때만
  카톡·배너로 말한다.** 그래도 사람이 손으로 해야만 하는 것이 남으면 그때만
  `내가_보는_것/` 에 **한국어 제목**으로 넣는다. 「분기마다 이 스크립트를 돌리세요」
  같은 지시를 새로 만드는 것은 이 원칙 위반이다 (v140 이 그 마지막 하나를 없앴다).
  소유자의 개입은 **실제 매매(전환 주문·방어 재조정) 두 가지**로 수렴시킨다.
- **화면 문구를 깎을 때의 중용선** (v134): **같은 말을 두 번 하는 곳만 지운다.
  정보 자체는 지우지 않는다.** 상세는 guide.html 이 맡고 signal.html 은 얇게 —
  이 분업이 v133~v136 의 기준이다.
- **코드 리뷰 등급** (소유자 합의 2026-08-31): 소유자가 평소 `/code-review max` 를 돌린다.
  **`ultra` 가 필요하다고 판단되면 AI 가 먼저 말한다** (소유자만 실행 가능·과금).
  ultra 를 제안할 조건 — ⓐ 코드 3개+ 파일에 걸친 구조 변경, ⓑ 엔진 계열
  (`axis_*.py`·`deploy/*`·`reentry_lib.py`·`hist_*.py`) 수정, ⓒ 비동기/캐시/동시성
  (v111 프라미스 캐시 레이스가 실사례), ⓓ **새 시뮬레이터인데 축퇴 검산을 짤 수 없을 때.**
  ⓓ 단서 주의: 환원할 기존 엔진이 있으면 **검산이 ultra 보다 싸고 확실하다** —
  `check_hold()` (axis_defmix.py) 가 그 본보기이고, 그 경우엔 max 로 충분하다.
  단일 파일(signal.html) 문구·CSS 작업엔 ultra 를 제안하지 않는다.
- 구현 후 필수 검증: `python verify_all.py --fast` **실패 0** · 412px 가로 스크롤 0 ·
  ⚠ **[v172] 화면 요소·함수를 지웠으면 `--fast` 가 아니라 `python verify_all.py` 전체로 돌려라**
  — 화면 문자열 검사는 fast 에 없다. v168 이 그래서 7건을 깨뜨린 채 지나갔다.
  콘솔 오류 0 · 기존 localStorage 키(`b_port_v1`, `b_trades_v1`,
  `b_goal_v1`, `b_lastseen_v1`, `b_lastbackup_v1`, `b_undo_v1`(v123), `b_firstseen_v1`(v157), `b_fold_v1`(v169), `b_chk_*`,
  `qqq_signal_*`) 구조 유지 (`b_view_v1` 은 v113 에서 폐기 — 로드 시 삭제.
  IndexedDB `b_autobak_v1` 은 v124 자동 백업 파일 핸들).
- 커밋: 작업 전 `git pull`. **signal.html 을 바꾸는 커밋 제목에 vNN 필수**
  (화면의 "전략 반영 vNN" 표기가 커밋 제목에서 추출됨 — deploy/stamp_rev.py).
  커밋 메시지에 큰따옴표 금지 (PowerShell 5.1 이 인자를 쪼갠다).
  push 까지가 한 작업이다 — push 안 하면 배포가 안 돌아 화면이 옛 버전에 머문다
  (pages.yml 은 `push: branches:[main]` 트리거. v133 이 로컬에 묶여 있던 실사례).
- **★ 자주 내는 실수 4종과 그 방아쇠** (2026-08-31 실사례에서 역산 — 전부 「읽을 수
  있었는데 안 읽어서」 또는 「확인해주는 검사만 골라서」 생겼다. 추론 깊이·리뷰 등급의
  문제가 아니므로 ultra 로도 안 잡힌다):
  1. **무언가를 지울 때 → 사용처를 먼저 grep 하라.** v133 이 `.clock` 과 붙어 있다는
     이유로 `.rules` 를 같이 지웠고, taxPanel 이 그걸 쓰고 있어 하루 동안 깨진 채
     배포됐다. verify_all 은 문자열만 보므로 이런 회귀를 못 잡는다.
  2. **「완료」라고 말하기 전 → 내가 세운 항목을 하나씩 파일에서 확인하라.**
     「문서에 권고를 적었다」를 「구현했다」로 착각해 8개 완료 보고 중 2개가 미구현이었다.
  3. **파일 일부를 고친 뒤 → 그 파일의 나머지를 다시 읽어라.** 트랜치 기각 후 3개
     문서를 고쳤으나 같은 파일의 「남은 것」 절이 기각 이전 결론을 가리키고 있었다.
  4. **반증 검사 — 「편향을 알아채라」가 아니라 아래 3개 중 하나라도 해당하면 무조건 실행.**
     (「내가 편향됐나」는 그 순간 느껴지지 않는다. 그래서 내면이 아니라 **관찰 가능한
     상황**을 방아쇠로 쓴다. 셋 다 눈에 보이는 사실이라 판단이 필요 없다):
     - **ⓐ 후보가 관문을 통과했을 때** — 떨어진 후보는 스스로 끝나므로 위험은 통과 쪽에만 있다.
     - **ⓑ 조합·집합·구간을 내가 손으로 골랐을 때** → **무작위로 다시 뽑아 분포로 판정.**
     - **ⓒ 「권고/채택/기각 아님」을 쓰기 직전** — 문장을 쓰기 전에 검사부터.
     - **ⓔ 후보가 B 밖의 계열·구조를 쓸 때** → 입력 뒤섞기 플라시보(§-1 🔬 · 04 §5-29 b).
     - **ⓓ 후보를 *기각*할 때도 ⓑ를 적용하라** (2026-08-31 실사례, 04 §5-11 정정):
       T4@3 을 「2010-01-04 시작 창에서 진다」로 기각했는데, 그 날짜가 **지는 13% 에 우연히
       속한 둥근 날짜**였다. 모든 시작일로 재니 85~88% 에서 이겼다. **구간은 특정 날짜 하나가
       아니라 「모든 시작일 분포」로 판정한다.** 기각도 편향될 수 있다 — ⓐ의 대칭.
     > **사전 등록의 한계 3종(전부 2026-08-31 실사례, 04 §5-8·§5-11·§5-13)**:
     > 잘못된 **통계량**(꼬리 위험에 중앙값) · 잘못된 **창**(둥근 시작일 하나) ·
     > **변별력 없는 검사를 관문으로**(B 의 타 시장 이식 — 특화와 과적합이 같은 결과를
     > 내므로 구분 불가인데도 「★실패」로 찍었다. 소유자 지적으로 철회).
     > 등록은 골대 이동을 막을 뿐 **엉뚱한 골대를 세우는 것은 못 막는다.**
     > → **관문을 세울 때 「실패하면 무엇이 참인가? 통과하면 무엇이 참인가?」를 먼저 적어라.
     > 양쪽 답이 같으면 그것은 관문이 아니다.**
     실사례: 트랜치 MDD 개선을 「내가 고른 룩백 5개」(ⓑ)에서만 확인하고 구조적이라 단정,
     「평가 전용 등록 권고」(ⓒ)까지 썼다. 무작위 200회로 다시 재니 바닥·부가 악화돼
     기각(04 §5-7). **앙상블·혼합류는 「내가 고른 조합」이 아니라 「무작위 조합 분포」로 판정한다.**
  > **가장 강한 방어는 내 자각이 아니라 사전 고정 관문이다.** 오늘 트랜치를 잡은 것은
  > 내 판단이 아니라 소유자가 미리 박아둔 관문 ①(Calmar +10.2%)·②(20년창 바닥)였다 —
  > 기준이 먼저 있었기에 +5.0% 를 보고 「미달」이라 말할 수밖에 없었다. **새 후보를 재기
  > 전에 통과 기준을 먼저 적어라(사전 등록).** 결과를 본 뒤 기준을 정하면 반드시 통과한다.
  > 소유자의 「정말 다 됐나 / 확증편향 아닌가」 질문이 오늘 셋을 잡아냈다. 위 방아쇠로
  > 스스로 멈추되, **그 질문이 여전히 최종 안전망이라는 사실을 부인하지 말 것.**

## 4. 기구현 요약 (중복 제작 금지 — 상세는 signal.html 직접 확인)

- v90: 신호 카드 첫 화면, 평시 "오늘 할 일 없음" 한 줄, 전환일 실행일 자동
  계산(휴장 반영)+체크리스트, 체결 기록(보유 자동 반영+기준가 차이%).
- v91: 루틴/전체 보기 토글(기본 루틴), 주문 메모 복사.
- v92: 매수/매도 배지(매수 빨강·매도 파랑 = MTS 관행), 무매매 종목 흐림,
  신선도 도트, 어림 금액 만원/억원 단위, 예정 주문 프리필 칩, 백업 복사/복원.
- v93~v95: 백업 파일 저장/복원(폰↔PC 이동 보완), 체결 괴리 요약(10건+ 시
  자동 표시, 기준가 입력분만), 다크 모드(OS prefers-color-scheme 추종 — 토글 UI 없음).
- v96~v99: 체결 기록 CSV 내보내기(UTF-8 BOM), 방어 전환 경과일(포트폴리오 패널 —
  재조정 기준일 미정이라 경과일+안내문, §5 참조), 단계 계산기(형성기/감속기/인출기
  참고용, `b_goal_v1`), 체결 기록 전체 보기/접기 토글.
- v100~v103: 신호 카드 방어 경과일+과거 방어기간 참고 한 줄, 접근성(aria-live ·
  입력 aria-label 20곳 · 신선도 도트 role), 지난 확인 이후 전환 배지(`b_lastseen_v1`,
  신호 카드 5초 열람 후 확인 처리), 성과 비교표 CSV 내보내기(숫자는 일반 마이너스).
- v104~v105: PWA 홈 화면 추가(manifest.json+아이콘 2종, pages.yml 복사 목록·
  verify_all 검사 포함 — **오프라인 캐시 sw.js 는 신선도 원칙과 충돌해 기각**),
  백업 리마인더(30일 경과 시 조용한 한 줄, `b_lastbackup_v1`).
  탭 알림·인쇄 CSS·다중 계좌·고대비 모드는 검토 후 **소유자가 불필요 판정**(2026-08-30)
  — 재제안 전에 그 판정 근거(페이지는 재조회 루프가 없고 데이터는 새벽 1회 갱신,
  전환 알림은 카톡이 이미 담당)를 확인할 것.
- v106: 체결 지연 실측 배지(전환 신호 대비 첫 체결 D+n — 상한 7일·D+0 표시는
  소유자 확정, 후속·재조정 매매엔 미표시) + oos_log.csv 공용 캐시(loadOosLog,
  페이지당 요청 1회). 폰↔PC 자동 동기화는 소유자가 **파일 백업 유지로 확정**(2026-08-30)
  — Gist/외부 DB 안은 토큰·서버 의존 때문에 보류.
- v107~v111 (평시 압축, 전부 소유자 승인 2026-08-30): 시계 한 줄+눈금자 접기
  (전환 근접 gap<8%p·전환일·방어 상태엔 자동 복원 — CLOCK_FULL/sigcompact),
  마스트헤드 버전·동결 메타 details 접기(로컬 사본 경고는 강제 펼침),
  백업 버튼 5→3(클립보드 복사/붙여넣기 제거), 전환일 문구 → #portPanel 앵커,
  동결 이후 OOS 이력 한 줄(oosHist) + **loadOosLog 프라미스 캐시 정정**(값 캐시는
  동시 호출 레이스 — fetch 중 둘째 호출이 null 을 받았다). 412px 평시 판정 시작
  579px→288px. guide.html 다크 모드도 별도 커밋으로 완료(v95 토큰 이식).
- v112 (소유자 승인): 모바일 성과표 핵심 5열 기본(전략·최종배수·CAGR·MDD·회복기간,
  표 530→352px 드래그 0) + 「전체 지표」 토글로 보조 4열(Calmar·Sortino·Ulcer·전환)
  펼침. 데스크톱(>700px)·CSV 내보내기는 항상 9열 전부 — 데이터 무손실.
- v113 (소유자 지시): **v91 루틴/전체 토글 제거** — 항상 전체 표시("난잡한 건 싫지만
  정보를 읽는 건 좋다"). 화면 압축은 v107~v108(시계 한 줄·눈금자 접기)이 담당.
  `b_view_v1` 폐기(로드 시 삭제). 루틴 모드 재도입 제안 금지.
- v114: 백업 파일에 단계 계산기 목표액(goal) 포함 — v:1 유지·선택 필드라 옛 파일↔새
  화면 양방향 호환. 백업이 이제 포트폴리오·체결·목표액을 전부 담는다
  (계산기 입력은 설계상 일회용 — 저장 안 함).
- v115~v117: 체결 기록 접힘 제목에 백업 명시(v115 — 버튼 발견 실패 실사례),
  다크 주소창 theme-color + 스크립트 오류·저장 실패 조용한 배너(v116 — #sysWarn
  평시 숨김, 오류 리스너는 메인 블록 앞 별도 스크립트, lsSet 은 사용자 입력 키만),
  **방어 재조정 D-day 카운터(v117 — 규약 확정 2026-08-30 소유자 위임: 진입일부터
  30일마다 확인, 확인일+2일 유예 표시. 주기 변경 아님 — 앵커만 확정, HANDOFF §4 ④ 해소)**.
  guide.html 엔 FAQ 4건(저장 위치·폰↔PC 이동·json 사용법·PWA)과 재조정 규약 문서화.
- v118~v119: navigator.storage.persist() 저장소 증발 방지 요청(승인 여부 무관 안전),
  신호 로드 실패 시 안내 배너(http(s) 로 열었을 때만 — 로컬 사본은 기존 경고 담당,
  입력 데이터는 무관하다는 문구 포함), 백업 「공유」 버튼(Web Share — canShare 파일
  지원 환경만 표시, 공유 완료 시에만 markBackup. 파일로 저장은 dlBlob 로 공용화).
- v120 (실사고 정정): **크롬 Web Share 는 확장자 허용목록이 있어 .json 공유를 거부**
  — v119 가 조용히 실패했다(카톡 미도착 실보고). 공유만 .txt/text-plain 으로 전송
  (내용은 같은 백업 JSON), 복원 accept 에 .txt 추가, 취소 아닌 공유 실패는 alert.
  교훈: Web Share 로 파일을 보낼 땐 허용목록(txt·csv·이미지·pdf 등)만 — json 불가.
- v121 (소유자 요청): 성과표 모바일 패딩 6→4px(v112 는 여유 0px 라 실기기 폰트 차로
  미세 드래그 — 이제 자연폭 317px/352px 컨테이너), 「같은 기간」 표의 「최근」은 첫
  열에만, **지표 심사평**(paintMetKey — Calmar·Sortino·Ulcer 에 고정 등급표(통상 눈금)
  + 이 기준 4개 변형 중 순위·최고·평균. 수치 해석 표시일 뿐 매매 판단 아님.
  Calmar·Sortino 는 수익÷위험이라 「수익 크면 낙폭 감내」가 지표에 내장, Ulcer 는
  수익 보정 없음 — 이 구분을 화면에 명시).
- v122~v125 (소유자 승인 2026-08-30): 체결가 오타 가드(기준가 ±20% 초과 시 확인),
  복원 되돌리기(덮어쓰기 직전 1세대 스냅샷 `b_undo_v1` + applyState 분리),
  자동 백업 연결(File System Access — PC 전용, 핸들은 IndexedDB `b_autobak_v1`,
  입력 변경 시 디바운스 자동 저장, 안드로이드 미지원이라 버튼 자체 숨김),
  **전환일 리허설 모드**(가상 전환을 paintState 로 시연 — 실데이터 무접촉:
  saveSeen 차단·체크리스트 `b_chk_rehearsal` 격리(종료 시 삭제)·체결 기록 차단,
  renderAuto/render 첫 줄 rehClear 로 어떤 재그리기든 리허설 해제).
- v126: **T4 동의율** — 같은 방향 빈도만 표시하는 순수 상관 통계. **승/패/스코어
  프레이밍 금지**(v80 부속서가 창 단위 총성적 판정을 폐기했으므로 — 그 판정은
  사건 단위 사전 고정 기준 별도 프로세스). t4Agreement()는 t4_w 비유한 행 제외.
- v127: **위기 타임머신** — trajPanel 에 위기 4구간 스크러버(1,000만원 → 전략/2배보유,
  ▶ 재생 100ms/일). 데이터는 data/crisis_paths.json — research/build_crisis_paths.py 가
  **build_stats.sc_us_2000 레시피 그대로**(reentry_lib.run) 전체 곡선을 재생성해
  위기 구간을 슬라이스+정규화만 함(클라이언트 근사 계산 0, 엔진 동치 검증 내장:
  final 167.315 == 공표). 로컬 수동 실행 — 자동화 미편입. 파일 없으면 위젯 숨김.
  화면에 「과거 재현·예측 아님·달러 기준·400거래일 창」 명시.
- v128 (소유자 요청 — 눌러볼 곳이 안 보인다): 인터랙티브 가시성 패스. .chipbtn 을
  파란 윤곽+옅은 파란 바탕(--acc-bg 토큰, 라이트/다크 각각)·13.5px 로, .tlio 버튼을
  ink 윤곽 13px 로, 체결 기록·백업 summary 를 버튼형 바(details.foldbox — 우측
  「펼치기 ▾/접기 ▴」 자동 표시)로, 타임머신을 박스+16px 제목+채움형 ▶ 로.
  슬라이더·체크박스 accent-color 브랜드색. 다크는 .chipbtn.solid 만 개별 오버라이드.
- v129 (소유자 지시 — 상기성 문구 불필요): 면책 푸터(.foot)·「이 화면은 참고용」·
  「예측이 아니라 …」류 안내 8곳 삭제/압축. **다시 넣지 말 것.** 유지한 것: T4
  「평가 전용·상관도」(판정 규약)·리허설 배너(격리 안내)·타임머신 「과거의 실제
  재현(예측 아님)」 한 줄(항목20 지시의 압축형)·perfNote 주의/전제(정보성).
- v130 (소유자 지시): **지표 풀이(metkey) 패널 통째 삭제**(paintMetKey·CSS·HTML —
  재도입 제안 금지, 성과 비교표로 충분 판정) + 성과표 각주에 「이름 읽는 법」 문단,
  「같은 기간」의 「여기서 보이는 것」 문단, 위기 궤적 「읽는 법」 문장 삭제.
  대신 성과표 주의/전제 아래 **전략 4개별 심사 한 줄**(선택 기준 수치 × v121 등급표 —
  gCal/gSor/gUlc 모듈 스코프 유지) + 위기 궤적 SVG 가로축에 경과 거래일 눈금
  (0~400일 — 위기마다 연도가 달라 날짜 축 불가).
- v131 (소유자 지시 — 21만배류 허황 수치 제거, 21세기 기준): **화면·가이드의 성과
  서사를 us_2000(달러 167배)·kr_1997(원화 2,800배) 공표 수치로 재정렬.** 기준 칩에서
  us_1972 제외(PERF_SHOW 3기준으로 통일, 저장된 옛 선택은 로드 시 kr_1997 보정),
  perfNote 전제의 54년 고아 문장 삭제·주의 수치 최신화(165→167 등), guide.html 의
  21만배·217,110배·54년 성과표 전부 21세기 기준으로 재작성. **54년 확장 표본은
  폭락 검증 서사(원금손실 확률표·독립 사건 22번·T4 같은 잣대)에만 명시적으로 유지**
  — 소유자 규정: 70년대는 폭락장 작동확인용, 성과·배수 주장은 21세기 기준.
- v132 (소유자 지시 — 우선순위 재배치): **신호 화면 패널 순서 = 운영(신호·포트·
  계산기·실행시각) → 보조(성과·계좌) → 참고(단계 계산기·T4·위기 궤적)** —
  goalPanel 을 taxPanel 뒤로, t4Panel 을 trajPanel 앞으로. **설명서는 「② 몇 년
  굴릴 수 있는가」(기간별 확률표, 손실 0 문턱=7년) 를 30초 요약 바로 뒤로 승격**
  (총 11절, ③~⑪ 재배열). 소유자 프레임: 수명 유한 — 3~20년 지평이 판단 기준.
- v133 (소유자 지시 — 신호 화면이 쓸데없이 길다): **execPanel(「한국장 실행 시각」패널)
  삭제**. 서머타임 05/06시 실행시각 표·「미루지 마세요」(1일 126배→3일 102배)·방어
  바스켓 고정+국채 선물형 안내는 guide.html §③(루틴)로 이식 — guide 에 없던 내용만
  옮겼고, 세금·환위험은 §④에 이미 있어 재이식 생략. **전환일 리허설 버튼은 신호 카드
  바로 아래로 이동**(execPanel 삭제로 자리를 잃어 재배치, 기능·문구 무변경).
  **goalPanel(내 단계는 어디?)을 v132 「참고」군에서 규칙 카드 위로 재배치** — 자주
  확인하는 항목이라는 소유자 판단, v132 의 그 분류를 갱신. 겸사겸사 goalPanel 의 스테일
  문서 참조 「설명서 §5」(구 번호 체계)를 실제 절 번호 「§⑦」로 정정(2곳 — signal.html
  본문·JS 문자열).
- v134 (소유자 지시 — 조각상 깎듯 다듬되 중용 유지): **v133 회귀 정정** — `.clock` 과 함께
  지운 `.rules` 를 taxPanel「놓치기 쉬운 것」이 계속 쓰고 있어 스타일이 깨졌다(구분선·색·
  17px 소실). `.clock` 만 삭제가 옳다. **CSS 를 지울 땐 클래스 사용처를 전부 grep 하라.**
  그 위에 중복 문구만 골라 압축: portNote「이 기기 브라우저에만 저장」(패널 제목의
  「저장은 이 기기」와 중복) 삭제 · calcNote「직접 입력」↔「실시간 시세 미연동」 동어반복
  정리 · goalBody 안내에서 단계 3종 나열 삭제(제목이 이미 나열) · goalPanel 제목의
  「저장은 이 기기」 삭제 · 예정 주문 칩 안내 축약. **정보는 1건도 안 지웠다 — 같은 말을
  두 번 하는 곳만 지웠다.** 이 구분이 「중용」의 기준선.
- v135 (소유자 질문 — 「괴리율 보는 법도 지정가 주문법도 모른다」): guide.html §③ 에
  **「주문 전에 — 괴리율 확인」·「지정가로 주문하는 법」 2절 추가**. NAV↔현재가 정의,
  괴리율 부호별 불리한 쪽(표), MTS/네이버에서 찾는 위치, 호가창 읽는 법(매도호가=살 때
  체결·매수호가=팔 때 체결), ETF 호가단위 5원, 안 붙으면 값 올려서라도 당일 체결.
  **근거: 괴리율 흔들림(418660 표준편차 0.84%, nav_history 실측) < 하루 지연 시 새로
  떠안는 시초가 갭(2.4%, 03_System_Params §)** — 그래서 「미루지 마라」가 유지되고,
  체결 위생만 보강한다(규칙 변경 0건). 신호 화면엔 넣지 않음 — v133~v134 가 화면을
  깎아낸 직후라 상세는 설명서가 맡는다는 분업 유지.
- v136 (소유자 승인): 전환일 체크리스트 아래에 **주문 방법 링크**(`guide.html#order` —
  v135 의 괴리율·지정가 2절). 전환은 연 2~3회뿐이라 그때마다 호가창을 다시 배우는 게
  실제 비용이다. **`<a>` 는 `<label>` 밖에 둔다** — label 안이면 링크를 눌러도 체크박스가
  토글된다. guide 쪽 h3 에 `id="order"` + `scroll-margin-top` 부여.
- **측정 감사 (2026-08-31, 소유자 지시 — 전략 변경 0건)**: `research/MEASUREMENT_AUDIT.md`.
  **「손실 0 문턱 7년」을 인용할 땐 반드시 유효표본을 병기하라** — 12,099창은 독립 관측이
  아니고 **비중첩 7.9개**(AR-ESS 10.7)다. 부트 재표집 68%가 손실확률>0, 견고한 0 은 15년.
  소유자 판정 ⓐ: **수치 유지 + 오차막대 병기**(guide.html 확률표는 그대로 — 화면 압축
  방향과 충돌). ISA 만기 자금의 연금 이체는 **기각**(레버리지가 IRP 에서 매매 불가라
  B 를 못 굴린다 — 최단 3년에서도 복리 격차 27% > 공제 10%). OTM 풋 테일헤지도
  **문헌 기각**(분산위험프리미엄 음수 — 04 §5-7). 재현: `horizon_ess.py`·`dsr_b.py`·
  `isa_pension.py`. **심화 3축도 완료**(`tranche.py`·`drag_sigma.py`·`withdraw.py`):
  **트랜치는 기각**(04 §5-7) — 무작위 200회 반증에서 MDD·Calmar 는 100% 개선이나
  **20년 p05 24%·최종배수 31%** 만 개선, 관문① Calmar +5.0%(기준 +10.2%) 미달.
  **MDD 를 부의 바닥으로 산 것** = 기전 1(전환을 나누면 V자도 덜 먹는다).
  **재탐색 금지.** σ 의존 드래그는 **비쟁점** — ★**`qldr` 은 2006 이후
  실물 QLD** 라 21세기 기준 차이 +0.0%. 인출 엔진: 명목 정액은 무력한 검사이고 **진짜 위험은
  소득 붕괴**(최악 연간 −51.3%) → **인출기엔 1년치 현금 완충이 설계 요건**.
- **운영 위험 6축 (2026-08-31, 04 §5-8 · `research/ops_risk.py`)**: 전략 변경 0건,
  운영 규칙 3개 산출. ① **전환 놓침의 위험은 꼬리에 있다** — 139회 중 102회는 놓쳐도
  이득(중앙 +1.9%)이나 **최악 −96.5%**(닷컴 방어 진입). **낙폭 게이지 「근접」일 때만
  절대 놓치지 말 것.** ② **자동 수집 종가에 이상치 가드 없음** — ±10% 오류가 신호를
  49/200 회 바꿨다. `update_signal.py` 급변 가드 권고(§2 금지 영역이라 미구현, 승인 필요).
  ③ **연속 손실 전환 최장 10회·누적 −20.4%, 구간의 54%가 손실** — 인내 요건 수치.
  확인된 전제(조치 불요): 방어 분산 유효(2022 지수 −32.6% vs 바스켓 −4.8%) ·
  일반계좌도 B 압도(1배 보유의 70배) · 비용 손익분기 편도 2.5%(모형의 25배).
- **v137 (소유자 지시 — 운영위험 후속 전면 반영)**: ① **종가 이상치 가드**
  (`update_signal.py` — 큰 움직임 10%↑ 시 **교차 출처 대조**. 크기만으론 진짜 폭락과
  데이터 오류를 구별 못 하므로. **실패 방향 규약: 인프라 문제엔 통과(fail open)** —
  대조 소스가 죽었다고 진짜 폭락일 신호를 막으면 04 §5-8 의 −96.5%가 현실이 된다.
  전략 무접촉 66줄 순수 추가, I11 PASS). ② **설명서 §③에 인내 요건·전환 우선순위 2절**
  (전환의 54%가 손실·최장 10연패 −20.4% / **게이지 「근접」 전환만 절대 사수**, 평시
  전환은 놓쳐도 중앙 +1.9%). ③ **인출 물가 스윕**(`withdraw.py` [5-b] — 물가 6%·인출 5%
  에서도 파산 0%. **파산확률은 이 전략의 인출 위험 지표가 아니다**; 실제 위험은 소득 변동.
  ⚠ FRED CPIAUCSL 접속 불가로 실계열 미사용 — 상수 스윕). ④ **체결비용 실측 하네스**
  (`exec_cost.py` — 판정 규약 사전 고정: 전환 20회+괴리율 60영업일. 현재 0/20 판정 불가.
  다만 418660 최대 괴리 1.45%가 모형 슬리피지 0.1% 초과 — n=4 라 관찰만).
- **v140 (소유자 지시 — 「사람 개입은 리밸런싱할 때만」)**: **자동 파수꾼**
  (`.github/workflows/watchdog.yml` + `deploy/watchdog.py`, 전략 무접촉 신규 파일).
  기존 알림은 `if: failure()` 뿐이라 **워크플로가 「아예 안 돈」 경우를 못 잡았다**
  (예약 슬롯 통째 스킵은 실적이 있다 — daily-signal.yml 주석). 네 가지를 감시:
  ① **신호 신선도** — 평일 08:40 KST(한국장 개장 20분 전)에 `signal.json` as_of 가
  **3영업일** 이상 밀리면 카톡. 정기 휴장은 연속 1영업일뿐이고 비상 휴장 최장 기록도
  2일(2012 샌디)이라 오경보가 없다 — 4일(2001 9/11)급이면 알림이 오는 쪽이 맞다.
  같은 알림은 3영업일마다 한 번만(알림 피로 · 이슈 댓글도 같은 주기). ② **알림 채널 생존** — 카카오 refresh
  토큰 교환·Telegram getMe 로 **메시지를 보내지 않고** 자격증명만 확인. 종전에는
  토큰이 죽어도 경고가 stderr 로만 나가고 그 스텝이 `continue-on-error` 라
  **정작 전환일에 조용히 안 왔다.** ③ **자동 점검** — `내가_보는_것/점검.py --json`
  (새 모드, 사람용 출력과 **같은 계산**에서 나온다)을 월요일에 돌려
  `data/ops_check.json` 에 기록. 알림은 **상태가 나빠졌을 때만**(Level 상승·새 AUM
  경보) — 같은 상태 반복은 침묵(분기 규약의 대응이 「지켜본다」이므로).
  ④ **방어 재조정일 알림** — 전환일엔 카톡이 가는데 **30일 재조정일엔 안 갔다**
  (화면을 열어야 D-day 를 봤다). 둘 다 실제 매매인데 한쪽만 알림이 있으면 사람이
  달력을 신경 쓴다. `defense_entry()` 는 signal.html `getDefenseEntryDate` 와 같은 규약
  (진입 기록이 장부 밖이면 **미확정 — 알리지 않는다**), `rebalance_due()` 는 주기일이
  주말이면 다음 평일로 밀며 **상태 저장 없이 주기당 정확히 1회**만 참이 된다(1년 전수 검증).
  한국 휴장일은 밀지 않는다 — 화면의 「휴장이면 다음 개장일에」와 같게 둔다.
  화면은 **평시 「버전·동결」 접힘 안에 한 줄**, 할 일이 있을 때만 상단 배너
  (drawOpsCheck — 화면에서 계산하지 않고 json 만 읽는다. todo 는 textContent 로만 삽입).
  **결과: 소유자의 정기 파이썬 실행 의무 0.** 점검.py 는 「직접 보고 싶을 때만」으로 강등.
  이슈 자동 개설(label `watchdog`)은 카톡이 죽었을 때의 마지막 통로(메일).
- **v141 (소유자 질문 「여러 소스로 탄탄해지는 게 없나」 후속)**: **비상 수동 판정법**
  (guide.html §⑩ `#manual`) — 전 자동화가 GitHub 단일 도메인이라 동시 장애 시 소유자가
  장님이 되는 공백을 문서로 메움. 「QQQ 종가 ≤ 52주 최고가 × 0.84 → 방어」 근사가
  정식 판정(252거래일 고점)과 **21세기 6,700일 불일치 0일**(54년 0.09%) 실측 후 등재.
  signal.html 은 v118 로드 실패 문구에 그 안내 한 문장만 추가(textContent 유지).
  같은 질문에서 검토 후 **비제안**으로 닫은 것: 근접 구간 상시 교차 대조(update_signal) —
  캐시가 keep=last 자가 치유 + 10% 가드 존재 + 최악이 하루 지연(평균 2.4%)이라 한계이득 소.
- **v142 (소유자 요청 — 「게임 패치노트처럼」)**: **세 번째 탭 `notes.html`**(업데이트 노트).
  단일 파일·바닐라, guide.html 토큰 그대로(다크 포함). 항목마다 **무엇이 / 왜 / 결론**
  3필드 고정 — 소유자가 지정한 형식이다. **최상단 히어로가 이 화면의 존재 이유**다:
  「매매 규칙은 한 번도 바뀌지 않았습니다 · 규칙 변경 0회」 + 동결 후 경과일
  (data/freeze.json 을 읽고, 실패하면 하드코딩 날짜로 표시). 필터 칩 4종
  (전체/신규/수정/삭제 — data-k 로 거르고 빈 시즌은 숨김, 거르는 중엔 접힌 이전 목록
  자동 펼침). v90~v141 을 5개 시즌으로 묶고 v122 이전은 details 로 접음.
  내용은 **git 커밋 이력·CLAUDE.md §4 에서 뽑은 실제 사건만** — 새 서사 창작 0.
  검사 3개 추가(verify_all: 탭 연결·Pages 복사 목록·「규칙 무변경」 문자열 존치 —
  그 문장이 사라지면 이 화면은 단순 변경 목록이 되고 동결의 의미가 화면에서 사라진다).
- **v144 (소유자 지적 — 「너무 자그맣고 신호등이 없어 티가 안 난다」)**: **평시 시계에
  신호등 복원.** v107 이 2카드를 한 줄로 접으면서 `.dot`(장중 초록)을 **같이 잃었다** —
  압축의 부작용이 1년 가까이 남아 있던 자리다. 장별 점 1개씩(초록=개장·빨강=닫힘) +
  13px→14px. 서머타임·휴장은 **이미 정확했다**(Intl IANA 타임존이 DST 처리 · NYSE
  휴장은 규칙 산출(부활절 Meeus) · 조기마감 13:00 ET · 한국 휴장은 `kr_holidays.json`).
- **v145 (소유자 요청 — 「주식앱 안 보고도 시세 파악」)**: **4다리 시세 스냅샷.**
  `deploy/price_now.py` → `data/price.json` → 화면 자산 행에 가격·등락률 배지 +
  현재가 입력 기본값. **CORS 실측**: 야후·스투크·네이버 전부 브라우저 직접 호출 차단이라
  클라이언트 조회 불가 → Actions 수집 방식만 가능. 출처는 `nav_collect.py` 와 **같은
  엔드포인트**(네이버 ETF 목록, cp949) — 새 의존성 0. `price.yml` 이 한국장 중 30분마다.
  **★ 전략 무접촉이 핵심**: 판정은 QQQ 미국 종가만(동결) — verify_all 이 「신호 생성이
  price.json 을 읽지 않는다」를 기계로 막는다. 실시간이 아니므로 화면은 **항상
  「몇 분 전」**을 표시한다(예약 슬롯 건너뜀 실적이 있어 더 필요).
- **v146 (소유자 요청 — 괴리율 + 외부 사이트)**: ① **괴리율 배지** — 자산 행에
  「괴리 +0.06%」, **1% 초과일 때만** 「살 때 불리 / 팔 때 불리」를 덧붙이고 강조색.
  임계·문구는 **설명서 §③(v135)에서 그대로 가져왔다**(±0.3% 평범 · 1% 초과 주의 ·
  +는 살 때 불리). 화면이 새 기준을 만들지 않는다. ② **종목코드가 네이버 금융 바로가기**
  (호가·NAV 를 한 번에 — 4다리 URL 200 확인). ③ 설명서 §③에 **「바깥에서 보기」** 절.
  **★ iframe 이식은 불가 — 실측**: 구글 파이낸스·야후는 `X-Frame-Options: SAMEORIGIN`
  이라 심으면 **「403 액세스할 수 없습니다」**가 뜨고(눈으로 확인), 네이버 브리핑은
  헤더가 없는데도 **백지**다. 링크 여는 방식만 작동한다. 네이버 AI 브리핑은
  **고정 목록 URL 이 없다**(`/briefing/market` 404 · 글 주소가 매일 4423→4424 로 바뀜)
  → 증권 홈으로 걸었다. 재제안 전에 이 실측을 볼 것.
- **v147 (소유자 — 「NAV로 싸게 사는 법을 모른다」)**: 설명서 §③에 **「NAV로 조금 더
  싸게 사는 법 — 실제 절차」**(`#navtip`) + **괴리 배지를 그 절로 가는 링크**(`?` 표시).
  핵심은 **기존 규칙의 이유를 드러낸 것**: 09:05~15:20 은 **LP 호가 의무 시간대**라
  (01 §·03 § 에 이미 있던 근거) 그 안에서 체결하는 것만으로 이미 절반은 지킨다.
  표(살 때 +가 불리 / 지정가는 NAV 쪽 한두 칸) + **숫자 예시**(NAV 39,257 vs 39,650 =
  100주에 39,300원) + **한계 경고**(아끼는 폭 0.84% < 하루 미룸 2.4% — 아끼는 것보다
  못 붙이는 것이 3배 비싸다). 화면 스냅샷은 「오늘 벌어져 있나」용이고 **체결 순간 값은
  MTS 가 정확**하다고 명시 — 스냅샷을 실시간인 척하지 않는다.
- **v148 (소유자 지적 — 「패치노트도 넣고 있지? 정합성 검사는?」)**: **둘 다 안 했었다.**
  v144~v147 을 구현해 놓고 **정작 그 기록용으로 만든 notes.html 을 안 고쳤고**(v143 에 멈춤),
  FILES.md 에 v145 신규 2개(`price_now.py`·`price.yml`)가 빠져 있었다. 둘 다 채웠다
  (노트 25항목 · 등재 누락 0). **★ 재발 방지는 자각이 아니라 관문으로**:
  verify_all 에 **「업데이트 노트가 CLAUDE.md §4 최신 vNN 보다 뒤처지면 실패」** 추가(실패 = 이슈·메일이지 배포 중단이 아니다 — §0 정정 참조) —
  **일부러 v999 를 넣어 FAIL 이 나는 것까지 확인**했다(통과만 보고 넘어가면 검사가 아니다,
  §-1 ⑤). 같은 작업에서 **`verify_all.py` 에 `import re` 가 없어 새 검사가 조용히 죽던 것**도
  발견·수정 — 「검사를 추가했다」와 「검사가 돈다」는 다르다.
  **이 세션의 결함 3건이 전부 같은 유형이다**: 종가 가드(구현됐는데 미구현 표기) ·
  FILES.md(49개 미등재) · 이번 노트. **실행은 됐는데 기록이 안 따라온 것.**
- **v149 (소유자 — 「여기 공간이 좀 남는데」)**: 신호 카드 판정 옆 빈자리에 **공격 자산
  시세**(가격·등락·스냅샷 시각, `#navtip` 링크). 겸사겸사 **「2배」 뒤 고아 가운뎃점** 제거 —
  뒤따르던 칩이 줄바꿈되면서 구분자만 남아 있었다.
  **★ 방어 상태에서는 일부러 숨긴다**: 3종을 한 숫자로 합치면 **어느 앱에서도 대조할 수 없는
  합성 값**이 된다. 이 저장소는 사용자가 검증할 수 없는 숫자를 화면에 만들지 않는다 —
  대칭성보다 대조 가능성이 우선.
- **v150 (소유자 — 「너무 길고 어려워」)**: NAV·괴리율 설명을 대폭 축소.
  §③이 같은 얘기를 **3곳**(주문 전 확인·실제 절차·지정가 주문법)에서 공식·용어·경고를
  겹쳐 말해 한 화면을 넘겼다. 공식(현재가−NAV÷NAV×100)과 「유동성공급자(LP)」 설명을
  뺐고, 중복된 「하루 미루면 손해」 경고는 **한 번만** 남겼다. 두 절로: 「30초 설명」
  (표 하나, +는 살 때 불리·−는 팔 때 불리) + 「1%를 넘었다면 할 일 딱 하나」(예시 1개).
  `#order`·`#navtip` 앵커는 유지(signal.html 이 그 id 로 링크한다).
- **v151 (소유자 질문 — 「NAV가 더 비싼 상황엔?」)**: §③ 「1%를 넘었다면」 절에
  **팔 때(−, NAV가 더 비쌈) 예시**를 추가 — v150 이 살 때(+) 예시만 남기고 팔 때를
  표로만 두고 잘랐던 것. 방어 전환·30일 재조정은 파는 행동인데 정작 그 요령이 없었다.
- **v152 (소유자 지시 — 「이런 쓸데없는 문구는 전부 삭제」)**: 「바깥에서 보기」의
  구현 사정 설명(iframe 왜 안 됐는지·왜 다시 안 만드는지) 2문단 삭제. **왜 이게 계속
  생겼나**: v146 에서 내가 iframe 실측(403·백지)을 남길 때 **개발 기록과 사용자 안내를
  안 갈랐다** — 실측 자체는 CLAUDE.md v146 항목에 이미 남아 있으므로 화면 쪽은 중복이었다.
  **규칙**: 「우리가 왜 이렇게 만들었는가」는 CLAUDE.md, 「무엇을 어떻게 쓰는가」만 화면.
  「보는 것은 자유, 규칙은 그대로」(§⑨ 원칙)는 성격이 달라 남겼다.
- **v153 (소유자 결정 — 새 탭 대신 접기)**: §⑥ 배율 다이얼(미국 직투)·§⑧ T4 를
  `<details class="foldsec">` 로 접었다(기본 닫힘). id 는 그대로 `#lev`·`#t4` 유지 —
  signal.html T4 패널 링크·guide 내부 §② 참조가 그 앵커를 쓴다. **접힌 절 안의 앵커로
  들어오면 자동으로 펼치는 JS**(`closest('details.foldsec')` → `open=true` →
  scrollIntoView) 를 페이지 끝에 추가 — 없으면 링크를 눌러도 내용이 안 보인다(v143 의
  notes.html 스크롤 버그와 같은 함정). 새 탭이 아니라 접기를 고른 이유: 4번째 탭은
  3개 화면의 탭바·배포 목록·검사를 전부 늘리는데, 접기는 guide.html 안에서 끝난다.
  겸사겸사 §③의 **정의 없이 남아 있던 「LP」 약어**(v150 이 정의 문단만 지우고 이 줄을
  놓침)와 **같은 경고(하루 미루면 손해)가 두 곳에서 반복**되던 것을 정리했다.
- **v154~v160 (소유자 지시 7종, 2026-09-01)**: 전부 signal.html/guide.html 순수
  클라이언트 작업 — 새 데이터 파일·Python 0. 항목별 커밋 분리.
  **★ 지시문 전제 3개가 실제와 달라 실측으로 바로잡았다** (전제를 그대로 코딩했으면
  전부 조용히 틀렸을 것):
  ① **`AUTO.recent` 는 최신이 [0] 인 내림차순**(signal.json 실측) — 지시문의
  `rc[len-1]-rc[len-2]` 는 **2주 전 이틀**을 잰다. `rc[0]-rc[1]` 이 맞다(0.05 vs 0.17%p).
  수동 rows(res)는 반대로 오름차순이라 `last-prev` — 두 경로가 정렬이 반대다.
  ② **`innerText` 는 접힌 `<details>` 본문을 못 읽는다**(실측) — 지시문은 읽힌다고 했다.
  FAQ 9개·접이 절 2개가 전부 접혀 있어 검색이 거의 다 실패했을 것. **`textContent`** 로 구현.
  ⚠ 첫 검사는 「252일」로 했다가 그 문자열이 **항상 보이는 summary 에 있어** 오탐 —
  본문에만 있는 문구로 다시 재서 확정(§-1 ⑤ 변별력 없는 검사).
  ③ **`execPanel` 은 v133 에서 삭제됐다** — 지시문이 그 위치를 지목했으나 없으므로
  changeBox(체크리스트) 안에만 넣었다.
  구현: v154 전일 대비 낙폭 · v155 OOS 전환 횟수 vs 연 2~3회(**1년 미만은 판정 보류** —
  장부 4일치로 「올해 0회」는 오해를 부른다) · v156 설명서 검색 · v157 함께한 날
  (신규 키 `b_firstseen_v1`) · v158 환율 민감도(직접 입력 — CORS 로 조회 불가, v145 실측.
  **방어 상태는 계산 안 함**: 3다리 환 민감도가 달라 합성 배수는 대조 불가) ·
  v159 자동 현재가 노후 경고(기존 warns 재사용) · v160 「오늘 못 하셨다면」(01 §3 인용).
- **v161~v164 (소유자 지시 2군 8종 중 4종 구현, 2026-09-01)**: 소유자가 「쓸모없는 건
  안 해도 된다」고 명시 — **4종 구현 · 4종 근거 갖춰 기각**.
  **★ 지시문 전제 3개가 또 틀렸다(전수 검사로 잡음)**:
  ① **2군-a(T4 동의율)는 이미 v126 에 구현돼 있다**(`t4Agreement()`) — 중복 제작할 뻔.
  ② **2군-b: `hist_tiger.py` 에 재사용 가능한 함수가 없다** — 추적오차 계산이 전부
  `__main__` 블록(29~76행)이라 「그 함수를 읽기 전용 호출」이 불가능하다. 더 결정적으로
  **그 「추적오차 34.69%」는 상품 품질이 아니다**: TIGER 418660 은 원화환산 지수의
  2배라 **환노출 ×2**인데 비교 대상 QLD×환율은 **환노출 ×1** 이라, 원화 약세 구간의
  구조 차이가 그대로 오차로 잡힌다(연차이 +14.07%p 의 정체). 화면에 「추적오차 ±34.7%」로
  쓰면 정면으로 오해를 부른다 → **기각**. 재제안 전에 이 문단을 볼 것.
  ③ **2군-h: `execPanel` 은 v133 에서 삭제**(v160 때와 같은 오류). 그리고 수수료 차이는
  **경제적으로 무의미**하다 — 04 §5-8 이 손익분기를 **편도 2.5%(모형의 25배)**로 실측했다.
  0.01%대 수수료 표는 의미 없는 최적화를 부르고, 틀린 사실을 실을 위험만 남는다 → **기각**.
  ④ **2군-g(비중 이탈 히트맵) 기각**: 공격 상태(전체 일수의 82%)엔 100% 단일 자산이라
  이탈이 사실상 평평하다 — 대부분 빈 줄무늬를 위해 새 키·새 위젯을 만드는 셈.
  구현: v161·v162 자동 검증 배지(GitHub SVG 직접 — API·CORS 불요, 실측 「검증 - passing」)
  + 역대 평균 전환 간격(성과표의 years·switches 재사용, **예측 아님** 명시. 전환 기록이
  없으면 「마지막 전환 후」를 쓰지 않는다) · v163 놓친 전환 감사(±10일 창 — v106 의 7일
  상한과 목적이 달라 값도 다르다. **판단하지 않는다**) · v164 낙폭 백분위
  (`research/emit_dd_distribution.py` 신규 — **수동 실행·연 1회**, 자동 편입 금지.
  검산: −16% = 더 깊었던 날 18.1% 가 **방어 보유일 비율 18.1% 와 일치**).
- **v165~v166 (소유자 지시 3군 5종 중 2종 구현, 2026-09-01)**: 「쓸데없이 리소스 먹거나
  의미 빈약한 건 걸러내라」는 지시대로 **2종 구현 · 3종 기각**.
  **★ 지시문 전제 3개가 또 틀렸다**: ① **3군-a(닷컴 타임머신)는 이미 v127 에 있다**
  (`data/crisis_paths.json` 38KB + trajPanel 스크러버) — 2군-a 에 이어 **두 번째 중복 지시**.
  ② 자동화 절은 §⑧ 이 아니라 **§⑩**(v132 재배열), ③ `.flow`/`.ph` 는 §⑤ 가 아니라 **§⑦**.
  **기각 3종과 근거**:
  ⓐ **3군-d(총액 이력 3종) 전면 기각** — 23(그냥 뒀다면 대조)의 **「추가 납입 없음」
  가정이 소유자에게 성립하지 않는다**(본인이 「한 달에 한 번 월급 붓는 정도」라고 명시).
  캐비엇으로 덮을 문제가 아니라 **가정 자체가 깨진 것**이다. 24(히트맵)는 2군-g 와 같은
  이유(공격 82% 구간에 단일 자산이라 평평), 30(내 궤적)은 화면을 연 날에만 쌓여 듬성한데
  **증권앱이 입출금까지 반영해 더 정확히 보여준다.** 셋이 빠지면 공용 저장소
  `b_valhist_v1` 만 남는데 **쓸 곳 없는 기록을 매일 쓰는 건 순수 낭비** → 키 자체를 안 만든다.
  ⓑ **3군-e(공유 카드) 기각** — **혼자 쓰는 비공개 도구라 받을 사람이 없다.** 알림은 카톡,
  백업 공유는 이미 있다. canvas 는 폰트 미로드·DPR·테마 캡처 시점 함정이 있는데 그 대가로
  얻는 게 없다.
  구현: **v165 평시 팩트 한 줄** — ★ 새 문장 창작 0. `FACTS` 배열 8개는 전부 guide/signal 에
  **이미 발행된 문장**이고 각 항목에 출처 주석을 달았다(투자 판단 문구 자동 생성 금지 규약).
  날짜 기반 결정론(새로고침해도 하루 동안 고정) · 전환일엔 숨김 · 높이 19px.
  **v166 신호 흐름 5단계** — `.flow`/`.ph` 재사용, 5열→2열→1열. **사람이 하는 4단계만
  강조**. 카드별 details 는 넣지 않았다: 바로 아래 표가 이미 상세라 이중 서술(v134 중용선).
- **v167 (소유자 지시 — 「접힌 카드 맨 밑으로 · 순번 조절 · 긴 부분 요약」)**:
  설명서 재배치. **새 순서**: ①30초 ②지평 ③루틴 ④필수6 ⑤−16vs헤지 ⑥단계 ⑦동결
  ⑧자동화 ⑨FAQ · **접힌 참고 2개를 맨 뒤로** ⑩배율(미국 직투) ⑪T4.
  **긴 절 7곳에 `.tldr` 한 줄 요약** — 그것만 읽어도 결론을 알게(전부 그 절 안의 결론을
  압축한 것 · 새 주장 0). **★ 번호 참조 전수 갱신**: guide 목차·내부 링크 · signal 13곳
  (§⑦→§⑥ 4곳 · ⑩→⑧ 2곳) · notes 3곳. **검증**: 깨진 내부 링크 0 · 접힌 절 앵커
  자동 펼침(v153) 유지 · 검색(v156) 재배치 후에도 접힌 절 안까지 탐색.
  **겸사겸사 원래 틀려 있던 참조 2건 정정**: ⓐ guide ④ 안의 「아래 ④의 헤지6/4」가
  **자기 자신**을 가리키고 있었다(헤지6/4 는 ⑤). ⓑ signal 의 T4 동의율 문구가
  「사건 단위 사전 고정 기준(설명서 §⑥)」으로 **배율 다이얼**을 가리키고 있었다(T4 절이 맞다).
  둘 다 재배치와 무관하게 이전부터 있던 오류다 — 전수 조사가 아니었으면 못 찾았다.
- **v168~v169 (소유자 지시)**: ⓐ **신호 화면 「규칙」 패널 삭제** — 성과 비교표와 중복.
  `drawPicker()` 통째 제거(`#opts`·`#scen`·`#scennote` 참조가 남으면 null 오류).
  기준 칩도 사라지지만 성과표가 **PERF_SHOW 3기준을 모두 나열**하므로 정보 손실 0이고,
  `scen` 은 심사 한 줄의 기준으로만 남아 kr_1997 에 고정된다. **★ 다만 그 패널에만 있던
  「표본이 얇습니다(전환 19회 미만 — v41 §5)」 경고는 drawPerf 각주로 옮겼다** — 패널을
  지우면서 경고까지 지우지 않는다.
  ⓑ **세 화면 전체 패널 접기 + 접힘 기억**(신규 키 `b_fold_v1`). 설계 요점:
  **기본은 펼침, 접은 것만 저장**한다 — 「내 선택이 곧 기본값」이라는 소유자 요구 그대로.
  **★ DOM 을 재구성하지 않는다**: `.folded` 클래스로 h2 외 형제를 CSS 로 숨길 뿐이라
  요소가 DOM 에 남아 화면 곳곳의 `getElementById` 가 그대로 산다(감싸는 방식이었으면
  drawPort/drawPerf 등이 전부 깨졌다). 대상 21곳(signal 7 · guide 9 · notes 11 중 중복 제외).
  **검색·필터와의 충돌 해소**: 접힌 칸이 검색/필터에 걸리면 빈 칸으로 보이므로 **화면만
  임시로 펼치고 저장값은 건드리지 않는다** — 검색을 지우거나 「전체」로 돌아오면 복원.
- **v170 (소유자 지시 — 「계좌·T4 는 접어서 맨 아래로」 + 「업데이트 노트가 너무 길다」)**:
  ⓐ **신호 화면 순서**: perfPanel → **trajPanel(위기 궤적) → taxPanel(계좌) → t4Panel(T4)**.
  뒤 둘은 **기본 접힘**(`data-foldshut`) — 한 번 정하면 자주 볼 일이 없는 칸이라는 소유자 판단.
  **★ v169 의 「내 선택이 곧 기본값」을 깨지 않으려면 저장이 3상태여야 한다**: 저장값 없음 →
  `data-foldshut` 유무로 결정 · **1=접힘 · 0=펼침**. 종전엔 「펼침=키 삭제」였으므로 기본 접힘
  패널을 펼쳐두면 새로고침 때 도로 접혔다. 판정을 `foldShut(p, st)` 한 곳으로 모으고
  **세 화면 · 복원 지점 3곳(setupFold · guide 검색 reset · notes 필터 all)을 전부 같은 함수로**
  갈았다 — 한쪽만 고치면 검색·필터가 기본 접힘을 잘못 되돌린다. 옛 저장값(1 뿐)과 호환된다.
  ⓑ **업데이트 노트 솎아내기 46건 → 27건**(보이는 17 · 접힌 10). 소유자 규정:
  **「글자 수·위치를 바꿨습니다」류는 넣지 마라 — 쌓이면 가독성이 죽는다.**
  등재 기준을 화면(v170 항목)에도 명시했다 — **기능이 생기고·바뀌고·사라진 것** +
  **몰랐으면 틀린 값을 믿었을 오류**만. 걷어낸 21건: 순서 재배치(v167) · 문구 축소(v150·v152) ·
  설명 보강(v135·v151) · 스타일 복구(v134) · 하루살이 뒷정리(v143·v148) · 내부 버그(v111) 등.
  **병합 4건**(v164+154 낙폭 맥락 · v162+155 전환 리듬 · v145+149+159 시세 ·
  v146+147 괴리율) — 정보는 안 버리고 항목 수만 줄인 것.
  시즌 id 도 실제 범위에 맞춰 정리(sea144→sea145 · sea107→sea113 · **seax→seafrz**).
  ⚠ **다음 세션 주의**: 이제 노트에 **안 올리는 것이 정상**인 작업이 있다. 그러나 verify_all 의
  노트 지연 검사는 여전히 「CLAUDE.md §4 최신 vNN ≤ 노트 최신 vNN」이므로,
  **화면을 바꿔 vNN 을 올렸으면 노트에도 그 vNN 이 한 줄은 있어야 한다** —
  자잘한 변경이면 새 항목을 만들지 말고 **그 시즌 기존 항목의 결론에 한 구절로** 붙여라.
- **v171 (소유자 승인 — 「월간 성과 스냅샷도 파수꾼이 보게」)**: `watchdog.py` 에
  **`stats` 모드 추가**(순수 추가 · 기존 4모드 무변경 · 전략 무접촉 · 파일 읽기만).
  **발단**: 소유자가 「성과표의 원화·실물 TIGER 는 한 달마다 갱신되냐」고 물어 확인해 보니
  `monthly-stats.yml` 은 **cron 은 맞는데 실행 이력이 0회**였다 — 워크플로가 2026-08-29 에
  추가됐고 그 뒤 첫 1일이 2026-09-01 이라 **오늘이 사상 첫 실행**이다.
  그 과정에서 **v140 이 막은 것과 똑같은 구멍이 이 워크플로에만 남아 있는 것**을 발견:
  실패 알림이 `if: failure()` 뿐이라 **예약이 통째로 건너뛰면 아무도 모른다.**
  ★ **신호보다 훨씬 안 보인다** — 신호는 매일이라 신선도 도트가 티를 내지만
  성과표는 월 1회라 **두 달이 밀려도 화면이 똑같아 보인다.**
  **문턱 45일**: 정상 주기는 최대 31일이고 한 번 건너뛰면 62일이다 —
  45 는 **한 번 건너뛴 것을 다음 예약일이 오기 전에** 잡는 자리다(31 은 월말 오경보,
  62 는 두 달 방치). 이후 **7일 간격**으로만 재알림(알림 피로).
  **알림 문구가 「매매와는 무관」을 먼저 말한다** — 신호 정지와 혼동하면 안 되므로.
  검증: **경과일 0~74일 전수**로 발화 지점이 정확히 45·52·59·66·73 인 것과
  **정상 주기 0~31일에서 거짓 알림 0**, 파일 손상 시 알림, 전 경로 종료코드 0 확인.
  겸사겸사 `main()` 사용법 문자열에 빠져 있던 `rebalance`·`check` 도 채웠다.
  ※ `deploy/*` 수정이라 통상 ultra 제안 조건 ⓑ 이지만, **환원 대신 전수 검산이 가능한
  날짜 함수**(rebalance_due 와 같은 모양)라 ⓓ 단서에 따라 max 로 충분하다고 판단.
- **v172 (소유자 지시 — 「관문 넣고, 쓸데없는 스킬은 막아줘」)**: 세 갈래.
  ⓐ **파일 지도 관문**(verify_all): 추적 파일이 `FILES.md` 에 없으면 실패. v148 에 49건
  났던 사고를 자각 대신 관문으로 막는다. **폭을 좁게** 잡았다 — deploy/research/audit 의
  `.py`·`.md` · `내가_보는_것/` · workflows · 루트 `.py`/`.html` · `data/` 최상위만
  (archive·docs·data/hist 제외. 넓히면 오탐이 나 관문이 무시당한다). **검사 대상 164개.**
  ★ **git 은 한글 경로를 이스케이프해 뱉는다(실측 131건)** — `core.quotepath=false` 가
  없으면 「내가_보는_것/」이 통째로 검사에서 빠진다. 만들자마자 **실제 누락 2건**을
  잡아냈다(`research/MEASUREMENT_AUDIT.md` · `data/retired_numbers.json`) — 등재 완료.
  ⓑ **★ v168 회귀 정정 — 이 관문을 만들다 발견했다.** 규칙 패널(`drawPicker`)을 지우면서
  **지표 6종 검사 + CAGR 검사 7건이 조용히 깨져 있었다.** 지표는 화면에 그대로 있다
  (성과표가 그린다) — **검사만 삭제된 함수를 겨누고 있었다.** 검사의 뜻은 그대로 두고
  보는 곳만 살아 있는 렌더(`>최종배수</th>` 등 열 머리)로 옮겼다.
  **왜 못 잡았나**: ① 로컬 필수 검증이 `--fast` 인데 **화면 검사는 fast 에 없다**,
  ② verify.yml 의 전체 검증이 **예약·수동에서만** 돌았다. → **전체 검증을 push 에도**
  걸었다(실측 3~4초). 교훈: **§2 가 「verify_all 은 화면 문자열까지 검사한다」고 이미
  경고했는데 나는 --fast 만 돌렸다.** 화면 요소를 지우면 `verify_all.py` 를 **전체로** 돌려라.
  ⓒ **스킬 차단**(`.claude/settings.local.json` 의 `skillOverrides`, 프로젝트 로컬·gitignore).
  `theme-factory`·`web-artifacts-builder`·`algorithmic-art`·`morning`·`setup-writing-style`
  = **off**. 근거 — **theme-factory**: 이 화면의 색은 취향이 아니라 **규약**이다
  (매수 빨강·매도 파랑은 MTS 관행 v92, 공격 파랑은 판정 상태와 묶임). **web-artifacts-builder**:
  React/Tailwind 는 빌드 단계를 만드는데, **빌드 없는 단일 파일이 v141 비상 수동 판정의
  전제**다. **morning**: v140 이 없앤 「정기 확인 의무」를 되살리고 세 탭 **바깥**에
  화면을 만든다 — §3 웹사이트 자족 원칙 위반(신호 화면의 「오늘 할 일 없음」이 그 역할).
  **setup-writing-style**: 「소유자가 본인 이름으로 보낼 글」용인데 여기 글은 소유자가 읽는 글.
  **algorithmic-art**: p5.js 외부 의존 + 세로만 늘린다. **켜 둔 것**: `skill-creator`·
  `import-memory` + 문서형 4종(xlsx/pptx/pdf/docx). **재제안 전에 이 문단을 볼 것.**
  ※ **스킬을 새로 만드는 것은 보류**로 판정했다 — 이 저장소가 지는 지점은 「절차를 빠뜨리는
  것」인데, **스킬은 내가 불러야 작동한다.** 빠뜨림이 문제인 상황에 「기억해서 부르는 도구」는
  같은 실패를 반복한다. v148 이 이미 답을 냈다 — **재발 방지는 자각이 아니라 관문으로.**
- **v173 (소유자 질문 「실시간은 못 하나」에서 시작 — 실제 결함 2건 적발)**:
  ⓐ **★ 시세가 배포되지 않고 있었다 (v145 이후 계속).** 봇 커밋은 무한루프 방지 때문에
  `push` 이벤트를 만들지 않는데(pages.yml 주석에 **이미 적혀 있던 사실**), pages.yml 의
  `workflow_run` 구독 목록엔 「일일 신호 갱신」만 있었다. **실측 2026-09-01 17:16 KST:
  저장소 main 은 15:40, 배포된 사이트는 09:06 — 장중 내내 6시간 묵은 값.**
  즉 시세는 **사람이 push 한 날에만** 갱신됐다. → 구독 목록에 `"price"` 추가.
  **비용 검산**(소유자 질문 「커밋 14회면 문제가 되나」): 공개 저장소라 Actions 분 **무료** ·
  Pages 의 **시간당 10빌드 제한은 커스텀 워크플로에 적용되지 않는다**(공식 문서) ·
  price.json 1KB × 14회/일 (저장소 현재 8MB) · `concurrency: cancel-in-progress` 로 겹치면
  취소. **문제 없음.**
  ⓑ **화면이 시세를 다시 안 읽었다** — `loadPrice()` 가 페이지 열 때 **딱 1회**.
  켜 둔 채로는 새 값이 올라와도 영원히 처음 값을 본다. → `refreshPrice()` +
  `startPriceTicker()`: **한국장 중 5분마다 · 탭 복귀 시 1회**(밤새 켜 둔 탭은 안 돈다).
  ★ **입력칸을 절대 다시 만들지 않는다** — `drawPort()` 는 종목이 바뀔 때만 칸을 재생성하므로
  (포커스 보존), 갱신은 **배지·placeholder 만 제자리 교체**한다. 실측 검증: 타이핑 중
  갱신해도 **포커스·값·캐럿 위치·저장값 전부 유지**. 판정 무접촉(전환은 QQQ 종가만).
  ⓒ **실시간 조사 결론 — 재제안 전에 볼 것.** 배포 화면에서 직접 재봤다:
  브라우저 직접 호출은 **6/6 차단**(네이버 2·야후 2·Stooq·KRX) · 공개 CORS 중계도
  죽었다(allorigins 20초 무응답 · corsproxy.io HTTP 401).
  ★ **v146 의 「iframe 불가」는 위젯 제공자에겐 틀렸다** — TradingView 위젯은 **심으면 뜬다**
  (X-Frame-Options 없음, 418660 을 「MIRAE ASSET TIGER SYNTH-NASDAQ」로 인식).
  막는 것은 기술이 아니라 **데이터 권리**다: 값 자리에 **「이 심볼을 볼 수 있는 곳은 오로지
  TradingView」** — KRX 실시간은 재배포되지 않는다. **결론: 실시간은 중계 서버를 우리가
  두는 길뿐이고, 그건 「볼 곳이 둘」이 되는 대가다.** 소유자 판단은 **보류**(「다른 걸 잃거나
  리스크를 감수할 마음은 없다」). 증권사 API 는 **키가 증권계좌에 묶여 있어** 정적 페이지에
  못 넣는다 — 기각.
  ※ 부수 사실: 네이버의 `nav` 필드는 **전일 공식 NAV 가 아니라 장중 iNAV** 다
  (70초 만에 39,141→39,107 이동 실측). 즉 **괴리 배지는 이미 iNAV 기준**이다.
  ⓓ **[v174] 곁딸린 정정**: 포트폴리오 안내줄이 「현재가 기본값은 자동 수집된 **전일 종가**」라고
  말하고 있었는데, v145 이후 기본값은 **오늘 장중 스냅샷**이다. **보유를 넣기 전에만 보이는
  줄**이라 v145~v173 내내 아무도 못 봤다. 문구와 옛 주석을 실제 동작에 맞췄다(정보 추가 아님).
  ★ 이 항목은 **새 §4 항목을 만들지 않고 여기 붙였다** — v172 에서 정한 규약대로
  (자잘한 변경은 기존 항목에 한 구절로. 노트 지연 관문도 그래야 헐거워지지 않는다).
- **v176 (소유자 결정 — 「5분 방식으로」)**: 시세 수집 **30분 → 5분**, 그리고
  **시세를 main 이력에서 빼냈다.**
  ⓐ **`price-data` 전용 브랜치**: 매번 orphan 브랜치를 만들어 force-push 하므로
  **항상 커밋 1개 · 파일 1개**다(실측 확인). main 의 시세 커밋은 **0건**이 되고 저장소도
  안 커진다. v175 에서 「이력을 지우는 것은 금지(증거가 죽는다)」로 못 박았으므로
  **애초에 안 쌓는 쪽**이 유일한 해법이었다. `data/price.json` 은 `.gitignore` 로 내렸다.
  ⓑ **배포는 브랜치에서 가져온다**(pages.yml). ★ **실패 규약이 핵심**: 못 가져오면
  **아예 안 싣는다** — main 에 옛 사본을 두고 물러서면 v173 에서 잡은 사고
  (6시간 묵은 값을 아무도 모름)가 그대로 재현된다. 파일이 없으면 화면이 배지를 숨기므로
  **실패가 눈에 보인다**(v145 가 스스로 정한 신선도 원칙).
  ⓒ **파수꾼 `price` 모드**: 「안 보이는 것」을 사람이 알아챈다는 보장이 없으므로
  브랜치 스냅샷이 **3영업일** 밀리면 카톡. 검증 — **정상 운영 2주 전수에서 거짓 알림 0** ·
  월요일 휴장(금→화)도 참음 · 수집이 멈추면 3영업일째 발화 후 억제.
  ⓓ **5분이 상한이다**: GitHub 예약 실행의 최소 간격이 5분이다(공식 문서 — 1분은 불가능).
  파이프라인 왕복은 **43초** 실측(수집 15초 + 배포 25초)이라 5분 슬롯이 안 겹친다.
  ⓔ **★ 재제안 방지 — `raw.githubusercontent.com` 직접 읽기는 검토 후 기각.**
  실측하면 **CORS 가 열려 있다**(HTTP 200 · JSON 파싱 성공 · 268ms. 응답 헤더에
  `Access-Control-Allow-Origin` 이 안 **읽히는** 것은 그 헤더가 CORS 안전목록이 아니라서지
  차단이 아니다 — 차단이면 fetch 가 던진다). 배포를 안 거치니 매력적으로 보이지만
  **안 쓴다**: ① `cache-control: max-age=300` 이라 **배포 경로(43초)보다 오히려 느릴 수
  있고**, ② 화면이 읽는 출처가 **둘**로 갈라진다(Pages + raw). 얻는 게 없다.
  ⓕ **미해결로 남긴 것 — 60일 규칙**: 공개 저장소는 **60일간 활동이 없으면 예약 실행이
  자동으로 꺼진다**(공식 문서). 이 시스템은 전부 예약 위에 있고 **파수꾼도 예약이라
  같이 꺼진다** — 즉 「꺼졌다」고 알려줄 것도 같이 죽는다. 봇 커밋이 「활동」으로
  쳐지는지는 **문서에 없다**(추측 금지). 04 §7 파생 질문 대장 후보.
- **v177 (소유자 승인 — v176 ⓕ 60일 문제의 대비책)**: **월 1회 「살아 있음」 알림**
  (`watchdog.py heartbeat`). **이 저장소에서 유일하게 「침묵이 정보」인 알림이다.**
  **왜 예외인가**: 다른 알림은 전부 「이상할 때만」이라 **침묵에 두 가지 뜻**이 있었다 —
  정상이거나, 알림 장치가 죽었거나. 특히 공개 저장소는 **60일 무활동이면 예약 실행이
  통째로 꺼지는데**(공식 문서) 그러면 **파수꾼도 같이 꺼져** 말해 줄 것이 남지 않는다.
  한 달에 한 번 살아 있다고 말해 두면 **그것이 안 오는 것 자체가 신호**가 된다.
  문구가 그 사용법을 직접 가르친다(「다음 달에 안 오면 자동화가 멈춘 것입니다」).
  ★ **상태를 어디에 두느냐가 설계의 핵심**: 「이번 달에 이미 보냈나」를 기억해야 하는데
  새 파일을 만들거나 매일 커밋하면 **v176 에서 없앤 이력 소음이 되살아난다.** 그래서
  주간 점검이 **이미 쓰고 커밋하는** `data/ops_check.json` 에 `heartbeat` 키 하나를
  얹었다 — **새 파일 0 · 새 커밋 0**. 「달」만 비교하므로 **슬롯이 몇 주 건너뛰어도
  그 달 안에 반드시 한 번은 나간다**(발송 누락 내성).
  검증: 같은 달 4회 실행 → 1회만 발송 · 달이 바뀌면 재발송 · 첫 주를 통째로 건너뛴
  달(11-23 에야 첫 실행)에도 발송 · 파일이 없으면 조용히 생략 · **실제 장부 파일 무접촉**.
  ※ 이것이 60일 규칙을 **막지는 못한다** — 꺼지는 것을 **알아채게** 할 뿐이다.
  봇 커밋이 「활동」으로 쳐지는지는 여전히 문서에 없다(v176 ⓕ, 04 §7 후보).
- **v178 (소유자 실보고 — 「백업 카톡에 글자만 온다」)**: 백업 공유가 **파일 없이
  제목만** 가던 것 정정. **Web Share 로 낸 두 번째 조용한 실패다**(v119·v120 에 이어).
  **★ 원인 특정의 근거**: 도착한 본문이 `title` 문자열(`나스닥 신호 백업`)과 **글자 그대로
  같았다.** 파일이 붙었는데 이름만 깨진 것이라면 파일이 보였을 것이다 — 즉
  **텍스트 공유로 처리되고 파일이 버려진 것**이다. MDN 은 title 을 「타깃이 무시할 수
  있다」고만 하는데, 카톡은 무시가 아니라 **그것만** 취했다.
  → **`navigator.share({files:[f]})` — title 을 넣지 않는다.** 인텐트에 텍스트가 없으면
  타깃은 파일을 쓰거나 **눈에 보이게 실패**한다. 조용히 글자만 가는 경로가 사라진다.
  **의심했다가 내려놓은 것**: v138 이 파일명을 한국어로 바꿨으니(`나스닥신호_백업_*.txt`)
  그게 범인일 수 있었다. 그러나 **증상이 안 맞는다** — 이름이 깨졌다면 이름이 이상한
  파일이 왔지 텍스트만 오지 않는다. 파일명은 그대로 뒀다(v138 한국어 규약 유지).
  겸사겸사 둘: ⓐ 보낼 **그 파일로** canShare 재확인(MDN 권고 — 종전 검사는 빈 더미
  파일이라 「환경이 파일 공유를 지원하나」만 봤다) · ⓑ 공유 후 **한 줄 확인 안내**
  (`shareNote()`) — `navigator.share()` 성공은 **시트가 닫혔다**는 뜻이지 상대 앱이 받았다는
  뜻이 아닌데 v105 리마인더는 그것을 백업으로 친다. 두 번의 사고가 그 증거다.
  ⚠ **다음 세션 주의**: 카톡 도착 여부는 **이 저장소에서 자동 검증이 불가능하다**
  (브라우저 밖 · 안드로이드 인텐트). 공유 코드를 고치면 **반드시 소유자에게 실제 전송을
  부탁하라** — 「구현했다」와 「도착한다」는 여기서 세 번 갈렸다.
  검증(브라우저 실측, 공유 API 를 가짜로 대체해 **실제로 넘기는 인자**를 잡았다):
  넘긴 키 `['files']` 뿐(title·text 없음) · 파일 `나스닥신호_백업_*.txt` text/plain 345B
  내용은 백업 JSON · 실파일 거부 시 공유 시도 0 + 안내 · 취소는 조용 · 진짜 실패는 알림.
- **v179 (소유자 실보고 — 「자동 현재가와 평단 기준 손익이 안 맞는다」)**: **같은 줄에
  현재가가 둘이었다.** 화면은 39,390(시세 스냅샷)을 보여주는데 총평가는 39,200
  (nav_history)으로 계산했다 — **보유 주수 × 190원**만큼 총평가가 어긋났고, 손익 표시가 몇백 원일 때
  실제로는 몇만 원 단위였다(실제 주수는 공개 저장소라 적지 않는다 — v197).
  **원인**: **v145 가 입력칸 placeholder 만 시세 스냅샷으로 바꾸고 `portCompute` 는
  nav_history 에 남겨 뒀다.** 부분만 고치고 같은 파일 나머지를 안 본 것 —
  **§-1 ③ 이 경고한 바로 그 유형**이다(그때도 v133→v134 로 같은 실수를 했다).
  **고침**: 자동 현재가를 **단일 진입점 `curPx(code)`** 로 모았다. placeholder ·
  계산 · v173 5분 갱신이 **전부 같은 함수**를 부르므로 구조적으로 다시 갈라질 수 없다.
  ★ 우선순위는 **고정 순서가 아니라 「더 최신인 쪽」**이다 — 시세 수집이 죽으면
  nav_history 가 더 새 값일 수 있다(실측 확인: 시세 없음 → nav 로 물러섬 ·
  시세가 더 오래되면 nav 가 이김 · 정상이면 시세).
  ※ **알아둘 사실**: `nav_history` 의 `close` 열은 **당일분에 한해 종가가 아니다.**
  `nav_collect.py` 는 daily-signal(05:17~09:17 KST)에 얹혀 돌아 **개장 직후 값**이 찍힌다
  (2026-09-01 실측 39,200 · 거래량 42,381 vs 전일 180,055). 장 마감 뒤에는
  `price.json` 쪽이 그날의 진짜 종가다. 이 열을 「종가」로 인용하기 전에 이 문단을 볼 것.
- **외부 AI 조언 검증 (2026-09-01, 소유자 요청 — 「제미나이가 알려준 60일 해법」)**:
  **★ 추천된 액션은 쓸 수 없다.** `gautamkrishnar/keepalive-workflow` 는 **GitHub 이
  차단한 저장소**다(API 403 · `"reason":"tos"` · 2025-04-21부터). `uses:` 로 부르면 실패한다.
  나머지 조언도 대조: ⓐ **60일 규칙 자체는 맞다**(공식 문서 확인, v176 ⓕ). ⓑ 「수동
  커밋이라야 한다」는 **출처가 없다** — 문서는 「repository activity」라고만 하고 봇 커밋이
  포함되는지 말하지 않는다. 추측을 사실로 옮기지 마라. ⓒ **「Read and write permissions
  로 바꿔라」는 이 저장소에 불필요하다** — 이미 `write` 이고(API 확인), 워크플로 4개가
  **각자 `permissions: contents: write` 를 선언**한다(저장소 기본값에 기대지 않는 쪽이 낫다).
  ⓓ **논리 구멍**: 그 액션도 결국 `github-actions[bot]` 으로 커밋한다. 봇 커밋이 활동으로
  안 쳐진다면 **그 액션도 같은 이유로 무효**다. 즉 조언이 스스로를 반박한다.
  **판정: 도입하지 않는다.** 이 저장소는 이미 봇이 매일 커밋하므로 ⓑ가 참이면 안전하고,
  거짓이면 **v177 월간 생존 알림이 안 오는 것으로 한 달 안에 드러나고** 복구는
  커밋 한 번(또는 Actions 탭의 재활성화 버튼)이다. **제3자 액션·PAT 를 새로 들이지 않는다.**
- **v180 (소유자 지시 — 「검증됐는지를 펼쳐야만 아는 건 아니다」)**: 「버전 · 동결」
  접힘의 **제목줄에 상태 두 개를 얹었다** — 자동 검증 배지(GitHub SVG)와 자동 점검 알약
  (이상없음 / N건 / 낡음). **접혀 있어도 보인다**(412px 실측: 오른쪽 끝 225px · 드래그 0).
  ★ **소유자가 준 분류 기준을 그대로 규칙으로 삼는다**: **기록만 하는 것은 접혀도 되지만,
  「에러가 났나·검증이 됐나」는 접힘 밖에 있어야 한다.** v108 이 세로를 줄이려고 이 칸을
  통째로 접을 때 **그 둘을 같이 넣은 것**이 문제였다 — 매일 볼 값어치가 있는 건 그 둘뿐인데
  매일 접혀 있었다. 상세(버전·동결일·OOS 장부·함께한 날·평균 간격)는 **그대로 접어 둔다.**
  ⚠ 앞으로 화면을 접을 때 이 기준으로 나눠라 — **상태는 밖, 기록은 안.**
- **v181 (소유자 지시 3건)**: ⓐ **판정이 「QLD」 → 「공격」**. 방어 쪽은 「방어」인데
  공격만 상품명이었다 — 같은 자리에 **상태와 상품명이 섞여** 있었다. `stateLabel()` 하나를
  고치면 표(수동 입력 이력·성과)까지 같이 바뀌는데 **그쪽도 상태 열이라 오히려 맞다**(의도).
  ★ 마스트헤드는 `'공격 · ' + stateLabel()` 이라 그대로 두면 **「공격 · 공격」**이 된다 —
  접두어를 뺐다(지우기 전 사용처 grep, §-1 ①).
  ⓑ **판정~자산 밀착**: `.verdict` margin-bottom 4→0 · `.product` 20→14 · line-height 1.5→1.45.
  카드 728→714px.
  ⓒ **전환일 리허설(v125) 통째 삭제** — 소유자: 「충분히 해봤고 더 쓸 일 없다」.
  CSS·배너·버튼·`REHEARSAL` 전역·`rehClear`/`startRehearsal`/`endRehearsal` + **다른 기능에
  얹혀 있던 분기 3곳**(`saveSeen`·`chkKey`·`tlAdd`)과 `renderAuto`/`render` 첫 줄 `rehClear`
  2곳까지 전부. 2,648자 감소 · 남은 흔적 0(v128 주석에는 「v181 삭제」로 표기).
  **재도입 제안 금지.**
  ※ **빈 공간 활용은 보류**(소유자 질문 「내 단계는 어디?를 넣으면?」에 대한 답) —
  412px 실측에서 판정 오른쪽 여백은 **가로 166px 짜리 띠**다. 입력칸이 있는 goalPanel 은
  그 폭에 못 들어가 **세로로 쌓이고, 그러면 목적(세로 압축)과 반대가 된다.**
  그 자리에 맞는 것은 **한 줄짜리 상태값**뿐이다(공격엔 이미 시세가 있고, **방어일 때는
  비어 있으므로 v117 재조정 D-day 가 후보**). 소유자 결정 대기.
- **v182 (소유자 선택 ⓐ)**: **방어일 때 판정 옆 빈자리에 30일 재조정 D-day.**
  공격일 땐 그 자리에 시세가 있는데(v149) **방어일 땐 비어 있었고**, 정작 방어 상태에서
  소유자가 하는 유일한 일이 재조정인데 그 카운터는 **스크롤해야** 보였다.
  v180 의 「상태는 밖, 기록은 안」과 같은 방향 — D-day 는 기록이 아니라 상태다.
  ★ **계산을 새로 만들지 않았다**: v117 규약(`getDefenseEntryDate` + 진입일부터 30일 +
  확인일 2일 유예)을 **같은 함수·같은 식**으로 부른다. **한 화면에 재조정일이 둘이 되면
  안 된다** — v179 가 그 사고였다(placeholder 와 계산이 갈림). 실측 대조: 옆칸 D-8 =
  아래 카운터 D-8. 검증: 22일→D-8 · 30·31일→재조정일(유예) · 33일→D-27 · 60일→재조정일 ·
  진입일 미확정→숨김 · 공격 복귀→숨김. 412px 드래그 0.
  ※ **공격 카드의 남은 여지(소유자 질문 「공격 중엔 더 줄일 게 없나」) — 있다, 실측**:
  카드 714px 중 **자산 영역 236px**(그 중 **「전환되면 살 것」 3칩이 128px**)과
  **수치 6칸 185px** 가 둘째·첫째다. 「전환되면 살 것」은 **오늘 할 일이 아니고**,
  수치 6칸 중 **QQQ 종가·252일 고점·다음 전환선·전환 기준가 4개는 계산의 재료**이며
  같은 정보를 **낙폭 게이지(50px)가 이미 그림으로** 보여준다. 둘을 접으면 **약 250px(35%)**.
  ⚠ 다만 v113 이 「정보를 숨기는 방식은 답이 아니다」로 루틴/전체 토글을 폐기했다 —
  **재도입이 아니라 v180 기준(상태는 밖, 기록·계획은 안)으로 나누는 것**이면 성격이 다르다.
  소유자 결정 대기.
- **v183 (소유자 지시 — 「일단 바꿔보고 써 본 뒤 정하겠다」)**: 신호 카드 세로 압축 3건.
  기준은 v180 하나 — **상태는 밖, 기록·계획은 안.** 공격 **714 → 552px(−23%)** ·
  방어는 눈금자만큼 −180px.
  ⓐ **낙폭 눈금자 삭제** — 소유자 질문 「이건 어디 숨어 있고 꼭 필요한가」에서 시작.
  **v108 이 이미 「게이지(prox)가 같은 정보를 한 줄로 준다」고 적어놓고** 평시에만 접어
  뒀던 것이다. 게이지는 거리·문턱·전환 기준가·막대를 다 주는데 눈금자는 **위치만** 준다
  (부분집합). 게다가 눈금자가 남아 있던 상태(전환일·근접·방어)가 **하필 화면이 가장 긴
  상태**였다. `.signal` 이 한 열이 됐다. **재도입 제안 금지.**
  ⓑ **「전환되면 살 것」 접기**(−128px) — 예고지 오늘 할 일이 아니다.
  ⓒ **수치 6칸 중 재료 4개 접기**(「계산 재료 보기」) — 상태는 「현재 낙폭」·「여유」 둘이고
  QQQ 종가·252일 고점·다음 전환선·전환 기준가는 그 값을 만든 재료다.
  ★ **v113(루틴/전체 토글 폐기)의 재도입이 아니다**: 그건 전부를 한꺼번에 숨겼고,
  이건 상태를 남기고 근거·예고만 접는다. 되돌리기는 이 커밋 하나.
  ⚠ **작업 중 실제로 낸 사고 — 지울 때 경계까지 먹었다**: 눈금자 마크업을 정규식으로
  지우면서 **`.signal` 의 닫는 `</div>` 가 고아로 남아** `.signal` 이 빈 껍데기가 되고
  `.state` 가 밖으로 나갔다. **화면은 멀쩡해 보였다**(카드 테두리만 사라짐) — 스크린샷으로는
  못 잡는다. DOM 부모사슬을 재서 잡았다. **마크업을 지웠으면 「그 요소가 아직 부모 안에
  있는가」를 재라** — §-1 ① 의 마크업판.
- **v184 (소유자 판정 — 「이상해, 되돌려줘」)**: **v183 의 접기 ⓑ·ⓒ 를 되돌렸다.**
  눈금자 삭제(ⓐ)는 **유지**한다 — 소유자가 명시적으로 「눈금자는 지운 게 맞다」.
  ★ **되돌린 것이 실패가 아니라 절차다**: 소유자가 「일단 바꿔보고 써 본 뒤 정하겠다」고
  했고 써 본 결과가 「이상하다」였다. **한 커밋으로 되돌아가게 만들어 둔 것이 값을 했다.**
  「전환되면 살 것」 접기와 「계산 재료 보기」는 **재제안 금지.**
  ⓓ **★ 그 과정에서 소유자가 진짜 버그를 하나 잡았다** — 「여유 12.2%p 가 −3.83% 와
  라인이 안 맞는다」. 원인: **특수 마이너스 −(U+2212)가 Pretendard 에 없어 대체 글꼴로
  그려지고, 그 글꼴의 자연 줄상자가 5px 크다**(실측 `−3.83%` 24px vs `12.2%p` 19px vs
  보통 하이픈 `-3.83%` 19px). `.fact .v` 가 `line-height:normal` 이라 **글자에 따라 칸 높이가
  달라졌다.** `line-height:1.2` 로 고정해 해소(전 칸 18px 통일, 같은 행 값 y 일치).
  ※ **이 버그는 v183 이 만든 게 아니다** — 원래 있었는데 6칸 배치에서 「현재 낙폭」과
  「여유」가 **다른 행**이라 안 보였을 뿐이다. 되돌린 뒤에도 「현재 낙폭 | QQQ 종가」가
  같은 행이라 고쳐야 했다. **숫자를 화면에 쓸 때 `−`(U+2212)를 쓰는 곳은 줄상자를 고정하라.**
- **v185 (소유자 질문 「포지션 계산기가 내게 필요할까」 → 실측 후 삭제)**:
  **판단 근거는 인상이 아니라 실측이다** — 포트폴리오에 **현금 300만원**을 넣으니
  「**매수 76주 · 약 299.4만원**」이 나왔다(검산 76주 일치 · 잔여 6,360원까지).
  즉 **계산기가 하던 일을 포트폴리오가 보유까지 반영해 더 정확히** 한다.
  계산기에만 있던 나머지 셋도 소유자에게는 죽은 기능이었다 — ① **헤지6/4 계산**:
  채택안이 아니라 실행할 일이 없다 · ② **방어 상태 미리보기**: 방어 3종 비중은 신호
  카드에 **항상** 떠 있고 전환일엔 화면이 지시한다 · ③ **보유 0에서 신규 진입**: 이미 지났다.
  4,038자 감소. **재도입 제안 금지.**
  ★ **지울 때 잡은 함정 2개**(§-1 ①: 지우기 전 사용처 grep):
  ⓐ **환율 민감도(v158)가 계산기 패널 안에 살고 있었다** — 소유자가 직접 요청한 기능이라
  같이 죽이면 안 됐다. `PORT_ATK`(포트폴리오의 공격 평가액)를 쓰므로 **포트폴리오가
  제자리**다 — 거기로 옮겼고 이사 후 동작 실측(1,400→1,500 → +14.3% · 142.9만원).
  ⓑ **verify_all 이 계산기를 검사하고 있었다**(실제로 FAIL 이 났다). 검사를 **지우지 않고
  책임을 물려받은 쪽으로 옮겼다** — 「오늘의 행동(주수·금액 지시)」. **관문 수를 줄이지 않는다.**
- **v186 (소유자 설계도 — 링크 방식 백업/복원, 파일 .txt 공유는 그대로 병행)**:
  `#backup=<base64url>` **fragment** 로 백업을 싣는다. **query(`?`)가 아닌 이유**는
  설계도 그대로 — fragment 는 서버로 안 가서 **Pages 로그·카톡 미리보기 봇에 자산
  데이터가 남지 않는다.** 받는 쪽은 로드 끝에 `consumeBackupLink()` 한 번.
  ★ **hash 는 처리 전에 먼저 지운다**(`history.replaceState`) — 안 지우면 ① 새로고침마다
  확인창 ② 히스토리·최근 탭에 자산 URL 잔류 ③ 실수로 재전달 시 옛 데이터 동반.
  확인창은 **「⚠ 링크로 받은 백업입니다」**로 출처를 밝힌다.
  ⚠ **★ 설계도 전제 하나가 틀려서 보강했다 — 이게 이번의 핵심이다.**
  「형식 검증·확인창·undo 가 전부 있으니 새로 안 만든다」고 했는데, `applyBackup` 의 검증은
  **껍데기만** 본다(`v===1` · port 가 객체 · trades 가 배열). **값은 안 본다.**
  그 값들은 `innerHTML` 템플릿에 그대로 들어간다(`drawTlogList` 의 `${t.d}` ·
  `drawPort` 의 `value="${p.qty}"`). **파일 복원은 자기 파일이라 넘어갔지만 링크는
  남이 보낸 URL 이다** — 조작된 링크로 스크립트가 돌면 **같은 오리진의 localStorage
  (장부 전체)가 털린다.** → **`sanitizeBackup()` 신설**: 코드 `^\d{6}$` · 날짜
  `^\d{4}-\d{2}-\d{2}$` · side 는 buy/sell · 수량류는 `[0-9.,]` 만 · 벗어나면 그 항목을
  버린다. **`applyBackup` 안에 넣었으므로 파일 복원도 같이 안전해졌다.**
  실측: `<img src=x onerror=…>` 날짜 → 그 체결 버려짐 · `" onfocus="alert(1)` 수량 → `"1"` ·
  `BAD<script>` 종목코드 → 통째로 버려짐.
  설계도 대비 조정 2개(둘 다 근거 있음): **base64url**(메신저 자동 링크가 `=` 를 자르면
  복원이 깨진다) · **압축 JSON**(`backupText()` 는 사람이 읽으라고 들여쓴다 — 링크에선 낭비.
  같은 원천을 재직렬화만). 길이 상한 6,000자 — **체결 50건 실측 5,543자**.
  검증 6종: 50건 링크 왕복 무결 · hash 자동 삭제 · 재실행 2회 무반응 · 변조 hash 조용히 거부 ·
  살균 4종 · 공유 인자가 **`{url}` 하나뿐**(v178 교훈). guide FAQ 「폰↔PC 이동」에도 등재.
  ※ **링크는 한 번 써도 계속 유효하다**(실측: 같은 링크 재방문 → 다시 복원됨).
  `replaceState` 가 지우는 것은 **열어놓은 탭의 주소창**뿐이고 메신저에 남은 문자열은
  손대지 않는다 — 그게 맞다(「나에게 보내기」에 두면 언제든 다시 쓰는 백업이 된다).
  **소유자 확인 사항**: base64 는 암호화가 아니라 인코딩이라 **그 링크를 볼 수 있는
  사람은 보유·평단을 그대로 읽는다.** fragment 가 막는 것은 서버 로그·미리보기 봇이지
  대화 상대가 아니다. → **혼자 보는 방에만.**
  ★ **만료 경고(「N일 전 링크입니다」) 는 소유자가 「놔둬」로 판정(2026-09-01) — 재제안 금지.**
- **★ 인용 오류 적발 — 「현행이 자기 표본 3위」 (2026-09-01, 소유자 질문에서 · 04 §5-20)**:
  소유자 질문 「54년을 통으로 해서 −16/−16 이 최우선이라고 되어 있는 게 맞나」에서 시작해
  **원문을 열어 보니 요약이 틀려 있었다.** 01 §4 의 그 행은 창을 「거치식·54년」이라 적었으나
  실제 격자는 **2000-2026**(v18 이 쓴 표본)이고, **「현행이 3위」가 아니라 현행이 1위**다 —
  **3위는 v18 이 고른 −16/−11**(구 전략 A). v56 의 원문장은 「**v18 이** 자기 표본의 1등을
  골랐나 → 3위를 골랐다」로 **주어가 v18 의 선택**인데, 요약되며 **주어가 현행으로 바뀌었다.**
  ★ **하필 전략에 유리한 쪽으로 틀렸다** — 「1등을 고른 게 아니다(과적합 지문 부재)」는
  안심 문장인데 실제로는 현행이 그 표본의 1등이고 **v56 §6 은 그것을 「남는 붉은 깃발」로
  적어 두었다.** 안심 쪽 요약이 그대로 살아남아 `01`·`04`(2곳)·`FINAL_AUDIT.md`·
  `MEASUREMENT_AUDIT.md`·`b_adversarial.py` **6곳**으로 번졌다(전부 정정).
  `docs/history/전략_v67_독립감사.md` §C-2 는 **§2 절대 수정 금지라 그대로 두고**,
  대신 `retired_numbers.json` 에 등재해 **현행 문서로 되살아나는 것을 I9 관문이 막는다**
  (v148 원칙: 재발 방지는 자각이 아니라 관문으로). ★ **등재 직후 실제로 FAIL 이 뜨는 것을
  확인**했다 — 통과만 보고 넘어가면 검사가 아니다(§-1 ⑤).
  ※ **전략 변경 0건.** 통짜 1위는 **비중첩 창 1개**라 근거가 될 수 없고(210개 중 하나는
  반드시 1등이다), 실제로 **시작을 4년 옮기면 9위·끝을 1990 으로 자르면 56위**다.
  근거는 통짜가 아니라 **겹치지 않는 블록**(미니맥스 5~7위/210, 어느 블록에서도 1등 아님)·
  **워크포워드 0/7**·**v18 미관측 28년 상위 79%** 쪽이다. 재현 `research/thresh_window.py`.
  ⚠ **다음 세션 주의**: 이 오류는 **요약하며 주어가 바뀐 유형**이라 **원문을 열어야만** 보인다.
  04 §7 대장에 「01~04 의 다른 요약 행들도 같은 병이 있지 않은가」를 미결로 올렸다.
- **★ 과적합 검증 전수 점검 — 빈칸 1개 발견·충전 (2026-09-01, 04 §5-22)**: 소유자 질문
  「OOS·WFA·DSR·PBO 를 다 해본 게 맞나」에 §-1 ② 대로 파일을 하나씩 열었더니
  **`audit_pbo.py` 의 후보 우주(47개)에 문턱 격자가 아예 없었다.** v70 의 PBO 0.437 은
  **T4 파라미터**에 잰 값이다 — 즉 **「−16 을 153칸 중에서 고른 행위」에는 PBO 를 한 번도
  안 걸었다.** §5-13 정정2 가 「PBO 가 답하지 않았다」고 적어만 두고 **답하게 만들지
  않았던 것**이다. `research/pbo_thresh.py` 로 충전(CSCV S=8·70분할, audit_pbo 와 같은
  구현·dsr_b 와 같은 격자). **PBO 0.400~0.829** — 「IS 1등 고르기는 동전던지기 이하」라
  **「−16 을 재조정하지 말라」를 처음으로 그 대상에 직접 잰 근거로** 지지한다.
  변별력 확인: 무작위 신호 60개를 섞으면 0.829 → **0.914**, 잡음이 IS 1등을 26/70 분할에서
  가져간다(§-1 ⑤ 통과만 보지 않기).
  ⓐ 반증: 현행의 OOS 중앙 백분위 **91% · 1위/17** — 격자 가운데라서가 아니다(−12~−9 는
  26~59%). **단 최악 분할은 −15/−15(44%)가 −16(15%)보다 낫다** — 중앙값 1위가 최악 1위는
  아니라는 것도 적어 두었다.
  ★ **가장 중요한 한정**: **CSCV 의 「OOS」는 진짜 OOS 가 아니다** — 같은 54년을 갈라 쓴
  것이고 −16 은 그 54년을 다 본 뒤 골라졌다. 진짜 OOS 는 동결 이후뿐이다(v56 §0 과 같은 말).
  ✅ **그 빈칸도 같은 날 메웠다 (소유자 승인 → `research/wfa_thresh.py`, 04 §5-13 정정3)**:
  §5-13 정정2 의 「정정 WFA」 표가 **결과만 있고 코드가 없었다.** 재현하니 **12/12 일치** —
  단 **문서에 안 적힌 규약이 둘** 있었다. ① **엔진**: 1972~ 54년·방어 40/40/20 이라야 맞고
  `b_adversarial` 규약(1985~·방어=현금)으로는 **12개 중 1개**만 맞는다. ② **「고정 승 73%」의
  뜻**: 적응형이 −16 을 고른 걸음은 **무승부**인데 그걸 고정 쪽에 센 값이다(=「지지 않은 비율」).
  **엄격히 이긴 비율은 33~40%** — 「이겼다」로 읽으면 과장이다.
  ★ **그 과정에서 §-1 ⑧ 위반 1건 적발**: 정정2 는 결함판(−0.21)과 정정판(−0.16)을 비교하며
  차이를 **창 수정** 탓으로 돌렸는데, **두 값이 서로 다른 엔진에서 나왔다.** 2×2 를 채우면
  같은 엔진에서 **창만** 바꿀 때 선택 중앙은 **−0.16 → −0.16 으로 안 움직인다.**
  **결론은 무너지지 않고 강해진다** — ext 엔진에선 **결함 설계로 골라도 −16** 이고
  훈련 길이 6~20년 **8/8 이 −0.16**. 바뀌는 것은 「창 수정 덕분」이라는 **서술**뿐이다.
  ⚠ **교훈**: 재현 스크립트가 없는 표는 **규약이 소실된다.** 이번엔 12/12 가 맞았지만
  「고정 승」의 뜻이 달랐고, 인과 서술이 틀려 있었다. **표를 실을 땐 엔진과 정의를 같이 적어라.**
- **v187 (소유자 질문 「공유용 아티팩트 UI 중 설명서·노트에 쓸 만한 건 없나」 → 실제 공백 적발)**:
  **설명서 ⑫ 「표에 나오는 말 — 30초 정리」 신설** + 성과표 각주에 `guide.html#terms` 링크.
  ★ **처음엔 「가져올 게 거의 없다」고 답했는데 소유자가 되물어서 다시 봤고, 그게 맞았다** —
  신호 화면 기준(운영용·압축 우선)을 설명서에까지 그대로 적용한 것이 내 오류다(§-1 ⑦ 계열).
  **실측한 공백**: 성과 비교표는 CAGR·MDD·Calmar 열을 쓰는데 **guide 에 CAGR·MDD 는 0회 등장**이고
  뜻을 적어둔 곳이 저장소 어디에도 없다. v130 이 신호 화면의 「이름 읽는 법」 문단을 지웠는데
  (화면 압축) **설명서로 옮기지 않고 그냥 사라진 것**이다 — v133(execPanel → §③ 이식)이
  선례라 그대로 이식했다. 겸사겸사 **설명서가 Calmar 를 「폭락 대비 효율」·「위험 대비 효율」
  두 이름으로 부르던 것**도 용어표에서 같은 값이라고 밝혔다.
  ⛔ **v130 이 금지한 것은 「등급·심사」(paintMetKey) 이지 「정의」가 아니다** — 여기엔 정의와
  방향(높을수록/낮을수록)만 두고 **판정은 넣지 않았다.** 재도입 금지선을 넘지 않는다.
  ★ 아티팩트에서 실제로 가져온 것은 **형식 2개뿐**: 용어표의 방향 배지, `text-wrap:balance`.
  **안 가져온 것과 이유**: 초록/빨강 승패색(⛔ 우리 색은 규약 — 매수 빨강·매도 파랑 MTS 관행) ·
  Google Fonts 2종(외부 의존 0 유지, v141 비상 수동 판정의 전제) · 슬라이더 탐색 UI
  (규칙이 동결이라 고를 게 없다 — 탐색형 UI 는 「아직 안 정해진 것」처럼 보이게 만든다) ·
  범위 막대/오차막대(소유자가 2026-08-31 「guide 확률표는 그대로」로 이미 판정).
  ※ 접이 박스 「펼치기 ▾」는 **원래 우리 것**(v128)이고 아티팩트가 가져간 쪽이다.
- **★ B 판정 규약 등록 (2026-09-02, 소유자 「Fable 5.1 로 분석·개선점 제시」→「일단 등록 해봐」, 결합·대응 위임)**:
  규칙 공간은 04 §1~§5-22 로 소진돼 **재탐색하지 않았다.** 대신 증거 체계의 실제 공백 하나 —
  **T4 그림자엔 v80 §6 부속서가 있는데 돈이 실린 B 에는 OOS 판정 방법이 없었다**(02·04·HANDOFF·v56·v57
  전수 grep). 규약 없이 장부만 쌓이면 판정은 사후 재량이 된다. **규칙 무변경** — 동결 이전 데이터로
  기저율만 재서(`research/oos_protocol_b.py`: 독립 사건 21건=2.6년/건 · 재난(H 창 MDD ≤ −50%) 지급
  **7/7** · 프리미엄 P05 −33% · 3년 롤링 P05 −31.5%) 02 §5-1 + `data/oos_protocol_b.json`(지문)으로
  등록했고 `verify_all` **I13** 이 지문을 매 push 검사한다(사후 수정은 실수로 못 한다). 평가는 `--oos`
  (사건 0건이면 「판정 불가 — 정상」). 대응은 **재검토 개시까지** — 자동 변경 없음 · 문턱 재조정 근거로 못 쓴다.
  ⚠ **룩백 200 그림자는 비권고**(판정 재료 6.8년/건 · 3건에 20년 — 04 §5-23 D). 재제안 전에 볼 것.
  ⚠ **「내가 못 움직일 때」 규칙은 저장소에 없다** — 부재 비용 표는 04 §5-23 E(2주 부재: 미리 방어 −1.1%
  vs 신호 확률 3.8%·못 팔면 최악 −23%). 위치(설명서/신호 카드)는 소유자 결정 대기.
  ⚠ 평가기를 파수꾼 `check` 에 얹는 것은 `deploy/*` 수정이라 **소유자 승인 필요** — 제안만 해 둔 상태.
- **★ 자유 설계 실험 3안 전부 실패 (2026-09-02, 소유자 「관문 배제·이전 룰에서 자유롭게·검증은 전부」 · 04 §5-24)**:
  무덤에 없는 구멍 3개를 겨눴다 — **장중 재난 스탑**(NDX 일중 OHLC 1985~ · B 의 하루짜리 급락 무보험) ·
  **방어 인버스 슬리브** · **SOX 2배 엔진**. 배터리(10y p05·4블록·홀드아웃·CSCV PBO·집중도)와 예측을 **결과 전에
  등록**하고 돌려 **3/3 실패**(최종/R 0.71× · 0.997× · 0.005×). 스탑은 1987 창에서 +24~37% 로 설계한 자리에서
  정확히 작동하지만 톱니가 그 값을 되돌린다 — **구멍은 실재하고, 그 방법으로 메우면 평시 비용이 더 크다.**
  `research/free_design.py` · **재제안 금지 3종.** ⚠ 부수 적발: 「방어 중 바스켓 연 +0.08%」(04 §5-14 C)는
  **신호일** 기준 — **보유일(pos=w.shift(1)) 기준은 +7.08%**. 선견 한 칸(HANDOFF §2 「체결 지연 한 칸」)의 재림 —
  결론은 안 바뀌나 **인용 시 기준을 붙여라.** 실험용 OHLC 캐시 2개는 `data/hist/`(월간 갱신 파이프라인 밖).
  ★ **소유자 질문 「그럼 −16/−16 을 하라는 거야?」에 대한 답: 그렇다.** 규칙 공간은 소진됐고 남은 구멍 3개도
  메우는 값이 더 비쌌다 — 매매는 아무것도 바뀌지 않는다. 바뀐 것은 「규칙이 고장 나면 알게 되는 장치」뿐이다.
- **v188 (소유자 「둘 다 승인」)**: **B 판정 규약 평가를 파수꾼에 얹었다.** `deploy/watchdog.py check` 가
  `research/oos_protocol_b.py --oos` 를 같이 돌려 요약만 `data/ops_check.json` 의 `protocol_b` 키에 얹는다
  (v177 heartbeat 와 같은 자리 — 새 파일·새 커밋 0). **판정은 평가기가, 파수꾼은 읽기만.** 주의·역사 밖·
  지문 불일치·평가 실패·기저율 표류는 `todo` 에 한 줄 추가 → 기존 배너·알약·카톡 경로가 그대로 처리한다
  (알림은 **나빠졌을 때만** — check 모드 규약에 verdict 등급 상승·표류 발생을 「악화」로 추가). 화면은
  「버전·동결」 접힘의 자동 점검 줄에 「판정 규약 사건 N건 — 이상 없음」을 덧붙인다(verdict 는 고정 표로만
  그린다 — 자유 문자열을 innerHTML 에 넣지 않는다). 평가기가 죽어도 점검은 산다(fail-open — 'error' 로 보이게).
  ★ `import re` 추가 — **실제 실행으로 확인**(v148 「검사를 추가했다 ≠ 검사가 돈다」).
- **v189 (소유자 「둘 다 승인」)**: **설명서 §③ 「자리를 비울 때 — 내가 못 움직이면」**(`#absence`). 04 §5-23 E 표
  (5/10/20/40 거래일: 미리 방어 −0.7/−1.1/−2.0/−3.6% · 신호 확률 2.3/3.8/6.6/11.3% · 못 팔면 최악 −24/−23/−29/−54%)
  + 규칙 넉 줄(2주 이내 그대로 · 대신 팔 사람 · 한 달+ 무대리인이면 방어 대기 · 「근접」이면 떠나기 전 정리). **→ v194 에서 대리인 삭제 — 남는 규칙 세 줄: 한 달 이내 그대로 · 한 달+ 방어 대기 · 근접이면 정리.**
  **신호 카드엔 안 넣었다** — 상태가 아니라 계획이다(v180 기준). 새 문장은 전부 04 §5-23 E 실측에서 나왔다.
- **★ 룩백 200 검증 (2026-09-02, 소유자 「어떤 룰보다 최우선 — 왜 200 이어야 하는지 근거까지」 · 04 §5-25)**:
  §5-14 D 의 룩백 재탐색 금지를 **이 질문에 한해** 풀고, 배터리 10종과 판정문을 **결과 보기 전에 등록**해
  돌렸다(`research/lookback200.py`, 엔진 검산 통과 · 6초). **결론: 유지 — 200 이어야 할 사전적 근거는 없다.**
  표본 내 200 이 **1.567배** 나은 것은 사실이고(격자 78칸 표 **15/15 재현**) 20년 창 승률 100% 도 맞지만
  (비중첩 1.8개), 그 격차의 **전부가 긴 약세장 끝의 재진입 4건**이다 — 1975·2003·2009·2023, 고점이 200일 넘게
  묵으면 200일 창에선 고점이 먼저 빠져 재진입선(고점×0.84)이 **22~43일 먼저** 내려온다(상위 2건이 격차의 81%).
  같은 기전의 반대면이 1973·2008-09 의 손실이라 갈린 20구간은 **9승 11패**(p=0.82). 저장소 잣대로는
  Calmar **+5.4%**(관문① 10.2% = 위기 1개 제거 폭 미달) · 갈린 독립 사건 **5/8**(p=0.73) · 격자 WFA 적응형이
  고정 252 에 **24/24 규약 전패** · 타 시장 **0/4**(S&P 300 · NYSE 40 · 니케이 760 · 러셀 50 — 200/252 전부 <1) ·
  **2023 이전엔 앵커 IS 1등이 220** 이었고 200 이 220 을 이기는 것은 **2009-07 8일 + 2023-01 20일** 두 구간뿐.
  200 을 편드는 유일한 검사는 CSCV(PBO 0.17 · OOS 백분위 중앙 98% vs 85%) — 4건이 시대에 흩어져 있어서다.
  ★ **적발**: §5-14 D 표는 「재현 what_we_know.py」라 적혀 있으나 **그 파일에 룩백 코드가 0줄** — §5-13 정정2 와
  같은 병(재현 스크립트 없는 표). 이번에 재현 가능해졌다. WFA 행은 규약 미기재라 정확히는 재현 안 됨(결론 동일).
  ★ 「200 도 관습값」이다(200일 이동평균) — 관습값끼리의 경쟁이고 어느 쪽도 도출된 값이 아니다.
  **재개 조건(기계적)**: 동결 이후 두 규칙이 갈린 독립 사건이 쌓일 때 — §5-23 D 실측 6.8년에 1건.
- **v190 (소유자 「각각 정시에서 5분 안에 반영해 줄 순 없어?」 — 2026-09-02)**: **미국 종가는 마감 뒤 5분 안 ·
  한국 시세는 09:00:20 부터 진짜 5분마다.** 둘 다 원인은 같다 — **GitHub 예약 실행은 미루고 버린다.**
  실측(gh run list): daily-signal 05:17 슬롯이 05:20(09-01)·05:25(09-02)에 떴고(지연 4~9분, 09:17 슬롯은 18분),
  price.yml 의 `*/5` 는 **84슬롯 중 17개**만 떴다(09-01: 간격 중앙 31.6분·최장 108분 · 첫 09:06 / 09-02 첫 09:22).
  ★ **v176 의 「5분마다」는 한 번도 실현된 적이 없다** — 43초 왕복만 재고 실현 주기는 안 쟀다(§-1 ② 유형).
  해법은 「슬롯을 더 넣기」가 아니다(떠야 도는 구조는 그대로) — **미리 떠서 기다린다.**
  ⓐ `wait_close.py`: 마감 **전** 슬롯(04:35·04:45 여름 / 05:35·05:45 겨울 — 각 25분 전, 예약 지연 흡수)에
  떠서 마감까지 자고 **20초 간격**으로 종가가 굳는 순간(regularMarketTime ≥ 마감 — v66 가드와 같은 판별)을
  잡는다. 쓰기 전 30초 안정 확인 · 마감 뒤 8분 안 굳으면 조용히 종료(휴장·지연 — 다음 슬롯이 맡는다, 새 실패를
  만들지 않는다) · **큰 움직임(>10%)이면 대조 소스가 CLOSE 될 때까지 최대 15분 대기**(v137 가드가 「아직 안 닫힘」
  으로 헛돌지 않게 — 그래도 안 닫히면 가드 규약대로 fail open). v75 슬롯 4개는 전부 예비로 남는다 — 새 경로가
  죽어도 최악은 종전 시각. `--selftest` 9경로(가짜 시계·가짜 시세, 네트워크 0).
  ⓑ `price_poll.py`(신규): 개장 전 슬롯(08:30·08:40·08:50)에 뜬 실행 하나가 09:00:20 부터 **5분 경계+20초**마다
  찍고 12:26 에 오후 실행으로 넘긴다(한 실행 6시간 상한). 스냅샷마다 price-data 브랜치를 임시 저장소에서
  덮어쓰고(v176 규약 그대로: 항상 커밋 1개) **`gh workflow run pages.yml`** 로 배포한다 — 실행이 안 끝나므로
  workflow_run 은 못 쓴다(GITHUB_TOKEN 이 만드는 이벤트 중 workflow_dispatch 는 예외적으로 실행을 만든다,
  공식 문서 → `actions: write`). 예비 슬롯(매시 :20·:50)은 동시성 그룹에서 대기하다 폴러가 죽었을 때만 이어받는다.
  **배포 깨우기가 막히면 즉시 종료**해 종전(슬롯당 1회) 방식으로 저절로 물러선다. 하루 85스냅샷(정렬 84 + 인계 1).
  ※ 시세는 표시 전용 — 판정 무접촉(QQQ 미국 종가만). **규칙·판정 변경 0.** 실행 수는 오히려 준다(17→2~4/일).
  ⚠ 확인 방법: 다음 영업일 새벽 `updated_at` 이 05:0x 인지 · 09:00 뒤 시세 「N분 전」이 5분 안에서 도는지.
- **★ 고평가·장기 횡보·대형 하락 환경 검증 (2026-09-02, 소유자 지시 · ChatGPT 작성 프롬프트 · 04 §5-26)**: 규칙 무변경 ·
  배터리와 판정문을 결과 전에 등록(`research/valuation_regime.py`, Shiller 1871~ + multpl CAPE + CPI-U 캐시 3개는 `data/hist/`).
  **[강화]** — 엔진과 겹치는 S&P 3년+ 사건 3개(1973·2000·2007) 전부에서 B 의 MDD 가 S&P·NDX 1배보다 얕고(−36/−45/−50% vs
  S&P −48/−49/−57%) S&P 회복일에 B 는 2.93/1.39/1.71배 · 고평가(CAPE 상위 20%) 시작 10년 창 B 20.3% vs S&P 총수익 7.0%
  (닷컴 국면을 빼도 20.3% vs 7.5%, B>S&P 100%). **단서 둘(새 약점 아님)**: ① **빠른 급락(<3년) 4/4 에서 B 의 MDD 가 S&P 보다
  깊다**(1987 −60% vs −34% · 2020 · 2022 — ④-3 급락 무방비) · ② **회복이 안 오는 하락(니케이 1989→2024, 34년)에서 같은 규칙은
  0.52배로 1배(1.00)보다 못하다** — B 의 우위는 「회복이 오는 하락」에서만 나온다. 「고평가라서 더 유리」는 표본이 못 낸다
  (10년 창 비중첩 1.6 · 국면 3 → C 판단 불가, **결과 보기 전에 그렇게 등록**). 현재 CAPE 41.7 = 1881~ 상위 1.0% ·
  CAPE 35+ 의 10년 창은 1998~2001 한 국면(실질 −3.0%/년). 배당·물가 넣은 실질 총수익 기준 미회복 최장 12.7년(20년 횡보 없음).
  ⚠ 인용 시 기준을 붙여라 — 「B 가 1배보다 낙폭이 얕다」는 **깊은 약세장 창에서만** 참이고 보통 창에선 반대(−52% vs −40%).
  ⚠ 예측 P2 는 틀렸다(1929 이전 15년+ 명목가격 사건 둘 더 — Cowles 월평균) — 04 에 그대로 적었다(§-1 ⑦).
  ★ **소유자 되물음 「강화?」 → 크기를 적었다(04 §5-26 판정 아래)**: 강화의 뜻은 「약화 근거 0 + 방어 3/3」이지
  「B 가 더 좋아졌다」가 아니다. 새로 보탠 것은 1973 창과 「고평가가 그림을 안 바꾼다」 둘뿐(비중첩 1.6)이고, 방향이 다른
  새 정보는 니케이형 0.52배 하나다(규약 밖이라 판정 제외 — 넣었다면 중립 쪽). cond_i 는 기지 결과로 통과가 예정돼
  변별력이 낮았다. **정확한 문장: 「약화 아님 · 강화라면 작은 강화 · 유지 판단은 검증 전과 같다」.** 라벨은 결과 뒤에 안 바꾼다.
- **v191 (소유자 「니가 알아서 다 해줘 묻지마」 — 실행 규율 4건 중 ①, 2026-09-02)**: **전환 미체결 경고.**
  발단은 소유자 질문 「실행 규율 쪽으로 뭘 더 할 수 있어」 → 실행 사슬 5고리(신호 생성·알림 도착·사람이 봄·실행·
  사전 준비)를 코드에서 대조해 구멍 4개를 찾았다. **가장 큰 구멍**: 체크리스트는 `changed_today` 인 **하루만**
  그려지고 다음 미국 종가가 들어오면 상단이 「🟢 오늘 할 일 없음」이 됐다 — 정작 아래 포트폴리오는 「🔴 매도 N주」를
  계속 내는데 **상단 큰 글씨가 반대말**을 했다. 지난 확인 배지(v102)도 5초 열람이면 「봤음」이라 새벽에 한 번 열고
  잊으면 아무것도 안 남았다. 04 §5-8 의 최악 −96.5% 는 정확히 「신호가 났는데 안 판 채로 다음 신호까지」다.
  ★ **판정 기준은 날짜가 아니라 「보유가 판정 반대편인가」**(`wrongSideWeight()` — 공격 판정인데 방어 다리 /
  방어 판정인데 공격 자산이 **2% 초과**, 「오늘의 행동」 오차 기준과 같은 값). 날짜 상한 없음(40일째여도 경고가
  맞다) · 월급 현금·방어 3종 드리프트는 반대편이 아니라 안 걸린다 · 보유 미입력이면 판정 안 함 · 전환일
  (`CHANGED_TODAY`)엔 기존 체크리스트가 담당. `drawPending()` 은 paintState 끝과 portCompute 끝(입력·체결 기록이
  바뀔 때마다)에서 재판정 — **체결 기록을 남기면 보유가 바뀌어 사라진다**(장부를 남기게 하는 부수 효과).
  실측 6경우(반대편 100% → 경고 · 같은 편 → 할 일 없음 · 방어색/공격색 · 먼지 0.39% 무시 · 전환일 무접촉 ·
  미입력 무판정) · 412px 드래그 0 · 콘솔 0 · verify_all 관문 추가.
  ※ **같은 세션의 인용 오류 정정**: 「하루 늦추면 126→102」라고 말했는데 원문(01 §3)은 **1일 지연 vs 3일 지연**
  이고 v21 표본이다 — §-1 ④(숫자를 인용하기 전 무엇에 대해 잰 값인지) 위반이라 소유자에게 정정했다.
- **v192 (같은 지시 ②③ — `deploy/watchdog.py` 모드 2개, 08:40 슬롯 재사용 · 전략 무접촉)**:
  ⓐ **`switchday` 전환 실행일 재알림** — 새벽 알림(signal_alert)은 자는 동안 오고 실행은 09:05 부터인데
  그 사이 4시간에 아무것도 없었다. 실행일 = 종가일+1 부터 첫 한국 거래일(`kr_holidays.json`, 화면 krExecLabel 과
  같은 규약). **실행일에만** 나가므로 거짓 알림 0 · 연 2~3건. 전환 출처는 장부 `changed=1` 행 1차 + signal.json
  `changed_today` 2차(장부 스텝이 실패한 날의 보험 — 늦은 쪽). 서버는 체결 여부를 모르므로 「이미 하셨다면 무시」.
  ⓑ **`near` 근접 진입 알림** — gap<3%p 에 **들어선 날** 한 번(전날 ≥3 · 오늘 <3 · 종가가 1영업일 안 · 전환일 제외).
  04 §5-8 「근접일 때는 절대 놓치지 마라」·설명서 §③ 「근접이면 떠나기 전 정리」가 **화면을 열어야만** 작동하던 것을
  폰으로 옮겼다. 근거 `research/near_zone.py`(54년 엔진 표본 · 게이지 정의는 화면과 동일): **진입 연 3.5회 ·
  55% 가 20일 안 전환 · 전환 140회 중 138회(99%)가 직전 5일 안에 근접을 거침 · 「여유」에서 곧바로 전환 0회 ·
  헛걸음 연 1.6회.** 「접근」(8%p)까지 넓히면 연 7.8회 — 피로라 안 넓혔다. **문구는 「아직 할 일 없음」이 먼저** —
  근접의 45% 는 되돌아가고 그때 규칙은 아무것도 안 하는 게 맞다. 04 「B 가 안 쓰는 숫자는 재량 개입 유혹」의
  경계 위 알림이라 미리 팔기를 부르지 않게 썼다(효과는 미측정 → 04 §7 Q5). 둘 다 alert 미점등(일정이지 이상 아님).
  ★ **적발 — `signal.json` 최상위 `state/exit/gap_pp/next_line` 과 `recent[].s` 는 구버전 화면 호환용 A 미러다**
  (`update_signal.py` 주석 「구버전 signal.html 호환용 미러 (A 기준)」 · 실측 최상위 **exit = −11**). B 는
  `strategies.B` 와 `recent[].B`. 첫 구현이 최상위를 읽어 **방어 상태의 복귀선을 −11 로 잡을 뻔했다** — 셀프테스트에
  그 경우를 박았다(A 미러를 일부러 반대로 두고 B 열을 읽는지). `signal_alert.py` 가 B 를 읽는 이유가 이것이다.
  **새 코드에서 최상위 키를 읽지 마라.** 검증: 합성 데이터 **24경우**(금 종가→월 · 추석 연휴→다음 개장일 · 장부
  실패 보험 · 경계 3.0/2.99 · 방어 복귀선 · A 미러) 실패 0 · 실제 저장소 발송 0 · yml 파싱 14스텝.
  ⚠ 알림 스텁이 서브프로세스라 stdout 캡처로는 발송을 못 잡는다 — 파일로 잡았다(첫 실행에서 5건이 「실패」로
  보였으나 실제로는 전부 나간 것 — 하네스 오류였다). ※ `deploy/*` 수정이나 rebalance_due 와 같은 모양의 날짜
  함수라 ⓓ 단서(전수 검산)로 max 충분.
- **v193 (같은 지시 ④ — 설명서)**: §③ 실행 시각 표·자동화 표·「미루지 마세요」에 08:40 재알림·근접 알림·
  미체결 경고를 등재. ※ 처음엔 부재 규칙의 「대신 팔 사람이 카톡 알림을 같이 받게」가 **현재 채널로 불가능**한 것
  (카카오 「나에게 보내기」 `memo/default/send` 는 본인에게만)을 적발해 텔레그램 그룹으로 고쳤으나,
  **소유자가 곧바로 v194 에서 대리인 자체를 삭제**했다 — 아래.
  ⚠ **카톡 도착은 자동 검증 불가**(v178 교훈) — 새 알림 2종은 실제 근접·전환이 와야 도착이 확인된다.
  **다음 근접 진입일에 소유자가 받았는지 물어볼 것.**
- **v194 (소유자 「대리인 관련은 삭제해 — 모든 건 내가 통제할 거야」, 2026-09-02)**: 대리인 관련 전수 삭제.
  ★ 지우기 전 repo 전체 grep(§-1 ①) — guide §③ 부재 규칙 ②·③, watchdog `near` 문구 「대신 팔 사람 확인」,
  notes v189·v193 항목, CLAUDE v189·v193 항목, 04 §5-23 E 요약줄. 전부 처리.
  **남는 부재 규칙 세 줄**: **한 달 이내 그대로 · 한 달 넘게 비우면 방어로 두고 간다 · 근접이면 떠나기 전 정리.**
  「한 달 이내」의 근거는 04 §5-23 E 표 그대로 — 미리 방어 비용(2주 −1.1% · 4주 −2.0%)이 그 사이 신호 확률
  (3.8% · 6.6%)보다 크다는 것(기대값). 대리인은 그 사이의 **꼬리**(−23~−29%)를 막던 장치였고, 두지 않기로 했으니
  그 꼬리는 소유자가 감수한다 — 표의 값을 바꾼 것이 아니라 사람을 뺀 것이다. 매매 규칙 무변경.
  **재제안 금지**: 대신 팔 사람 · 알림 공유(텔레그램 그룹·디스코드) · 증권사 가족 대리인 등록 · 자동매매.
- **v195 (소유자 「내게 더 필요한 건 없어? 놓치는 게 없는지 찾아줘」 — 동선 전수 점검, 2026-09-02)**: 신호 생성 →
  알림 → 사람 → 실행 → 보관 → 인출까지 훑었다. **빠진 것 셋을 채웠고, 나머지는 이미 있었다.**
  ⓐ **휴장일 표 자동 연장** — `data/kr_holidays.json` 은 2026~2032 로 **조용히 끝나는 파일**이었다(범위 밖이면
  화면 시계·실행일·재알림이 전부 주말만 밀게 된다). 파수꾼 주간 슬롯이 `kr_holidays.py --emit` 을 돌려 해가 바뀌면
  한 해씩 민다. **emit 이 내용을 비교해 같으면 쓰지 않는다**(generated_at 만 바뀐 파일을 매주 커밋하면 v176 이
  없앤 이력 소음이 되살아난다). 재생성 동치 실측(116일 전부 일치) 후 자동화 — 임시공휴일·선거일은 여전히 SPECIAL 에
  손으로. 과거 연도가 빠져도 되는지 사용처 전수 확인(signal.html 시계·price_poll·watchdog 전부 오늘/미래만).
  ⓑ **인출기 현금 완충** — `withdraw.py`·MEASUREMENT_AUDIT 의 설계 요건(「최소 1년치 생활비 현금」, 평가액 비율
  인출 시 최악 연간 소득 −51.3%)이 **설명서엔 없었다**(§⑥ 「두 방패」에 배당·채권금만). 세 번째 방패로 등재.
  ⓒ **폰 쪽 무음** — 카톡 「나에게 보내기」 방의 알림을 끄면 전환 알림까지 전부 무음인데 어디에도 없었다 → §⑧ 한 줄.
  **확인했고 이미 있던 것**(재점검 불요): GH_PAT 없거나 실패해도 카카오 만료 예고가 카톡으로 간다(kakao_keepalive
  46~75행) · 30일 재조정 앵커는 화면·파수꾼 모두 OOS 장부 기준(체결 기록 무관) · 시세 없으면 nav 로 후퇴 + 3일
  경고 · 카톡 죽으면 이슈(메일) · 60일 규칙은 v177 생존 알림 · 파수꾼 08:40 슬롯 실측 2회 모두 5~6분 지연(25분 여유 안).
  **알려진 한계(수정 안 함)**: 08:40 슬롯이 통째로 버려지면 그날의 재알림·근접 알림은 없다 — 05시 알림(다중 슬롯)이
  1차이고 이건 2차라 받아들인다. daily-signal 이 08:40 뒤에야 도는 날(예비 09:17 슬롯만 산 날)도 같다.
  **소유자만 할 수 있는 것(제안만, 만들지 않음)**: GitHub 계정 복구 코드·카카오 개발자 계정 접근을 적어 두었는가 ·
  「내가 움직일 수 없을 때 이 계좌를 어떻게 정리하나」 한 장 — 소유자가 「모든 건 내가 통제」라 했으므로 제안에서 멈춘다.
- **v196 (소유자 「더 없어? 기능 추가·헤지·압축」 → 실측 후보 → 「전부 해봐」, 2026-09-02)**: 여섯 건.
  **압축 셋(실측 412px)**: ⓐ 업데이트 노트 최신 시즌 26항목·11,714px(68%) → **v186~ 만 펼치고 v168~v185 는
  `data-foldshut` 로 접음**(시즌 분할, 항목 삭제 0). ⓑ 성과표 **각주 716자·542px 가 표(511px)만큼** → `details`
  「각주」로 접음(3문단 검사 유지). ⓒ 위기 타임머신 342px → `details` 로 접음(그래프는 펼침 유지 — v170 「위기
  궤적은 그 위로」). 셋 다 v180 기준(상태 밖·설명 안)이고 **정보 삭제 0.**
  **설명서 §③ 정리**: ★ **실제 오류 2건 적발** — 전환일 목록 2번이 **v185 에서 지운 「포지션 계산기」**를 아직
  가리켰고(§-1 ③: 지운 뒤 같은 파일 나머지를 안 읽은 사례가 또), 「실행 시각 상세」 표의 「06:00~09:00 종가를
  화면에 입력」은 **수동 입력 시절의 유물**이었다. 목록을 오늘의 행동·08:40 재알림·체결 기록으로 다시 썼고,
  표는 §⑧ 표와 겹쳐 삭제(경고 「미루지 마세요」는 전환일 아래로 이동), 인내 두 소절은 하나로.
  **문구 하나**: 전환일 체크리스트에 「전환의 54%(139회 중 75회)는 손해로 끝나는 게 정상 · 놓친 전환 최악 −96.5%」
  — 새 문장 창작 0(04 §5-8·설명서 §③ 기발행). **v129 가 지운 상기성 문구와 다르다** — 평시 면책이 아니라
  망설이는 순간의 근거. 그래도 소유자 취향 영역이니 「이상하다」면 이 한 줄만 되돌린다.
  **기능 하나**: 월 1회 생존 카톡에 「판정·낙폭·선까지 거리·동결 후 장부 일수·전환 횟수」 — 메시지 수 불변,
  B 는 `strategies.B`(최상위는 A 미러). 실제 `ops_check.json` 무접촉으로 가짜 디렉터리에서만 실행 검증.
  **헤지·추매 질문에 대한 답(같은 대화)**: 04 에 전부 있다 — 낙폭 분할 매수 −1.5~−43% · 무한매수 54년 3.2배
  MDD −99.4% · 딥매수 재공격 B 의 1/24 · 트랜치 기각 · 풋 문헌 기각 · VIX 3중 · 변동성 타깃팅 MDD −80% ·
  인버스 0.997× · 스탑 0.71×. **재제안 금지.** 헤지6/4 는 설명서 §⑤의 선택지로 이미 있다.
- **v197 (소유자 「더 대비·계산·찾아보기·추가·압축할 건 없는지 알아봐」 — 마지막 점검, 2026-09-02)**: 아직 안 본
  네 갈래(세금·계좌 만기 / 공개 저장소 개인정보 / 남은 수동 작업 / 열린 계산 항목)를 훑었다.
  ⓐ **공개 저장소에 실제 보유 주수가 있었다**(v179 항목의 주수·금액) — 일반화해 지웠다. ※ git 이력에는 남는다(이력
  재작성은 v175 규약으로 금지) — 현재 파일에서만 사라진 것이다.
  키·토큰 모양 문자열 0 ·
  TODO 0. ★ **소유자 판정(2026-09-02 v198): 「실제 보유 주수는 적혀 있어도 된다 — 개인정보 관련은 그냥 놔둬,
  알아서 한다」. 개인정보를 이유로 문서를 고치거나 제안하지 마라.** (지운 것은 되돌리지 않았다 — 손댈 이유가 없다.)
  ⓑ **ISA 만기 연장**은 02 §7 에 확정돼 있었는데(3년마다 해지하면 20년 −16%) 설명서엔 없었다 → §④-6 한 줄.
  ⓒ **상품 소멸 FAQ** — 순자산 감시(주의 <300억)가 먼저 알리고 대체품은 그때 정한다(04 Q2: 409820 은 환헤지형).
  현재 4다리 전부 정상(418660 6,488억). ⓓ §② 「당장 쓸 돈·비상금은 전략 밖」 반 문장.
  ⓔ **마지막 남은 「사람이 기억하는 연 1회」를 없앴다** — `emit_dd_distribution.py` 를 `monthly-stats.yml` 의 원자료
  연장 직후에 얹었다. v164 가 「자동 워크플로에 넣지 않는다」고 적었지만 그 근거(분포는 안 변한다)는 **원자료 연장이
  자동인데 파생물만 수동이면 조용히 낡는다**는 v171 유형의 구멍을 못 봤다. 실측 1초 · 같은 원자료면 산출 동일(diff 0).
  **확인만 하고 손 안 댄 것**: 금융소득종합과세 문턱(설명서 ⑥ 표에 이미) · 04 §7 Q1·Q2·Q5(전부 기계적 재개 조건, 지금 할 일
  없음) · crisis_paths.json(정적 자료, 갱신 불요). **결론: 이 이상 대비·계산·추가·압축할 항목은 찾지 못했다.**
  소유자만 할 수 있는 것 둘은 v195 그대로(GitHub 복구 코드 · 카카오 개발자 계정 접근).
- **v198 (소유자 「바깥에서 보기 — 너무 중요한 거라 최상단에」 + 「개인정보는 놔둬」, 2026-09-02)**: ⓐ 신호 화면 마스트헤드
  바로 아래에 **바깥 링크 스트립**(구글 파이낸스 · 네이버 증권 · 418660 호가·NAV · `?`→설명서 #links). ⓑ 설명서의 §③
  「시장이 궁금할 때 — 바깥에서 보기」 블록(표·안내·「보는 것은 자유, 규칙은 그대로」)을 **맨 위(검색창 위)로 이동** —
  삭제 아님, `id="links"` 유지(스트립의 `?` 가 여기로). 번호 절이 아니라 TOC 재번호 없음(v167 전수 갱신 재발 방지).
  ⓒ 개인정보 규칙 정정 — v197 의 「실제 수량을 적지 마라」는 소유자가 철회. **개인정보 이유의 수정·제안 금지.**
- **★ 열린 연구 항목 셋 연구 (2026-09-02, 소유자 「열린 연구 항목 셋은 연구해봐 명령이야 — 본작업물에 반영하지 말고 연구만」 · 04 §7-3~7-5)**:
  규칙·상품·화면 **무변경**. 셋 다 관문·예측을 결과 전에 파일 머리에 등록했고 **틀린 예측 4개를 그대로 적었다**(§-1 ⑦).
  ⓐ **Q1 현물 국채 → 닫힘**: 재개 조건이 실제로 충족돼 있었다(네이버 목록에 현물·환노출 미국채 0085P0·476760). ★ 그 목록은
  **cp949** 라 `nav_collect.fetch()` 의 utf-8 디코드로는 이름이 깨진다 — 코드 매칭이라 수집엔 영향 0, **이름 검색엔 cp949 로 다시 읽어라.**
  현물 10년 Calmar **+4.4%**(관문① 10.2% 미달) · ② 통과, 30년 ①②미달 → **305080 유지**. 국채 다리 단독 +5.7%p/년 이 전략 잣대에선
  절반이 되는 이유: 방어일 18% × 비중 40%.
  ⓑ **Q2 환헤지 대체품 → 숫자표(실측 carry, 09-03 갱신)**: 갈아타면 최종 **0.82배**(carry 평균 +1.85%p/년 · 앞으로가 2022형이면
  0.37배 — 범위 0.4~0.8) · CAGR −0.9%p · 2008형 창 −10.4%p. 그러나 **전체 MDD 는 +2.0%p 얕고 20년 p05 는 +84%** — 예측 P1(MDD 5~15%p
  악화) 틀림. 환노출 2배는 위기 종류에 따라 완충(2008·2020·2022)이자 소음(1998 원화 반등)이다. ★ 첫 실행(09-02, carry 0)의
  **0.53배는 과소평가**였다 — 원화 금리가 달러보다 높던 25년 동안 헤지는 비용이 아니라 수입. 숫자를 인용할 땐 carry 전제를 붙여라(§-1 ④).
  ⓒ **Q5 미리 팔기의 가격표**: 근접(−13)에서 팔고 벗어나면 되사면 **0.33배**(예측 0.6~0.9 — 훨씬 비쌌다) · 절반만 팔아도 0.62배 ·
  에피소드당 −0.66%. **행동 자체는 미측정**(사건 0) — 규약: D+n<0 또는 무신호 매도 1건이면 알림 유지 여부 판단.
  ★ S15(−15/−15)가 Calmar·p05 에선 현행보다 높다 — §5-22 의 기지 신호. **재탐색 안 함**(통짜 1위는 비중첩 1개 · PBO 0.4~0.8).
- **★ 외부 출처 보험 + 파수꾼 셀프테스트 (2026-09-03, 소유자 「FRED 다시 시도해 실측으로 갱신 · 응답 안 하는 경우 보험도 들어놔」)**:
  FRED 가 이틀째 타임아웃(urllib·curl, `graph`·`data` 엔드포인트 전부 60초 무응답). **같은 데이터의 2차 출처**를 실측으로 찾았다 —
  한국 3개월 금리는 **OECD SDMX**(`sdmx.oecd.org` DSD_STES@DF_FINMARK KOR·IR3TIB, 2026-08 까지 · 2026-06 = 2.91 로 FRED 와 일치 · 1.5초),
  환율은 **야후 KRW=X**(0.4초). DBnomics 는 MEI 가 2024-01 에서 끝나 못 쓴다.
  ⓐ `research/q2_hedged_attack.py`: FRED → OECD SDMX → 캐시(`data/hist/kr_3m_rate.csv`, 출처 열) → 상수 감도. 캐시를 커밋했으니 다음엔
  네트워크 없이도 돈다. Q2 는 위 ⓑ 로 실측 갱신.
  ⓑ `deploy/refresh_hist.py refresh_fx`: FRED 실패 시 **야후 KRW=X 로 마지막 날짜 이후만 이어 붙인다**(실측 7행 보강). 「짧아지면 유지」
  가드를 행 수 → **마지막 날짜(45일) + 행 수 10% 여유**로 바꿨다 — 야후 행이 붙은 뒤 FRED 파일이 행 수로는 잠깐 짧을 수 있어서.
  세 갈래 실측: FRED 타임아웃 → 야후 보강 · 가짜 FRED(2행 짧음·최근 날짜) → 전체 교체(야후 행 제거) · 절반 손상 파일 → 거부.
  `hist_korea.fx` 는 안 건드렸다(§2) — 파일 형식이 같아 읽는 쪽은 모른다. 종전엔 예외 → 「기존 유지」뿐이라 달을 넘기면 환율이 조용히 낡았다.
  ⓒ **파수꾼 `--selftest`**(28경우: switchday 13 · near 12 · heartbeat 3)를 저장소 안으로 — v192 때 스크래치에만 있었다(「검사를 추가했다」와
  「검사가 돈다」는 다르다, v148). `verify_all` 전체 모드 **I14** 가 이것과 `wait_close.py --selftest`(9경로)를 돌린다 → 모드를 고치면
  push 검증에서 잡힌다. ※ 셀프테스트의 「fatal: not a git repository」 한 줄은 heartbeat 가 임시 디렉터리에서 price-data 브랜치를
  fetch 하려다 나는 소리 — 예외로 잡혀 무해.
  **안정화 점검(같은 질문 「더 최적화·안정화할 건 없나」)**: 외부 의존 5개의 예비 — QQQ 종가(야후→야후2→네이버→캐시 ✔) · 환율(FRED→야후 ✔ 이번)
  · 한국 금리(FRED→OECD→캐시 ✔ 이번, 연구 전용) · 한국 시세·NAV(네이버 단일 — 죽으면 배지가 사라지고 3영업일 뒤 카톡, 판정 무관) ·
  휴장일(규칙 산출, 네트워크 0 ✔). **「최적화」는 전략엔 없다**(§2 · 04 무덤). 남은 선택지(제안만): 카톡 외 본인용 2채널(텔레그램)은 Secrets
  하나면 켜진다 · 저장소 클론을 두 번째 기기에.
- **★ 한국 시세 예비 출처 체인 (2026-09-03, 소유자 「안정화 — 네이버·구글·토스·카카오·증권사 뭐든, 보험료 없으면 보험은 많을수록」)**:
  새 모듈 `deploy/kr_sources.py`. **판정 경로엔 안 붙였다** — 전환은 여전히 QQQ 미국 종가(야후→야후2→네이버→캐시 + v137 가드).
  붙은 곳 셋(표시·기록·원자료): ① `price_now.py`(→ `price_poll.py` 도 같이) 장중 스냅샷 — 1차 네이버 ETF 목록이 죽거나 **종목이
  빠지면 그 종목만** 예비로 채운다(`price.json` 의 `source` 에 「naver etfItemList + daum(kakao)」처럼 남는다). ② `nav_collect.py` —
  1차 죽으면 예비(예비엔 NAV 가 없어 그날 행은 안 쌓이지만 스텝이 죽지는 않는다). ③ `refresh_hist.py splice_kr` — 야후 죽으면
  **네이버 일봉 XML**(⚠ 수정주가 아님 · 배당락 미반영 — 458730 은 배당락일 0.3% 안팎 어긋남, 이음 비율 k 가 수준 차이는 흡수).
  **체인(종목별, 12초씩)**: 네이버 polling → 네이버 모바일 basic → 다음(카카오) quotes → 토스 v1 stock-prices → 야후 chart →
  구글 파이낸스(HTML, 첫 ₩ 금액 — 취약해서 맨 뒤). **실측 2026-09-03(KR IP): 1차 + 예비 6/6 전부 4다리 응답, 값 전부 일치.**
  안 넣은 것과 이유: **증권사 API(미래·삼성·한투)** — 키가 계좌에 묶이고 토큰이 하루짜리라 「보험료 0」이 아니다 · KRX data 는 OTP 필요 ·
  네이버 `api.stock.naver.com/etf` 는 409. 예비 출처엔 **NAV 가 없다** → 화면은 괴리 배지만 숨긴다(devBadge 는 nav 없으면 빈 문자열 —
  v146 부터 그렇게 돼 있었다). 화면 파일 무변경(price.json 스키마 그대로, 새 필드 `source` 만 서술적).
  검증: `--probe` 생존 표 · 실패 시뮬레이션 4/4(1차 전멸 → 5종목 예비 · 한 종목만 빠짐 → 그 종목만 · nav_collect 1차 전멸 → 9종목
  예비·행 0 · 야후 전멸 → 네이버 일봉 1행 이음) · verify_all 「예비 체인이 셋에 붙고 판정 경로(update_signal·wait_close)엔 없다」 관문.
  ⚠ **러너 IP 검증**: 로컬(한국)에서 되는 출처가 GitHub 러너(미국)에선 막힐 수 있다 → `source-probe.yml`(수동)로 러너에서 같은
  표를 찍는다. 결과는 아래 「러너 실측」 줄에 — 없으면 아직 안 돌린 것.
  ★ 시뮬레이션 함정 하나: `urllib.request.urlopen` 을 몽키패치하면 **모든** 출처가 같이 죽는다(공유 모듈) — 1차 URL 만 바꿔서 재라.
  **러너 실측 (2026-09-03, run 33650329853, GitHub 미국 IP)**: 1차 OK(1,167종목 1.9s) · 예비 **6/6 출처가 4다리 전부 응답** ·
  값 전부 일치(418660 37,935) · 네이버 일봉 OK — 지역 차단 없음. 재점검은 `gh workflow run source-probe.yml`.
  **판정 경로(QQQ 종가) 출처 3개도 같은 날 실측 생존**(야후 query1·query2 마지막 09-01 707.64 = 캐시 · 네이버 해외종목은 미국장
  중이라 「확정 종가 아님」 정상 거부). `--probe` 가 이 셋도 같이 찍는다(읽기만 · 판정·파일 무접촉). **4번째 판정 출처는 안 넣었다**:
  Stooq 는 봇 차단(JS 검증 페이지) · 구글 파이낸스 QQQ 는 HTML 이라 확정 종가 플래그가 없어 장중가를 종가로 오인할 수 있다 —
  그 오차는 보험료다. 판정은 야후 2 + 네이버(marketStatus 가드) + 캐시 + fail-open 그대로.
- **v199 (소유자 「이제 더 없는 거야? 에러나 반영·압축 등」 — 마지막 문구 점검, 2026-09-03)**: 오늘 바뀐 것 때문에 낡은 문구를 세 화면에서
  검색 — 포트폴리오 안내 「장중 시세(30분 간격 스냅샷)」이 v190(5분) 뒤에도 남아 있었다 → 5분. 같은 파일 주석 4곳(30분)·v164 주석
  (연 1회 수동 → v197 매월 자동)도 정정. 배포 사이트 3페이지 내부·교차 앵커 깨짐 0 · 콘솔 오류 0(실측). 압축은 v196 뒤 추가 후보 없음.
- **v200 (소유자 「기능개선도 없고? 내가 놓친 게 없어?」 — 2026-09-03)**: 세 가지가 나왔다. 둘은 내가 만든 것의 부작용, 하나는 소유자가 놓쳤을 것.
  ⓐ **예비 출처 가격 가드**(`price_now._plausible`) — 어제 붙인 예비 체인이 잘못 파싱한 값(예: 구글 HTML 의 다른 종목 금액)을 그대로 실으면
  「오늘의 행동」 **주수 계산에 들어간다**. nav_history 의 마지막 종가와 **25% 넘게 어긋나면 그 종목을 싣지 않는다**(화면은 없으면 숨김 →
  소유자는 MTS 를 본다). v122 체결가 오타 가드(±20%)와 같은 발상, 2배 ETF 하루 움직임보다 넓게. 실측: 19,280 을 418660 값으로 주입 → 차단 ·
  정상값 통과 · 전부 이상값이면 items 0 → 기존 파일 유지.
  ⓑ **알림 채널이 하나도 없는 상태를 파수꾼이 침묵하던 것** — `mode_channel` 은 secret 미설정을 「확인할 것 없음」으로 넘겼다. 그 상태는
  「전환일에 아무 알림도 안 오는 상태」다. 주간 슬롯에서 이슈(메일)를 열게 했다(카톡으로는 말할 수 없으니). 지금은 카카오 2개가 등록돼
  있어(`gh secret list`, 2026-08-29) 해당 없음 — 지워지거나 만료 삭제될 때의 보험.
  ⓒ **방어 중 분배금** — 배당 ETF·국채 ETF 분배금이 현금으로 들어오는데 화면은 계좌를 못 보니 「현금」 칸에 직접 넣어야 재조정이 맞는다.
  설명서 ③ 30일 재조정 문단에 한 문장. 저장소 어디에도 없던 안내다.
  ★ 「더 없나」에 대한 답: 이 셋 뒤로는 **찾지 못했다.** 훑은 갈래는 CLAUDE.md v195·v197·v199 항목과 이 항목에 적힌 대로다.
  ※ **업데이트 노트 2차 솎아내기 (2026-09-03, 소유자 「중요한 것만 남기고 자잘한 건 첨삭」)**: v168~v200 두 시즌 31 → 17항목(전체 54 → 40).
  기준은 v170 그대로. 걷어낸 것: 문구 정정(v199) · 내부 검사 수리(v172) · 용어표 등재(v187) · 점검 보고 자체(v195·v197 은 설명서에 추가된
  대비 항목만 v200 에 묶음). 합친 것: 화면 배치 7건→1(v184) · 시세 2건→1(v176) · 파수꾼 알림 3건→1(v177). 문장도 줄였다.
  ⚠ 노트 관문(CLAUDE §4 최신 vNN ≤ 노트 최신 vNN)은 **합친 항목의 span 이 v200** 이라 유지된다 — 다음에 vNN 을 올리면 노트에
  그 vNN 항목이 하나는 있어야 한다(자잘하면 기존 항목 결론에 한 구절 + span 은 그대로 두고 **제목에 버전만 얹어라**).
- **★ [별도 탐구 · 가상 전략] → `research/EXPLORATION.md` (2026-09-03, 소유자 「내 전략과 별개로 · 기준 틀 없이 · 기계든 새 무덤이든 · 별도로 분류」)**:
  자유 설계 6형태 · 개량 2라운드(설계 창/보류 창·역방향) · 새 길 넷(병렬 슬리브·안전자산 단위 낙폭·고점 이후 시간·기계 정책) · 새 길 2(환 오버레이·계절·
  평활 고점) · C3 반증 8종 + 플라시보 — **39후보 · 관문 ①②③ 동시 통과 0.** ①② 통과 1건(C3 ÷T-bill: 54년 Calmar +13.4%·p05 +26%)은 이득이 1973~90
  고금리 시대에 몰렸고 **뒤섞은 T-bill 200 중 27% 가 ①② 를 넘겨** 우연과 구별 불가 → 닫음. 기계 정책은 규칙을 재발견하지 못했다(낙폭 계열 3/45년).
  **본 전략 문서·화면·deploy·data 무접촉** — 이 탐구의 기록은 처음엔 04 §5-27·5-28 과 이 §4 에 붙였으나 소유자 지적(「연구인데 기존 자료를 손댔나」)으로
  **분리했다**: 04 엔 포인터 한 문단, 여기엔 이 항목 하나. **정지 규칙**: 표본 안 탐색은 멈춘다 — B 를 넘을 길은 새 데이터(그림자 장부, 소유자 승인 ·
  04 §7 Q7)와 잣대 변경(04 §7 Q6)뿐. 다음 세션이 「새 후보」를 요청받으면 **먼저 EXPLORATION.md 의 재제안 금지 목록을 읽어라.**
  ⚠ 교훈(§-1 계열): 「별도로 분류」라는 지시를 받고도 공용 문서(04·CLAUDE)에 붙였다 — 기각 기록은 남기되 **어디에 남기는가도 지시의 일부**다.
  ★ **이식 (같은 날, 소유자 「B 에 도움되는 결과면 반영해도 좋다 — 통제조건 차이를 감안해서」 · 04 §5-29)**: 후보는 0, **재는 법 셋**.
  ⓐ 관문 ①②③ 의 파라미터 잡음 폭을 정식 엔진으로 처음 쟀다(`research/b_gate_noise.py` · 공표 재현 뒤 이웃 200×5): 좁은 이웃 ΔCalmar p95
  **+9.0~+10.1% vs 관문 ① +10.2% — 경계선이나 4씨앗 0/4 로 잡음 위 · 상향 없음**. **①② 만은 잡음의 2~4.5% 가 넘고 ①②③ 은 800개 중 0 → ③ 은 필수.**
  B 는 이웃에서 부의 꼭대기(최종배수 중앙 0.68배)지 Calmar 꼭대기가 아니다(57~60% 가 높음 · 전부 −15 계열 · 블록 2/4) — §5-22 신호의 분포 확인,
  재조정 근거 아님. ⓑ 입력 뒤섞기 플라시보를 §-1 반증 방아쇠 **ⓔ** 로. ⓒ 탐구 이름 색인 + 「÷T-bill 낙폭 = S15 재표현」. 그림자 장부는 **불요**
  (C3·X1 은 공개 계열의 결정론적 함수 — 갈린 사건이 나면 사후 재현 · deploy 무수정). 틀린 예측 P2·P4(좁게 흔들수록 관문에 가까워진다)는 그대로 적었다.
- **★ 회복 속도 가설 검증 (2026-09-03, 소유자 「최근으로 올수록 폭락 회복이 빨라졌다고 느껴 — 물론 폭락의 성질이 다를 수 있다」 · 04 §5-30)**:
  외부 AI 가 짜 준 프롬프트(§1~§9)를 같이 받았다. **가설 부분만 쟀고 설계 부분은 무덤과 대조만 했다**(재탐색 금지). `research/recovery_speed.py` —
  배터리 8종·예측 7개·판정 규칙 결과 전 등록. **판정: 판단 불가 · 전략 변경 0.**
  ★ **초판을 소유자가 잡았다**: 겹침만으로 사건을 묶었더니 **닷컴 회복이 2015 라 그 안에 2008 이 통째로 들어갔다**(「08년 금융위기도 따로 떼내야
  하지 않을까」). **저점이 직전 묶음보다 높으면 새 사건**으로 분리 규칙을 고쳤다(임의 상수 없음 — 같은 약세장은 저점이 계속 낮아진다).
  아래는 정정 후 값이고 **판정은 그대로**다(순열 p 0.272 → 0.315 로 오히려 약해졌다).
  ⓐ 시대별 중앙 회복일(독립 사건 14건): 1970~89 374일(n6) · 90년대 94일(n2) · 2000년대 2,666일(n2 — 닷컴 4,582 · 금융위기 750) ·
  2010년대 109일(n1) · 2020~ 79일(n3) — **단조가 아니다**(90년대가 이미 지금만큼 빠르다). 순열 p **0.315**.
  ⓑ **깊이가 진짜 변수**(상관 −0.65) — 같은 깊이대로 통제하면 시대 차가 사라진다. 즉 「회복이 빨라졌다」가 아니라 **「1972·2000 같은 깊은 것이 그 뒤로
  안 왔다」**. ⓒ 코로나 빼면 2020년대는 2건이고 **2022 는 545일**로 느리다. 실질(CPI) 기준은 전부 더 길다(2022 545→699 · 2008 750→785).
  S&P 도 같은 방향이라 나스닥 구성 변화 설명은 약하다.
  ⓓ **★ B 의 승패를 가르는 축은 회복 속도가 아니라 하락 속도다** — B÷2배보유 순위상관이 회복일과는 +0.62 인데 **하락일과는 +0.90**.
  1987 은 21일 만에 −39.9% 라 회복이 느렸는데도 B 가 0.80 으로 **졌고**, 닷컴(861일에 걸쳐 −50%)은 39.69, 2008(386일 −49.4%)은 1.54 로 **이겼다**.
  §5-26 「빠른 급락 4/4」와 같은 축. 정정 덕분에 보인 것 하나 — **깊은 폭락 중 가장 빨리 회복한 것은 2008(750일)**이고 그 사건은 B 가 이겼다.
  ⚠ **측정 정의의 함정**: 사상 최고가 기준(D1)은 2008 을 삼키고, 지그재그(D2)는 긴 약세장을 중복 계수하며, 묶음(D3)은 **분리 규칙이 없으면 또 삼킨다**.
  **D2·D3 는 결과를 본 뒤 추가·수정한 정의**라 방향을 명시했다 — 두 번 다 **가설에 유리한 쪽**으로 고쳤는데 판정은 같았다.
  회복 표를 인용할 땐 **어느 정의인지 붙여라.** 틀린 예측 P2(절단 3건 → 1건)·P4(코로나 제외 1건 → 2건)는 그대로 적었다.
  프롬프트 설계 12항목(분할매수·현금대기·이동평균·VIX·스프레드·금리·거시·국면분류·회복시작 포착·부분노출 등)은 **전부 기존 기각**과 1:1 대응 — 04 §5-30 표.
- **★ QQQ ↔ 방어 배당 다리 — 상관·섹터·「2008 처럼 같이 빠지나」 (2026-09-03, 소유자 질문 3건 · 04 §5-31)**:
  `research/schd_qqq_overlap.py` · 예측 5개 결과 전 등록 · **전략 변경 0**. 새 캐시 13개(섹터 ETF 11 + QQQ·SCHD)는 **연구 전용**(월간 파이프라인 밖).
  ⓐ 「2배보다도 졌나」 → **전체로는 아니다**: 54년 B 217,110배 vs 2배 계속보유 5,760배(**38배**) · MDD −60.5% vs −98.8%.
  §5-30 F8 은 **고점→회복 구간만** 잘라 본 표다. 10년 단위로는 1970년대 2.61배·2000년대 39.90배로 이기고 나머지 넷은 0.84~0.98배로 조금씩 진다 —
  **평시에 조금씩 내주고 대폭락에서 전부 되찾는 구조.**
  ⓑ **종목별 상관은 못 낸다** — 보유종목 데이터가 저장소에 없다. 대신 수익률 상관(QQQ↔SCHD 전체 +0.713 · 폭락일 **+0.749** · 평시 +0.641)과
  **수익률 기반 섹터 역추정**으로 답했다. ★ 252일 이동 상관이 최근 크게 낮아졌다(2012~16 +0.81~0.89 → 2024 +0.377 · 2026 +0.054).
  ⓒ **섹터 겹침 7%** — QQQ 기술63·통신17·경기소비16(R² 0.98) vs SCHD 필수소비20·에너지20·헬스케어18·소재11·부동산10·**금융 9**·산업8(R² 0.86).
  ⓓ **★ 소유자 전제 정정**: **SCHD 는 2011-10-20 상장이라 2008 에 없었다.** 엔진의 2011 이전 배당 다리는 French **BE/ME Hi30 가치주 대리**다
  (`hist_defensive.py` 머리에 명시). 그 시절 실물인 **DVY** 로 재니 GFC 에서 **−59.9%** 로 QQQ(−53.0%)보다 깊었다 — **소유자 기억은 정확하다.**
  오늘 SCHD 의 내재 금융 비중은 **9%**(DVY 2007~09 은 28%)라 그 정도는 아니지만, **2020 코로나에서는 SCHD −32.8% 로 QQQ −27.9% 보다 더 빠졌다**
  (예측 P3 틀림) — 금융 비중과 무관하게 **유동성 급락엔 주식이 다 같이 빠진다.** 방어에 배당이 40% 뿐인 이유(§5-16 A·§5-17)와 같은 사실.
  ⚠ **금융 β 는 국면 간 비교 금지** — DVY 2007~09 β(XLF)=0.51 < SCHD 오늘 0.58 인데, 그때 XLF 변동성이 폭발해 분모가 커진 탓이다. 비중은 스타일 분석으로 본다.
  ⚠ 스타일 분석은 보유 공시가 아니라 **역추정**이다(SCHD 의 「부동산 10%」는 리츠 제외 지수라 금리 민감도가 대신 잡혔을 가능성) — 인용 시 붙여라.
  ⚠ 작업 중 실제 버그 2개: 스타일 회귀에서 **날짜 하나만 결측이어도 그 섹터가 통째로 제외**돼 QQQ 에서 기술이 빠졌다(R² 0.26 → 교집합 정렬 후 0.98) ·
  자작 최적화기가 코너로 수렴 → Lawson-Hanson NNLS 로 교체. **R² 가 낮으면 그 표는 읽지 마라 — 그게 이번의 경보였다.**
- **★ QLD ↔ SCHD 배합 스캔 (2026-09-03, 소유자 「SCHD 를 공격에서의 헤지로 · QLD 를 최대한 들고도 낙폭이 크게 좋아지는 비율」 · 04 §5-32)**:
  `research/hedge_ratio_scan.py` — 비율 10~100% 전수 · 창 3개(54년 · 21세기 · 실물 SCHD 2011~) · 관문·예측 결과 전 등록 · **전략 변경 0**.
  **검산 통과**: 21세기 QLD100% 167.3배·21.18%·−48.4%(공표) · QLD60% 63.0배·16.82%·−38.5%(guide §⑤) 와 일치.
  ⓐ **신호가 없으면 배합은 공짜 점심**: 정적 매수보유 54년에서 QLD 100→80% 는 **돈도 늘고(5,760→7,200배) 낙폭도 준다**(−98.8%→−96.3%).
  2배 매수보유의 변동성 끌림이 워낙 커서다. 소유자가 느낀 「기하급수적」은 **여기서는 실재**한다(100→90% 한계 교환비가 음수).
  ⓑ **신호를 얹으면 공짜가 사라진다**: Calmar 최고는 54년 QLD 50~60% 에서 **+8.7%** 로 **관문 ①(+10.2%) 미달**이고 **20년 p05 는 전 비율에서 악화**(−5~−90%).
  구조적 이유 — **−16 전환이 이미 낙폭을 자르므로 배합은 보험을 두 번 드는 것**이고 두 번째 보험료가 부의 바닥에서 나간다.
  ⓒ **최고 비율은 창에 따라 움직인다**: 54년 50~60% · 21세기 80% · 실물 2011~ 100%(섞지 않는 쪽). **guide §⑤ 의 「50~60%」는 54년 창 이야기** —
  틀리지 않았으나 인용 시 창을 붙여라.
  ⓓ 소유자 시대 주장: 2022(−10.1% vs −35.1%)·2026 연초~(+27.6% vs +15.7%) **맞음** · 2008 은 반대(DVY −59.9% vs QQQ −53.0%) · **2025 는 나스닥 승**(+21.0% vs +4.6%).
  **「닷컴 이후 2007 까지」는 시작점이 답을 뒤집는다** — 고점 2000-03 부터면 배당 +135.3% vs 나스닥 −52.4%(배당 압승), 저점 2002-10 부터면 +179.4% vs +122.7%(나스닥 승).
  ⚠ 표는 **거래비용 0** 이고 월 1회 재조정은 **연 12회 매매**를 새로 만든다(현행 연 2~3회) — 소유자 개입을 줄여 온 v140 방향과 반대다.
  ★ `공유용_별도전략/` 은 **근거로 쓰지 않았다**(§2 격리 · QQQ 1배+SCHD 라 자산이 다르다). 엔진만 같고 새로 쟀다.
  **결론: 채택 후보 아님 · 현행 공격 100% 유지.** 다만 헤지6/4 는 **최적화가 아니라 감내선의 선택지**로 guide §⑤ 에 그대로 남는다.
  ⓔ **[보강] 소유자 되물음 「10%·20% 만 섞어도 유의미한가」 (04 §5-32 D)**: **변동성은 분명히 줄지만 비례일 뿐 볼록하지 않고, 바닥은 안 올라간다.**
  10%p 마다 변동성 상대 **−7~9%** · MDD **3.3~3.5%p** · 하루 −3% 하락일 **연 2회** 감소(15.8→13.6→11.5회). ★ **10년 최악5% 는 54년 창에서
  3.2→3.2→3.1 배로 사실상 불변**이고 줄어드는 것은 중앙값(10.0→9.2→8.5배) · 20년 최악5% 는 오히려 악화(34.7→32.9→30.7). 즉 **사는 것은 최악의 개선이
  아니라 평소의 멀미 감소**다. −60% 가 −57% 가 되는 것으로 「반토막을 볼 수 있나」의 답은 안 바뀐다. **채택 후보 아님 — 지평 창으로 봐도 같다.**
  ⓕ **[보강] 「54년이 아니라 최근 30년이면?」 (04 §5-32 E)**: 30년만 내지 않고 **시작 연도 1972~2006 전수(35개 창)**로 재확인(§-1 ⓑ 둥근 시작일 금지).
  ★ **변동성 감소는 −7.2~−7.7% 로 창과 무관하게 거의 불변** — 창의 산물이 아니라 두 자산 상관이 1 이 아니라는 구조다.
  그러나 **낙폭 이득은 최근 창일수록 작아지고**(54·40년 +3.3%p → 26·20년 +1.3%p — −16 규칙이 이미 −48% 근처로 묶어 더 깎을 게 없다)
  **값은 비싸진다**(10년 중앙 −7.3% → 30년 −10.9% → 15년 −12.0%). ★ **10년 최악5% 는 54년에선 불변(+0.2%)인데 20년 창 −8.7% · 15년 창 −10.1% 로 악화** —
  최근 창에서는 반대 근거가 「바닥이 안 좋아진다」가 아니라 **「바닥이 나빠진다」**로 바뀐다. Calmar 개선은 90% 에서 27/35 창(중앙 +1.4%, 관문의 1/7) · 80% 는 16/35.
  **결론 불변 — 창을 줄여도 채택 후보가 아니다.**
- **★ 외부 표(SPY vs SCHD 위기 7구간) 대조 (2026-09-03, 소유자 「이건 뭐야? 우리가 조사한 거랑 다른 건가?」 · 04 §5-31 E)**:
  전부 재현했다. **표는 신뢰할 만하고 우리와 모순이 아니다** — 다른 것은 셋. ⓐ **가격수익(배당 제외)**이라 우리 총수익 측정과 그 기간 배당만큼 어긋난다
  (중국 1.8%p vs 배당 1.9 · 인플레 2.6 vs 2.8 — 기준 차이지 오류 아님). ⓑ **코로나 행 종료일은 오기** — 03-20 으로 재야 SPY −31.8%·SCHD −31.1% 로 표와 일치한다
  (03-30 이면 −22.0%). ⓒ **비교 대상이 SPY 다** — 같은 닷컴 창에서 **QQQ 는 −72.9%**(SPY −34.8%)로 두 배 넘게 빠졌다.
  ★★ **2011 이전 세 행(닷컴·금융위기·유럽재정)은 펀드 실적이 아니라 지수 백테스트**다(SCHD 상장 2011-10-20 · 유럽재정 행조차 상장 2개월 전).
  그 지수는 **2011 년에 설계됐다** — 「데이터를 다 본 뒤 만든 규칙」의 지위.
  ★ **그래서 2008 만 갈리고, 그 갈림이 §5-31 Q-a 의 답이다**: 표의 백테스트 **−41.7%** vs 저장소 대리 **−51.3%** vs **실재한 DVY −51.3%**(소수점까지 같다) ·
  SPY −46.1% · QQQ −43.8%. **닷컴에서는 표 +15.6% 와 우리 대리 +14.4% 가 거의 같다** — 대리는 그 시대엔 잘 맞는다. 2008 차이는 **퀄리티 스크린이 은행을
  걸러냈겠는가** 하나인데 2007 년 은행은 배당 이력도 길고 ROE 도 높았다. 겹침 구간이 0 이라 **검증 불가 → 보수적인 쪽(대리)을 계속 쓴다.**
  04 §5-16 A 의 「배당은 GFC −59.7%」 줄에 **그 값이 대리에서 나온다는 주의**를 달았다. 방어 배당 40% 는 애초에 「2008 형에 같이 빠진다」는 전제 위에 정해졌고,
  그 전제가 비관 쪽이었다면 **더 안전한 쪽으로 틀린 것**이다. **전략 변경 0.**
- **★ 「미국 강세장 끝났다?」 외부 예측 심판 (2026-09-03, 소유자가 가져온 유튜브 요약 · 04 §5-33)**: `research/forecast_check.py` · 예측 4개 결과 전 등록 · **전략 무접촉**.
  ⓐ **CAPE 예측력을 산포로**: 이후 10년 실질 CAGR = 26.0 − 7.1×ln(CAPE) · **R² 0.29** · 잔차 4.3%p · CAPE 38 에서 **95% 구간 −8.4%~+8.7%(폭 17%p)**.
  ★ **골드만 3% 와 JP모건 6.7% 의 차이 3.7%p 는 그 구간의 1/5 도 안 된다 — 데이터로 두 예측을 구별할 수 없다.** 둘 다 모형과 양립한다.
  ⓑ **고평가 증거는 사건 하나**: CAPE 35~45 시작 창의 독립 국면은 **1개(1998~2001)** — 「CAPE 38 이니 3%」는 닷컴 하나에서 뽑은 외삽이다(§5-26 과 같은 한정).
  ⓒ **저수익 10년이 실제로 왔을 때**(엔진 1972~ · 비중첩 4.1): S&P 10년 ≤3%/년 창에서 S&P −0.6% · NDX1배 −4.1% · **2배보유 −21.0%** · **B +13.2%**(최소 +4.9% · B>S&P 100%).
  ⚠ 단 그 구간은 **국면 1개(2000년대)** — 기전의 확인이지 확률이 아니다. S&P 10년 ≤3% 자체가 역사에서 **7%** 뿐.
  ⓓ 인용 확인: 「100년 평균 11%」 → 실측 명목 **10.5%**(실질 7.4%) · 「CAPE 38 = 상위 3%」 → **상위 2.2%(맞음)** · 「상위10개 40%」 → **확인 불가**(지수 구성 자료 없음).
  ★ **영상의 결론(대응 말고 대비)은 저장소 규약과 같다**(§5-6 선행 경보 부재 · §4 국면 적응 Oracle +288% vs 실시간 0~−28%). **다만 그 유튜버는 1배 무신호이고 B 는 2배+전환** —
  저수익 10년에서 1배는 견디면 되지만 2배 매수보유는 −21%/년이었고 B 가 +13%/년인 이유는 **버텨서가 아니라 −16% 에서 나갔기 때문**이다.
  ★ **가장 정직한 약점**: 집중도 논거는 우리 자산을 정확히 겨눈다 — §5-31 의 QQQ 내재 배합이 **기술 63+통신 17+경기소비 16 = 96%**. B 의 방어는 분산이 아니라 전량 이탈이고,
  그것이 안 통하는 유일한 형태가 **니케이형(§5-26 0.52배)**이다. **전략 변경 0.**
- **★ 공격 다리 분산 — 「선형이 아닌 분산효과」의 상한 (2026-09-03, 소유자 「수익은 오르는데 변동성이 더 줄 보험은 없나」 · 04 §5-34)**:
  `research/attack_diversify.py` · 예측 5개 결과 전 등록 · **전략 변경 0 · 재제안 금지 추가(공격 다리에 금·국채·현금·방어바스켓 상시 배합, 어떤 비율이든).**
  ⓐ **왜 SCHD 는 선형이었나 — 수학**: σ² = w²σ₁²+(1−w)²σ₂²+2w(1−w)ρσ₁σ₂ 이므로 **볼록성은 ρ 가 낮을 때만** 생긴다. QQQ↔SCHD ρ = +0.71.
  ⓑ **분산 보너스 실측(같은 변동성을 현금으로 만들 때 대비)**: **금 +1.20%p** · 배당 +0.62 · 방어바스켓 +0.14 · **국채 −1.03%p(현금보다 못하다)**.
  ★ 상관이 가장 좋은 국채(−0.19)가 음수인 이유 — 자체 수익 1.31% 가 T-bill 4.57% 보다 낮다. **낮은 상관만으로는 부족하고 자체 수익이 같이 필요하다.**
  ⓒ **★ 금이 관문 ①③ 을 통과했다**(QLD 70%: Calmar +21.9% · 4블록 4/4 · ② 는 −26.8% 실패) → §-1 ⓐ 반증 실행 → **시대에서 무너졌다**:
  배수비 1972~80 **1.32** · 1981~99 **0.26**(금 자체 −3.66%) · 2000~12 **1.26** · 2013~26 **0.50**. 금이 이기는 구간 = 금이 강세인 구간.
  ★★ **그 과정에서 관문 ②의 존재 이유가 가장 선명하게 드러났다** — 1981~99 에서 금 배합은 **부가 1/4 로 줄었는데 MDD 가 얕아져 Calmar 은 더 높다.**
  **Calmar 은 부를 파괴하면서 낙폭을 줄이는 것도 보상한다.** ①만 보면 속고 **부의 바닥을 재는 ②가 그것을 잡는다.** 앞으로 ① 통과 후보는 이 사례를 먼저 볼 것.
  ⓓ **★ 소유자가 찾는 비선형은 이미 갖고 있다**: 같은 자산으로 **2배 계속보유 17.20%/45.5%/−98.8%** vs **B 25.26%/33.5%/−60.5%** —
  **수익 +8.06%p 이면서 변동성 −12.1%p · MDD +38.3%p**. 정적 배합은 항상 같은 비율이라 직선 위를 움직이고, **전환은 상태에 따라 0/100 을 바꾸므로 곡선 밖으로 나간다.**
  **정적 분산의 최대 보너스(+1.20%p)는 전환이 만든 +8.06%p 의 1/7.** 「획기적 방어」가 없는 이유 넷: σ₁ 지배 · 낮은 상관+자체수익을 동시에 가진 자산 부재 ·
  진짜 볼록한 옵션은 분산위험프리미엄이 음수(문헌 기각) · 부분 노출은 §0-3 기각(2배에서 MDD −80~86%). 예측 P3 **부분 틀림**(금이 ①을 넘을 줄 몰랐다).
- **★ [조건부 설계] 「MDD 를 50% 까지 낮춰야 한다면」 (2026-09-03, 소유자 질문 · 04 §5-35)**: `research/mdd_target.py` · 판정 기준(20년 p05 최대) 결과 전 등록 ·
  **전략 변경 0** — 목표를 바꿀 때만 쓰는 표다(§7 Q6 실행판). 54년 MDD ≥ −50% 를 **간신히** 만족하는 지점끼리 비교.
  ⓐ **답: 공격 다리의 20% 를 빼는 것이 가장 싸다.** 금 20%(p05 **0.84배**) > 배당 32%(0.79) > **현금 20%(0.78)** > 방어바스켓 24%(0.73) >
  국채 22%(0.62) > **배율 1.55배(0.54)** > **문턱 −11.5%(0.49)**.
  ⓑ ★ **예측 두 개가 틀렸다** — **P1 「배율 낮추기가 가장 싸다」 틀림**(8개 중 8위). 낙폭은 배율에 비례해 줄지만 **공격 상태인 82% 의 시간에서 복리가 통째로 깎인다**;
  배합은 20% 만 옮기고 나머지 80% 는 2배 그대로라 상방을 덜 잃는다. **P3 「금이 p05 에서 진다」도 틀림**(금이 1위).
  ⓒ **금 1위에는 §5-34 반증이 그대로 붙는다**(1981~99 부 0.26배) — **시대 의존이 없는 최선은 현금 20%(0.78)이고 금과 6%p 차이뿐**이다.
  즉 금의 초과분이 곧 「금 강세 시대의 몫」이다. 견고한 답을 원하면 현금 20%.
  ⓓ **문턱을 얕게(−11.5%)는 최악** — p05 0.49배에 전환이 **140 → 272회**. 「더 빨리 팔면 되지 않나」가 깨지는 지점이고 §5-6 「얕은 컷은 고배율일수록 더 죽는다」와 같은 기전.
  ⓔ **값 한 문장**: MDD −60.5% → −50% 의 값은 **20년 최악 성적의 20~22%**(34.7 → 27.1배) · **10년 중앙 10.0 → 7.9배**.
  54년 최종배수 0.24배는 CAGR 3.2%p 를 54제곱한 것이라 지평이 짧으면 훨씬 작다.
  ⚠ 제약이 **54년 전 구간** 기준이다 — 지평이 10~20년이면 제약도 그 창에서 걸어야 하고 필요한 조정이 더 작아진다(15년 창 현행 MDD 는 이미 −44.8% · §5-32 E).
- **★ 신호 커밋 push 경쟁 정정 (2026-09-03 실사고)**: 09-03 새벽 v190 마감 전 슬롯 둘(04:37·04:48 KST)이 **종가는 정확히 잡고**(마감 23초 뒤
  709.24) `git push` 에서 실패 — 러너가 체크아웃한 뒤 마감까지 23분 기다리는 사이 **내가 연구 커밋을 main 에 push** 해서(19:54Z) 봇의 push 가
  non-fast-forward 로 거부됐다. 예비 05:27 슬롯이 살렸지만 그날 「5분 안」은 깨졌고 새벽에 실패 카톡 2통이 갔다. **v190 이 대기 시간을 25분으로
  늘리면서 생긴 경쟁 창**이고, 파수꾼 커밋 스텝(v140)에는 있던 `git push || { git pull --rebase --autostash origin main && git push; }` 가
  daily-signal.yml·monthly-stats.yml 엔 없었다 → 둘 다 넣었다. 사람 커밋은 문서·연구, 봇 커밋은 data/ 라 rebase 충돌은 없다.
  ⚠ 교훈: **장시간 대기하는 작업의 커밋 스텝은 반드시 rebase 재시도**. 그리고 04:30~05:10 / 05:30~06:10 KST 에 main 에 push 하는 것은 이제
  실패를 만들지 않지만, 그 시간대의 push 는 여전히 봇 커밋과 교차한다 — 급하지 않으면 피하라.
- 그 외: 시장 시계, 낙폭 게이지, 내 포트폴리오, 계산기, 성과 비교표,
  ISA 가이드, 위기 궤적, T4 패널, 신선도 경고, 수동 입력 백업.

## 5. 알려진 공백 (개선 환영 영역)

폰↔PC 자동 동기화(무서버 한계 — v93 파일 백업으로 수동 이동만 보완) ·
카톡 알림에 매매 금액 미포함 — 남은 공백은 이 2개뿐.
**둘 다 뿌리가 같다**: 보유 수량은 설계상 기기의 localStorage 에만 있고 서버가 모른다.
알림을 만드는 GitHub Actions 는 그 값을 볼 수 없으므로 금액을 넣으려면 보유를 서버에
올려야 한다 — 그건 이 시스템의 「사용자 데이터는 localStorage 만」 전제를 깬다.
**금액은 화면의 「오늘의 행동」이 대신 계산한다** (알림 → 화면 열기 → 수량·금액).
그래서 이 2개는 「자동화 실패」가 아니라 **의도한 경계**다 — 재제안 전에 이 문단을 볼 것.

**운영 루틴 (공백 아님 · v140 에서 자동화 — 사람이 돌리지 않는다)**: 전제 감시(느린
변수 4종: 지수 10y/20y CAGR·3y 변동성·2배 드래그) + 4다리 AUM + 체결비용 진행률을
**자동 파수꾼이 매주 월요일** 돌려 `data/ops_check.json` 에 남기고 화면이 읽는다.
Level 규약·밴드는 `research/SURVIVAL_MONITOR.md` §F (Level 3 「역사 범위 밖」은
verify_all I10 P2 가 이미 자동 검사 — 이 루틴은 주의 단계용). 선행 예측형 경보는
검증 결과 존재하지 않음(04 §5-6) — 수준 감시만 유효. **밴드·판정 규약은 v140 에서
바뀌지 않았다** — 관측 주기(분기→주)만 촘촘해졌고, 대응은 그대로 「지켜본다」이며
알림은 상태가 **나빠졌을 때만** 나간다.
**단 하나의 예외가 v177 월간 생존 알림이다** — 거기서는 **침묵이 곧 고장 신호**라서
정상일 때 말해야 뜻이 생긴다. 이 예외를 「알림 원칙 위반」으로 오해하고 지우지 마라.
(종결된 항목 — 방어 재조정 기준일: v117 확정·해소, §4 참조. 스크린리더 실기기 확인:
실측 불필요 — 소유자 확인(2026-08-30, 시각장애 없음), v101 aria 기본 반영으로 종결.)
