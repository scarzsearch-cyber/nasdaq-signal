# 설치·실행 재현성 감사 (2026-09-06)

> 기준 97cc391. **깨끗한 격리 체크아웃**(`git clone --branch main` · 스크래치 폴더 · 작업트리와 별개) + **임시 가상환경**(`python -m venv` · 사용자 전역 환경 무변경)에서
> 문서(README · deploy/README · CLAUDE §3 · 워크플로)에 적힌 의존성과 실행 순서만으로 주요 검사·빌드가 되는지 봤다.
> 실제 운영 데이터 갱신·배포·알림을 일으키는 명령은 클론 안에서만(또는 셀프테스트로) 돌렸다. 회귀: `audit/test_repro6.py` R1.

## 0. 문서에 적힌 의존성·순서
- 의존성 파일(`requirements.txt`·`pyproject`) **없음**. 워크플로가 `pip install pandas numpy`(미고정) · Python 3.12(price.yml 만 3.11). README 에 pip 안내 줄 없음(워크플로가 유일한 기록).
- 실행 순서: `python verify_all.py --fast` → 전체 → `deploy/*.py --selftest`(README) · CI = verify.yml unittest 16모듈 + `research_kit.py`.
- 환경: 로컬 Python 3.12.10 · numpy 2.4.2 / pandas 3.0.5 · 클론 venv 는 최신 pip 설치 → numpy **2.5.2** / pandas 3.0.5(CI 도 미고정 최신).

## 1. 격리 클론 실행 결과
| 단계 | 명령(클론 루트 · venv) | 결과 |
|---|---|---|
| 빠른 검증 | `verify_all.py --fast` | 실패 0 · 3초 |
| 전체 검증(I14 셀프테스트 16종 포함) | `verify_all.py` | 실패 0 · 8초 |
| CI unittest 16모듈 | `python -m unittest …`(verify.yml 목록) | 232 OK · 20초 |
| 설계 가드 | `research_kit.py` | PASS |
| deploy README 셀프테스트 | `wait_close`·`nav_collect`·`watchdog --selftest` | 전부 PASS(67경우 등) |
| cp949 콘솔(PowerShell 5.1 · PYTHONIOENCODING 없음 · stdout cp949) | `verify_all.py --fast` · `watchdog --selftest` | rc 0 — `sys.stdout.reconfigure` 가 있는 진입점은 문제 없음 |
| French 원자료 추출본(`data/hist/ff_tmp/` · gitignore) | 첫 실행 시 zip 에서 자동 생성 | ✅ 미추적 파일 의존 없음 |
| Git Bash 경로(Windows) | 회귀 모듈이 `C:\Program Files\Git\...bash.exe` 후보로 찾는다 | ✅(test_ops_recovery3·watchdog_chain4 통과) |

## 2. 재현성 — 생성물 재생성 vs 커밋본(클론 안에서만)
| 생성기 | 대상 | 차이 |
|---|---|---|
| `deploy/build_stats.py` | `data/strategy_stats.json` · `data/signal.json`(성과 사본) · `01_Strategy_Logic.md` AUTO-STATS | **generated_at 만**(수치 0건 차이 · JSON 전수 비교) |
| `research/axis_isa.py --emit` | `data/isa_stats.json` | 차이 0 |
| `research/emit_dd_distribution.py` | `data/dd_percentile.json` | 차이 0 |
| `deploy/kr_holidays.py --emit` | `data/kr_holidays.json` | **내용 차이**(§3 R2) |

## 3. 발견·수정
| # | 발견 | 재현 | 수정 · 검사 |
|---|---|---|---|
| **R1** | `verify_all.py` 가 **작업 디렉터리에 의존**한다 — 다른 cwd 에서 `python <경로>/verify_all.py --fast` 를 부르면 `data/…` 가 전부 「없음」으로 읽혀 **경고 12건 · 종료 1**(실패-폐쇄라 조용히 통과하진 않지만 원인이 안 보인다). research 스크립트는 경로 보정 3줄(README 「어디서 실행하든」)이 있어 다른 cwd 에서도 돈다(`axis_isa.py` 실측) | 스크래치 루트에서 실행 → rc 1 · `hist_data.build_ext()` 도 다른 cwd 에서 FileNotFoundError | `main()` 첫 줄에서 파일 디렉터리로 `os.chdir` — CI(루트 실행)·`gate_mutation`(클론 루트 cwd) 무영향 · 수정 후 다른 cwd rc 0 · **`test_repro6.R1`** |
| **R2** (미수정 · 관찰) | `data/kr_holidays.json` 커밋본이 **현행 생성자 코드와 어긋난다** — 코드 18d8be7(2026-09-04 · 근로자의날→노동절 · 노동절·제헌절 대체공휴일)이 산출물 1d68e1c(같은 날 앞) 뒤에 바뀌었고 재생성 커밋이 없다. 재생성하면 이름 6건 + `2027-05-03 노동절 대체` 추가 | 클론에서 `--emit` → 14줄 diff | **수정하지 않았다**(운영 데이터 갱신) — 설계상 **주간 파수꾼(월 09:10 KST)이 `--emit` 후 차이가 있으면 커밋**한다(v195). 월요일 09:52 관찰에 「kr_holidays.json 커밋 여부」를 추가. 화면 영향은 2026-05-01 라벨과 2027 실행일 계산뿐 |
| R3 (기록) | `open()` 에 encoding 이 없는 곳 1(`audit/test_ops_recovery3.py:761` · 임시 signal.json 읽기) — cp949 로컬에서도 통과(ASCII 내용)라 미수정 | — | 기록만 |

## 4. 미확인·한계
- Linux/macOS 셸에서의 재현은 CI(ubuntu)가 대신한다 — 로컬 Windows + Git Bash + PowerShell 두 콘솔만 직접 쟀다.
- 의존성 미고정: numpy 2.4.2(로컬)와 2.5.2(클론) 모두 통과했지만 **버전 고정 파일이 없어** 미래 버전에서의 재현은 보장하지 않는다(대규모 업그레이드·핀 도입은 이번 범위 밖 · 제안 §5).
- `gate_mutation.py`(약 4분) · `audit_full.py` 는 클론에서 돌리지 않았다(예약 CI 스텝이 담당).
- 브라우저 정적 서버(README 없음)는 파이썬 `http.server` 로 충분했다 — 문서화는 제안.

## 5. 제안
- `requirements.txt`(pandas·numpy 두 줄 · 버전 범위)와 README 「설치」 3줄 — 워크플로 밖 첫 기록. 핀 값은 CI 와 같이 미고정 유지 여부를 소유자가 정한다.
- README 137행의 「어디서 실행하든」은 research 스크립트 얘기다 — `verify_all.py` 도 이제 같다는 한 줄 추가 여부.
