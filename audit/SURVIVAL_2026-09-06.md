# 자동화 생존·감시 실패 검증 (2026-09-06)

> 기준 7af2939 · 작업트리 `review/doc-claims-10`. 실제 자동화 비활성화·secrets 변경·카톡 발송 없음. 회귀 `audit/test_survival5.py`(13 · 격리 대역).

## 1. 60일 무활동 규칙 — 공식 문서와 실행 이력을 분리
| 출처 | 확인된 것 | 미확인 |
|---|---|---|
| 공식 문서 「Disabling and enabling a workflow」(2026-09-06 열람) | 「In a public repository, scheduled workflows are automatically disabled when no repository activity has occurred in 60 days.」 | **「repository activity」의 정의 없음** — 누구의 커밋이 활동인지, 봇 커밋이 세지는지 문서에 없다 |
| 공식 문서 「Events that trigger workflows › schedule」 | 같은 문장 + 「write 권한 사용자가 cron 을 수정하면 비활성 워크플로가 재활성화」 | GitHub Actions/봇 커밋의 효과는 **언급 없음** |
| 이 저장소 실행 이력(`gh api …/actions/workflows`) | 워크플로 8종 전부 `active` · 저장소 생성 2026-08-25(12일) · 최근 200커밋 중 사람 193 / 봇 7 | 60일 사건이 아직 없고 소유자 커밋이 매일 있어 **봇 커밋만으로 유지되는지 실증 불가** |

결론: 규칙은 실재(공식). 봇 커밋의 효과는 **미확인**으로 둔다(CLAUDE v176 ⓕ·v177 과 같음). 실증 방법은 「사람 커밋 60일 부재」뿐이라 이 저장소에선 사실상 못 잰다.
복구 절차는 문서상 「Actions 탭 Enable」 또는 cron 수정 커밋(사람) — 둘 다 사람의 개입이다.

## 2. 감시 대상과 감시 도구가 같은 이유로 멈추는 경우
- 일일 신호·시세·월간·파수꾼이 **전부 예약 실행**이다. 예약이 통째로 꺼지면(60일 규칙·Actions 장애) 파수꾼도 같이 꺼진다 → 카톡·이슈 어느 것도 안 나간다.
- 남는 신호는 **월 1회 생존 카톡이 안 오는 것**(v177 · heartbeat 모드)뿐 — 「침묵이 정보」인 유일한 알림. 회귀 S3 가 그 모드·스텝의 존재를 고정한다.
- 화면 쪽 보조 신호: 신호 신선도 배너(3거래일)·시세 「N분 전」·자동 검증 배지 — 전부 사람이 화면을 열어야 보인다.
- **제안(도입 안 함 · 외부 서비스)**: 저장소 밖 무료 감시(예: 정적 사이트의 `data/signal.json` `as_of` 를 외부 uptime 서비스가 하루 한 번 읽어 3일 낡으면 메일) 또는 두 번째 예약원(다른 저장소/계정의 cron 이 `gh workflow run` 으로 이 저장소를 깨움). 둘 다 「볼 곳이 둘」이 되는 대가(v173 소유자 판단)라 제안만 한다.

## 3. 경고 경로 격리 검증 (`audit/test_survival5.py` · 실제 발송 0)
| 경우 | 경로 | 결과 |
|---|---|---|
| 카카오 refresh 토큰 만료(HTTP 401) | `kakao_keepalive.main` rc 2 → `watchdog channel` 이 죽은 채널로 판정 → `alert=1`(이슈) + 살아 있는 채널로 「알림 채널 이상」 | ✅ |
| 연명 처리 예외 | 같은 경로(`카카오톡(RuntimeError)`) | ✅ |
| secret 불완전(키만·토큰만) | 「설정 불완전」 알림 + alert | ✅ |
| 채널이 하나도 없음 | 주간 슬롯만 이슈(alert) · 평일은 침묵(설계 v200) · notify 호출 0 | ✅ |
| 정상 | 무발송·alert 없음 | ✅ |
| refresh 토큰 회전 + **GH_PAT 없음** | 긴급 경고 카톡 발송 → rc 2(스텝 실패로 드러남) · 같은 잡 뒤 스텝에 새 토큰(GITHUB_ENV) 전달 | ✅ |
| 회전 + GH_PAT 있으나 secret 저장 실패 | 긴급 경고 → rc 2 | ✅ |
| 회전 + 저장 성공 | 무발송 rc 0 | ✅ |
| 긴급 경고 발송까지 실패 | rc 3(0 아님) | ✅ |
| 이슈 여는 워크플로의 권한 | verify·watchdog·daily-signal 전부 `issues: write` 선언 · 저장소 기본 토큰 권한 `write`(API) | ✅ 정적 |
| 예약 누락 감시 | stale(신호 3거래일)·stats(45일)·price(3거래일)·heartbeat 모드가 스텝에 있고 cron 08:40 평일 / 09:10 월요일 | ✅ 정적(값 검증은 셀프테스트 67경우) |

## 4. 발견·수정
- 새 결함 없음. 경고 경로는 전부 설계대로였다. 수정 0 · 회귀 13개 추가(verify.yml 등재).

## 5. 한계
- 60일 규칙의 봇 커밋 효과·실제 비활성화 시 이메일 도착은 미확인(실증 불가). 예약 실행이 통째로 꺼지는 사건은 격리로 재현할 수 없다.
- 카톡 도착 자체는 자동 검증 불가(v178).

## G. 통합
- (push 뒤 기입)
