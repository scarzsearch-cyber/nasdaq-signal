# 모바일 미검증 흐름 · bare 클론 간헐 실패 · 신선도 공용 정의/표류 라벨 (2026-09-06 · v225)

> 기준 51908c9 · 별도 작업트리 `review/mobile-9`. 소유자 우선순위 ①②③. research·F계열·전략 계산·동결값·실측 장부·원자료 제외.
> 공용 파일 수정 전 `origin/main` 재확인(다른 세션 커밋 없음). 자기 변경 파일만 스테이징.

## ① 모바일 미검증 흐름 (412×915 · 안드로이드 UA 에뮬레이션 · 정적 서버 실제 로드)

| 항목 | 확인 | 판정 |
|---|---|---|
| 접힌 절 안 앵커 `guide.html#order` (routine 접힘) | 절 펼침 · 대상 표시 · 가로 412 | 정상(v224) |
| 설명서 검색 「괴리율」 / 해제 | routine 만 표시 · 목차 숨김 · 해제 시 접힘 복원 · 412 | 정상 |
| 노트 앵커 `#opsRecovery` (sea169 접힘) · 필터 삭제/신규/전체 | 대상 표시 · 삭제 4시즌 5항목 · 신규 27 · 전체 복원(sea168 접힘) · 412 | 정상 |
| 신호 `#portPanel` (패널 접힘) | 펼침 · 오늘의 행동 표시 · 412 | 정상(v224) |
| 접힌 참고 패널 3종(계좌·T4·타임머신) 펼침 | 내용 렌더 · 페이지 가로 412(궤적 SVG 는 `overflow-x:auto` 컨테이너 안에서만 넘침 · 설계) | 정상 |
| 백업 링크 `#backup=` | 살균(코드 `BAD<x>`·`<img onerror>` 날짜 체결 버림) · 확인창 「⚠ 링크로 받은 백업」· 되돌리기 저장 · 해시 즉시 삭제 · 거절 시 무변경 · 깨진 링크는 alert 만 · 오늘의 행동 재계산 | 정상 |
| CSV 불러오기 3갈래 + 헤더 없음 | 70행 → 「70행 불러옴」·판정 공격 / 40+3행 → 「40행 불러옴 · 2행 건너뜀」오류색 / MM/DD 전량 → 「불러온 행이 없습니다(2행 건너뜀)」·기존 행 보존 / 헤더 없음 → 안내 · 전체 삭제 → 「데이터 없음」 | 정상(v202) |
| 다크 모드 희귀 상태(전환일·근접·신선도 2단계·점검 todo·로드 실패+수동) | 본문 대비 5.6~15:1 · 가로 412 | 정상 |
| PWA | manifest(standalone·start_url ./·아이콘 2종 200)·theme-color 2종 | 정적 확인만(standalone 실행은 에뮬레이션 불가) |

관찰(미수정 · 취향/접근성): 점검 알약 「점검 N건」(warn) 은 다크 카드 위 합성 대비 약 3.6:1(작은 굵은 글씨) — 기능 오류 아님. 화면 스크린샷은 pane 이 뒤에 있어 타임아웃 → DOM 값으로 판정.

## ② bare 클론 간헐 실패 (원인 미확정)
- 사건: b303eb8 CI 검증에서 `test_ops_recovery3.S6.test_remote_already_has_same_close_discards_duplicate_as_success` 의 `git clone -q <임시 bare>` 가 exit 128 · 재실행 통과.
- 표본: 로컬(Windows) S6 20회 연속 OK · 러너 workflow_dispatch 3회 전부 success · 그 뒤 push 4회 success → **재현 0/27**.
- 조치: 51908c9 에서 git 헬퍼가 실패 시 stderr 를 예외에 붙이도록 계측(재시도·완화 없음). 다음 발생 때 원인 문자열이 남는다.
- 코드 검토에서 배제한 가설: 임시 디렉터리 충돌(`mkdtemp` 고유) · 순차 실행이라 테스트 간 경합 없음 · `safe.directory`(같은 사용자) · gc 자동 분리(객체 수가 문턱 훨씬 아래). 남는 후보: 러너 일시 I/O 오류 — 근거 없음.

## ③ 신선도 공용 정의 + 기저율 표류 라벨 (v225)
**시장·데이터별 의미**: 신호 `as_of` = 미국 종가일(QQQ) → 미국 달력 · 시세 `price.json` = 한국장 스냅샷 → 한국 달력(`kr_biz_days_since` 기존) · NAV 장부 = 한국장(nav_collect 기존).
**판정 vs 표시·운영 경고**: 판정(state)은 `update_signal` 이 종가로 정하며 이 변경과 무관. 바뀌는 것은 화면 신선도 점·배너·「동결 후 N거래일」 표시와 파수꾼 stale/near/heartbeat 의 **셈**뿐.

| 대상 | 종전 | 이후 |
|---|---|---|
| `deploy/watchdog.py biz_days_since` | 평일 수 | 미국 거래일 수(주말 + `wait_close.nyse_holidays` 표 — 한 곳) · 소비자 stale·near·heartbeat |
| `signal.html bizDaysSince(iso, now?)` | 평일 수(미국 장중 오늘 제외) | + `usHolidays` 제외(시계·실행일과 같은 표) · `now` 주입은 검사용 |
| 문구 | 「n영업일째」·「미국 휴장이 겹쳤다면 정상」 | 「n거래일째(미국 휴장 제외)」·「미국 정기 휴장은 세지 않은 값(특별 휴장이 겹쳤다면 정상)」 |
| 재현 근거 | 노동절 주: 화요일 08:40 에 금·월·화 = 3 → 문턱 도달(누락 1일) · 화면은 화요일에 노란 점 | 화 1 · 수 2 · 목 3(문턱) — 실제 누락 2세션에서만 |

- 회귀: 파수꾼 셀프테스트 +6(61→67 · 노동절·독립기념일 관측일·토요일 신정) · `test_watchdog_chain4` W7(옛 사본 FAIL → 새 코드 PASS · `us_holidays == wait_close.nyse_holidays`) · 화면은 `now` 주입으로 6경우 실측(화 1·수 2·목 3·정상 주 2·미국 장중 제외 1·독립기념일 2).
- 특별 휴장(애도일)은 표에 없어 하루 이르게 세는 쪽 — 보수적. 문턱 3·주기 3 은 그대로.
- **표류 라벨**: 평가기 rc 2(기저율 표류) → verdict `drift`(신규 · `PB_RANK` 1 = warn/error 와 같은 등급이라 악화 알림 규칙 동일) · 화면 PBV 「기저율 표류 — 재등록 필요」(종전 「평가 실패」). W1 표류 검사에 verdict 단정 추가(옛 코드 FAIL). 평가기(research)는 무변경.

## 인계·한계
- 신선도 정의 변경은 파수꾼·화면 두 곳의 공용 규약이다(문서 §4 v140 「정기 휴장 오경보 없음」 논거는 이제 달력 반영으로 대체). 아스트라 쪽 도구가 `biz_days_since` 를 쓰면 미국 달력으로 바뀐 것을 알 것.
- `ops_check.json` 의 `protocol_b.verdict` 어휘에 `drift` 추가 — 다른 소비자가 있으면 알려 달라.
- ② 는 미확정. ①의 PWA standalone 은 정적 확인뿐.

## G. 통합
- 커밋 **9f3d6fb**(v225) → **ecd9fa2**(Node 하네스 정정). 9f3d6fb 의 CI 검증은 내가 로컬에서 CI 회귀 목록 전체를 안 돌려 `test_ops_review2` Node 하네스(서명 추출)가 깨진 채 밀렸다 — ecd9fa2 에서 서명·휴장 경우를 맞추고 219개 전체를 로컬에서 돌린 뒤 push. CI 검증·Pages 둘 다 success. 배포본에 v225 신선도 코드 반영 확인(usHolidays 제외 줄 1건).
