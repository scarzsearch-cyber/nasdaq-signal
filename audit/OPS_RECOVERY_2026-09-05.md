# 운영 자동화 장애·재시도·복구 통합 검증 (2026-09-05 · 3차)

> 기준 **92cceab**(v221 교차검증 뒤 최신 main) · 별도 작업트리 `review/ops-recovery-3`.
> 목표: 함수별 검사는 통과하지만 **연결하면** 실패하는 상황. 방법: 가짜 시계·고정 응답·임시 디렉터리·
> 로컬 bare 원격에 실제 실패를 주입하고 호출 순서·파일 상태·종료코드·다음 재실행까지 본다.
> 실제 알림·토큰 회전·운영 장부·원자료 변경으로는 시험하지 않았다. 회귀 검사: `audit/test_ops_recovery3.py`
> (21개 · 수정 전 92cceab deploy 사본에서 **S1·S2·S4 6개 실패**, 수정 뒤 21/21 · `OPS_RECOVERY3_OLD` 로 재현).
> 이 문서는 6개 시나리오의 연결 검사 결과다 — 저장소 전체 검토 완료를 뜻하지 않는다.

## 발견한 실제 장애 → 사용자 영향 → 수정 → 재시도 복구 증거

| # | 시나리오 | 실제 장애(연결 경로) | 사용자 영향 | 수정 | 증거 |
|---|---|---|---|---|---|
| S1 | 신호 생성 도중 실패 | `update_signal.main` 이 qqq.csv 를 쓴 뒤 signal.json 을 `open('w')`+`json.dump` 로 썼다. 직렬화·디스크 오류가 그 사이에 나면 **signal.json 이 빈 파일**이 된다(CSV 는 새 날짜). 같은 실행의 `oos_log`·`signal_alert`·화면이 마지막 성공 판정까지 잃는다. | Actions 에서는 wait_close 가 빈 as_of 를 「없음」으로 읽어 240초 뒤 재시도하고 커밋은 성공한 뒤에만 나가므로 화면까지 번지지 않았다. 로컬 수동 실행·같은 실행 안의 소비자는 판정을 잃는다. | 두 산출물을 **임시파일 → os.replace** 로 교체(`_atomic_write_text`). 직렬화를 먼저 끝내고 교체한다. | 수정 전: `signal.json == b''`(검사 실패). 수정 후: 바이트 동일 보존 · CSV 만 앞선 상태에서 `oos_log` 는 원천 대조로 거부(장부 무기록) · 다음 갱신이 JSON 을 따라잡고 장부 1행 · 재실행 no-op · 교체 실패 시 임시파일 잔류 0(허용 목록 밖 변경 없음). |
| S2 | 종가 확인 실패 | `wait_close.main` 은 예상 종가일(meta) 조회가 계속 실패하면 갱신이 **이미 끝났어도** 240초마다 갱신을 반복하다 170분 뒤 exit 1. 그 실행의 갱신분은 커밋되지 않고 **거짓 실패 카톡**이 간다. 이미 최신인 신호도 같은 경로로 실패 종료. | 야후 meta 엔드포인트만 죽은 날 새벽에 실패 알림 · 다음 슬롯이 다시 계산(신호 자체는 손상 없음). | 갱신 결과가 **원천 meta 로 마감을 확정**한 경우(source yahoo/yahoo2/naver · 정규형·비미래)에만 예상일 없이 성공 종료(`certified_as_of`). 캐시 폴백(source=cache)·갱신 실패(rc≠0)는 그대로 실패-폐쇄. | 수정 전: 두 검사 SystemExit(1)(43회 반복). 수정 후: 1회 갱신 뒤 정상 종료 · 캐시 폴백은 여전히 exit 1 · 갱신 실패도 exit 1 · 예상일이 있는 옛 신호도 거부(보존). |
| S4 | 가격 불변 | `price_poll.cycle` 이 값이 같으면 발행을 생략해 브랜치 `as_of` 가 마지막 **변화** 시각에 멈췄다. 화면 「N분 전」으로 조용한 장과 멈춘 폴러를 가를 수 없었다(파수꾼 `price` 는 3영업일 뒤에야 알림). | 장중 값이 오래 같으면 화면이 「40분 전」처럼 보여 수집 정지로 오인하거나, 반대로 폴러가 죽어도 같은 모양이라 눈치채지 못한다. | 값이 같아도 **30분(REPUBLISH_S)** 이 지나면 다시 발행. `as_of` 는 이제 마지막 **확인** 시각(최대 30분 양자화). 브랜치 상태를 `{'items','published'}` 로 읽는다(`branch_state`). | 수정 전: 새 인자 없음(구별 불가). 수정 후: 5분 뒤 생략 · 31분 뒤 발행 · 값 변화 시 즉시 발행 · 브랜치 시각 없음/브랜치 없음이면 첫 사이클 발행. 발행 수 상한 +12/일. |

### S2 보완 (통합 담당 지적 · e1c9103 반례 → 수정)
- **반례**(재현됨): 현재 시각 2026-09-05(토) UTC 에서 Yahoo 일봉과 meta 를 모두 09-02 마감으로 주면 `_parse_yahoo_result()`(마지막 봉 = meta 의 마감 세션) 와 v222 의 `certified_as_of()`(source·비미래) 가 둘 다 통과해 09-03·09-04 종가가 빠진 신호를 「반영 완료」로 끝냈다. 별도 meta 조회가 살아 있어도 그 meta 가 같이 뒤처지면 종전 코드도 「이미 최신」으로 조용히 성공했다 — **source 와 비미래 날짜는 최신성의 증거가 아니다.**
- **수정**(`deploy/wait_close.py`): 원천과 무관한 **NYSE 달력**으로 「마지막으로 마감된 세션」을 독립 계산한다(`nyse_holidays`·`latest_closed_session` — signal.html `usHolidays()` 와 같은 규칙: 신정 토요일 미관측·관측일·굿프라이데이·Juneteenth 등, 정규 마감 16:00 ET 를 pandas 시간대로 환산해 서머타임 자동). 세 경로 모두 이 달력에 뒤처진 as_of 를 성공으로 인정하지 않는다: ① 예상일 없음 → `certified_as_of` 에 달력 조건 추가 ② 예상일 있음 → 예상일 자체가 달력보다 뒤처지면 「원천 지연 의심」으로 갱신 재시도 ③ 장중 → 직전 마감이 빠져 있으면 갱신 시도(종전엔 무조건 「직전 종가가 최신」). 시한 초과 시 「최신성 확인 불가(원천 지연·특별 휴장)」와 「수동 확인 필요」를 문구로 가른다. 주말·휴장·장 시작 전은 달력이 흡수한다(토요일→금요일, 노동절 월요일→금요일, 화요일 장 시작 전→금요일, 조기마감일은 보수적으로 16:00 까지 미마감).
- **증거**: 생산자 파서(`update_signal.fetch → _parse_yahoo_result`, 고정 chart JSON)부터 대기 루프(`wait_close.main`, 가짜 시계)까지 연결한 회귀 7개(`S2b_*`). e1c9103 deploy 사본에서 3실패+1오류(반례 통과·원천 전체 지연 조용한 성공·장중 누락 미갱신·달력 함수 없음) → 수정 뒤 28/28. 달력 검사: 2026 휴장일 10종 · 2022 토요일 신정 미관측 · 마감 경계(EDT 20:00Z·EST 21:00Z) · 노동절·장 시작 전·조기마감일.
- **한계**: 특별 휴장(국가 애도일 등)은 달력이 모른다 — 그날은 원천이 「전날 마감」을 말해 「원천 지연 의심」으로 실패-폐쇄하고 다음 슬롯이 다시 본다(조용히 낡은 신호를 최신이라 하는 것보다 낫다). 원천이 뒤처진 날엔 갱신을 시도해도 같은 값이라 시한까지 반복한 뒤 실패 알림이 간다(의도한 「확인 불가」 상태). 파수꾼 `stale` 은 여전히 평일 3일 기준의 2차 안전망이다.
- **S1 정정**(통합 담당 지적 반영): S1 의 원자 교체는 **각 파일의 쓰기 중 손상 방지**이지 qqq.csv 와 signal.json 의 **동시 갱신을 보장하지 않는다.** 두 파일 사이에서 죽으면 CSV 만 앞선 상태가 남으며, 그 상태는 `oos_log` 원천 대조(장부 무기록)와 다음 갱신(JSON 이 CSV 를 따라잡음)으로 수습된다 — 검사 `S1_*` 가 그 경로를 고정한다.
- **S4 확인**: `branch_items` 의 기존 참조는 저장소 검색에서 남아 있지 않았다(통합 담당 확인).

## 설계 선택과 실제 동작이 일치함을 확인한 것 (수정 없음)

| # | 시나리오 | 확인한 연결 동작 | 근거(검사) |
|---|---|---|---|
| S3 | 알림 부분 실패 | 카카오 분할 발송 중 한 조각이 실패하면 `notify` 는 rc 2(전체 실패) → `signal_alert` 는 성공 표시를 남기지 않음 → 다음 슬롯이 **모든 조각**을 다시 보낸다(앞 조각 중복 1회 = 「누락보다 중복」). 성공 표시 저장 실패(os.replace 예외)도 rc 2 → 다음 슬롯 재발송. 성공 뒤엔 생략. | `S3_*` 2검사 · 전송 순서·횟수·상태 파일 유무를 단계마다 확인 |
| S5 | 원자료 일부 갱신 실패 | 파일 하나(^TNX)가 실패하면 나머지 8파일은 각자 갱신되지만 `main` 은 1 → 워크플로가 `build_stats`·커밋을 건너뛴다(스텝에 `continue-on-error`/`if: always()` 없음 · 검증 뒤 커밋). 실패 파일 바이트 보존. | `S5_*` 2검사(임시 저장소 전 파일 합성 · monthly-stats.yml 텍스트) |
| S6 | 실행 경쟁 | daily-signal 「변경분 커밋」 셸을 **로컬 bare 원격**에서 그대로 실행: 원격 그대로 → push · 원격에 같은/더 새 종가 → 이 실행의 커밋을 버리고 0(오래된 결과가 최신을 덮지 않음, 다음 슬롯 reset 이 원격과 일치) · 원격이 다른 이유로 움직임 → 1(옛 체크아웃 산출물을 새 코드 위에 안 밀음) · 허용 목록 밖 파일 → 1. 예약 중첩은 `concurrency: cancel-in-progress: false` + 첫 스텝 `reset --hard origin/main` 이 막는다. | `S6_*` 5+2검사(실제 YAML 에서 셸을 뽑아 실행) |

## 실패 전후 불변조건 (검사로 고정)
- `data/signal.json` 은 어느 시점에 죽어도 마지막 성공 판정으로 파싱된다. `data/qqq.csv` 가 앞서 있어도 장부는 원천 대조로 쓰지 않는다.
- 예상 종가일이 없을 때 정상으로 인정되는 것은 「원천이 마감을 확정했고 **NYSE 달력상 마지막 마감 세션에 뒤처지지 않는** 갱신」뿐이다. 캐시·실패·미래 날짜·달력보다 뒤처진 날짜는 아니다. 예상일이 있어도 달력보다 뒤처지면 최신이 아니다.
- 실측 장부(`oos_log`·`nav_history`)는 재실행에서 같은 날 no-op(기존 셀프테스트)이며, 알림은 성공 표시 뒤에만 생략된다.
- 시세 발행 시각은 마지막 확인 시각이다(값 불변 시 최대 30분 지연). 폴러가 살아 있는 한 35분을 넘지 않는다.
- 월간 원자료는 한 파일이라도 실패하면 성과표가 발행되지 않는다. 일일 커밋은 원격이 같은/더 새 종가면 버려지고 다른 이유면 실패한다.

## 남은 한계 (수정하지 않음)
- S2 에서 예상일이 있는데 signal as_of 가 그보다 **미래**면 손상으로 보고 exit 1 이다 — 야후 meta 엔드포인트가 chart 보다 뒤처진 날의 거짓 실패 가능성. 원천 둘 중 어느 쪽을 믿을지 정할 근거가 없어 두었다. 달력 보완 뒤에도 특별 휴장일은 「원천 지연 의심」으로 실패-폐쇄한다(위 S2 보완 한계).
- S3 부분 실패의 중복은 설계다. 조각 단위 성공 표시를 두면 줄일 수 있으나 상태 계약이 커진다 — 전환일은 연 2~3회라 두지 않았다.
- S4 는 「값이 같고 폴러 생존」과 「공급원이 같은 값을 반복 반환(동결된 피드)」을 구별하지 못한다. 후자는 값 자체가 같아 화면으로는 어떤 설계로도 안 보인다.
- S5 로컬 수동 실행(`python deploy/refresh_hist.py`)에서 실패 뒤 작업트리에 남는 부분 갱신은 사람이 정리해야 한다(자동화 경로는 러너가 버린다).
- S6 의 「중복 커밋은 버린다」 경로가 뜨려면 다른 실행이 같은 종가를 먼저 밀어야 한다. 그 실행에서 NAV/OOS 만 실패했고 이번 실행이 성공했다면 그 행은 다음 슬롯까지 미뤄진다(마지막 슬롯 뒤면 다음 날 as_of 로 넘어가 OOS 하루가 빌 수 있다). 실측 빈도 0 · 예약 직렬화로 도달하기 어렵다.
- 실제 카카오 도착·GitHub 예약 지연·Pages 배포 지연은 이 검사가 재지 못한다.

## 검증
- 수정 전 92cceab deploy 사본: `audit.test_ops_recovery3` 21개 중 **6개 실패**(S1 2·S2 2·S4 2) · S3/S5/S6 통과(설계 확인).
- 수정 후: 21/21 · `update_signal`·`wait_close`·`price_poll` 셀프테스트 PASS(조용한 장 재발행 경우 추가) · `verify_all --fast`·전체 실패 0.
- S2 보완(2차 커밋): e1c9103 deploy 사본에서 `S2b_*` 7개 중 3실패+1오류 → 수정 뒤 전체 28/28 · `wait_close` 셀프테스트에 달력 검사 추가 · `verify_all --fast`·전체 실패 0.
- 화면 변경: notes.html 안내 항목 추가뿐(신호·설명서 무변경). 통합 결과는 §G.

## G. 통합
- 통합 직전 `origin/main` = 92cceab(기준과 동일 · 겹친 변경 0) → `review/ops-recovery-3` 빨리감기 push → **58bb7a7**.
- CI: 검증 run 33963526116 success(unittest 에 `audit.test_ops_recovery3` 포함) · Pages 배포 run 33963526126 success.
- 배포 사이트: notes.html 에 v222 항목·시즌 v186–v222 반영 확인(curl). 신호·설명서 화면은 이번에 바뀌지 않았다(도장 v220 유지).
- 소유자 작업 폴더(main)는 깨끗한 92cceab 상태였고 손대지 않았다 — `git pull` 로 빨리감기된다.
- **S2 보완 통합**: e1c9103 → **d77aab2** 빨리감기 push(겹친 변경 0). 검증 run 33966523663 success(unittest 188개 · `S2b_*` 7개 포함) · Pages 33966523576 success · 사이트 notes.html 보완 문장 반영 확인.
- **실제 야후 응답으로 한 드라이런**(2026-09-05 12:4x UTC · 저장소 밖 사본 `deploy/`+`data/` 복사본에서 `wait_close.py` 본실행 · 알림·커밋 없음):
  ① 현재 사본(as_of 09-04) → 「이미 최신 (as_of 2026-09-04 = 예상 2026-09-04)」 rc 0(달력 09-04 통과).
  ② 사본을 09-03 으로 낡게 한 뒤 → 실제 야후 갱신 → 「종가 반영 완료: as_of 2026-09-04 (시도 1회, 1초)」 · source yahoo.
  ③ 가짜 시계 2026-09-07 19:35Z(노동절 다음 화요일 04:35 KST 슬롯) + 실제 meta → 달력상 마지막 마감 09-04 → 「이미 최신」 rc 0 — 휴장일 거짓 실패 없음.
  ④ 가짜 시계 2026-09-09 21:30Z(달력이 원천보다 앞선 상황) + 실제 meta · 시한 1분 → 「원천 예상일 09-04 이 달력상 마지막 마감 09-09 보다 뒤처졌다 — 원천 지연 의심」 → 갱신 재시도 → 「시한 초과 … 최신성을 확인할 수 없다」 exit 1 — 확인 불가가 성공과 갈린다.
  다음 실제 실행(09-08 새벽 슬롯)은 이 경로와 같은 조건이라 별도 확인 의무를 두지 않는다 — 실패하면 기존 실패 알림 카톡이 온다.
