# 설명서·노트 상태/앵커 행렬 + 시세 경로 시간 실측 + 주간 파수꾼 관찰 (2026-09-06 · v224)

> 기준 **b77a7c9** · 별도 작업트리 `review/guide-notes-7`. 소유자 지시 세 갈래: ① guide·notes 상태/앵커 행렬(이미 검증한 것은 반복하지 않고 재현되는
> 기능 오류만 수정) ② `price_poll` → Pages 를 수집·브랜치 반영·배포 완료·화면 반영으로 갈라 측정(「5분 안」의 대상 경로·기산점 먼저) ③ 다음 주간
> 파수꾼에서 `protocol_b` 생성·커밋 확인(장부 조작·카톡 중복 금지 · 예약 관찰 불가면 미확인). research·전략·판정·실측 장부 제외. 공용 규약 변경은 인계.

## ① 설명서·노트 상태/앵커 행렬

**반복하지 않은 것**: 세 화면 앵커·id 교차(v199 배포 실측 0건) — 정적 재확인만 했다: 깨짐 0(색상 리터럴 `#E1E7EB` 등 3건은 오탐). `details.foldsec`
앵커 자동 펼침(v153) · 검색의 접힌 details 탐색(v156) · 필터×접힘 시즌 임시 펼침/「전체」 복원(v169·v170) — 이번 실측에서도 설계대로였다.

**새로 본 상태**(정적 서버 · 실제 로드 · localStorage `b_fold_v1` 로 접힘 저장):

| 상태 | 수정 전 | 수정 뒤 |
|---|---|---|
| 설명서 §③(routine) 접어 둠 → `guide.html#order`(신호 화면 「주문 방법」 링크) | 내용 숨음 · scrollY **0** · 버튼 「펼치기」 | 절 펼침 · 대상 화면 상단(top 12px) · 버튼 「접기」 · 저장값 그대로 |
| §⑧(auto) 접어 둠 → 같은 문서에서 `#manual` 로 hashchange | 숨음 | 펼침 · 상단 |
| 검색 중 접힌 절이 임시로 펼쳐질 때 / 검색 해제 뒤 | 펼쳐졌는데 버튼 「펼치기」 · 복원됐는데 버튼 「접기」 | 라벨이 상태를 따라감 |
| 노트 최신 시즌(sea169) 접어 둠 → `notes.html#opsRecovery` | 숨음 · scrollY 0 | 시즌 펼침 · 상단 |
| 노트 필터 「삭제」→「전체」 | 시즌 접힘/펼침은 맞으나 버튼 라벨 불일치 | 라벨 일치 |
| 신호 화면 포트폴리오 패널 접어 둠 → `#portPanel`(전환 미체결·체크리스트·D-day 링크) · `#tlogWrap`(체결 기록) | 접힌 제목만 보이고 「오늘의 행동」·체결 기록 숨음 | 패널 펼침 · 내용 표시 |

**원인**: v169 접기는 `[data-fold].folded > *:not(h2){display:none}` 로 숨길 뿐이라 앵커 목적지는 DOM 에 있어도 보이지 않았다. v153 의 `openFoldTarget` 은
`details.foldsec` 만 다뤘고 notes·signal 엔 그 처리 자체가 없었다.
**수정**(세 화면 공통 · 표시 전용): `openFoldTarget` 이 `el.closest('[data-fold].folded')` 를 화면에서만 펼치고(저장값 무접촉 — 검색·필터와 같은 규약)
`p._foldPaint` 로 버튼을 다시 그린다. guide 는 `setupFold()` 를 앵커 처리보다 먼저 부르게 순서를 바꿨다(접힘을 복원한 뒤라야 펼칠 수 있다).
회귀 `audit/test_fold_anchor.py`(옛 파일 3실패+1오류 → 4/4).
**실측 함정(그대로 적음)**: 같은 문서에서 해시만 바꾸는 `navigate` 는 재로드가 아니라 hashchange 라 첫 guide 검사가 「이상 없음」으로 나왔다 — 다른 문서를
거쳐 fresh load 로 다시 재서 재현했다.

## ② `price_poll` → Pages 구간별 시간 (로그 실측 · 09-03·09-04)

**「5분 안」의 대상 경로와 기산점**: v190 의 약속은 「한국 시세가 09:00:20 부터 5분마다 사이트에」다. 기산점은 **5분 경계(HH:M5:00 KST)**, 대상은
**사이트의 `data/price.json`**(price-data 브랜치 → `gh workflow run pages.yml` → 배포). 화면은 그 위에 열린 탭의 ticker(`PRICE_TICK_MS` = 5분 · 한국장 중)
또는 탭 복귀 즉시 fetch(`?t=` 캐시 우회 · Pages 는 `cache-control: max-age=600` 이지만 URL 이 매번 달라 CDN MISS)가 얹힌다.

| 구간 | 정의 | 09-03 (78건) | 09-04 (79건) |
|---|---|---|---|
| 수집 | 경계 → 스냅샷 로그 | 중앙 22s · max 22s | 22s · 22s |
| 브랜치 반영 + 배포 깨우기 | 스냅샷 → pages run 생성(dispatch) | 2s · 3s | 2s · 3s |
| 배포 | pages run 생성 → 완료 | 22s · 52s | 22s · 42s |
| **경계 → 배포 완료** | dispatch run 기준 | **46s · p90 50s · max 76s** | **46s · p90 49s · max 66s** |
| 경계 → 유효 배포 | dispatch 가 취소된 건은 후속 성공 run 완료 | 46s · max **88s** | 46s · p90 53s · max **349s** |
| 화면 반영 | 유효 배포 → 화면 표시 | 로그로는 못 잰다 → **라이브 1건(2026-09-07 09:40 경계)**: 배포 완료 +284s 에 표시 · 아래 ②-a | 〃 |

- 값이 안 바뀐 스냅샷(09-03 7건 · 09-04 6건)은 당시 규약대로 발행 생략(v222 부터는 30분마다 재발행). 발행된 것은 전부 dispatch 와 짝이 맞았다(미매칭 0).
- **dispatch 취소** 09-03 2건 · 09-04 6건. 그중 후속 push/인계 run 이 30~62s 안에 덮은 것 외에 **09-04 3건(11:55·13:30·13:55)은 다음 스냅샷까지 341~349s**
  늦게 반영됐다. 연쇄(실측): 예비 슬롯(:20/:50, 동시성 그룹 대기)이 다음 예약에 밀려 **취소**됨 → `price` 의 `workflow_run(completed)` 이벤트 → pages 새 run
  생성(:55:32) → 워크플로 수준 `concurrency: cancel-in-progress` 가 **진행 중인 5분 dispatch 배포(:55:24)를 취소**(:55:35) → 그 run 자신은 skipped(0 스텝).
- **판단**: 이틀 표본에서 「경계 → 사이트」는 1분 남짓이 통상이고, 한 번의 성공이 아니라 157건의 분포다. 그러나 **상시 보장은 아니다** — 예약 실행 지연은
  v190 실측대로 분 단위로 흔들리고, 위 취소 연쇄가 하루 2~3번 5분을 먹는다. 화면까지의 시간은 ticker 위상 때문에 최대 약 6분이며 라이브 표본은 없다.
- **인계(공용 워크플로 · 미수정)**: `pages.yml` 후보 — ⓐ `concurrency` 를 job 수준으로 내려 skipped 될 run 이 진행 중 배포를 못 죽이게 하거나 ⓑ `workflow_run`
  트리거에서 `conclusion == 'cancelled'` 를 걸러 낸다(g_deploy 의 「`conclusion == 'success'` 금지」 검사와 충돌 없음). 어느 쪽이든 dispatch 실측으로 확인 뒤 적용.

### ②-a 화면 반영 — 라이브 표본 1건 (2026-09-07(월) 09:40 경계 · 읽기 전용)

| 시각(KST) | 경계 대비 | 무엇 |
|---|---|---|
| 09:40:00 | +0s | 5분 경계 |
| 09:40:24 | +24s | pages dispatch run 생성(`34070550943`) — 스냅샷 발행 직후 |
| 09:40:45 | +45s | 배포 완료(run updated) |
| 09:40:45~49 | +45~49s | **사이트 데이터** — `data/price.json?t=` 를 4초 간격으로 폴: 09:40:44 까지 `09:35`, 09:40:49 에 `09:40` |
| 09:45:29 | +329s | **화면** — `#vpx` 「09:35 시세」 37,765원 → 「09:40 시세」 37,730원 |

- 화면 값은 **탭 복귀 즉시** 경로다. 관찰 내내 탭이 숨김이었고(`visibilityState: hidden`) ticker 는 설계상 그때 돌지 않는다 —
  `startPriceTicker` 의 `if(document.visibilityState !== 'visible') return;`(v173 「밤새 켜 둔 탭이 헛돌지 않게」). 탭이 보이게 된 09:45:29 에
  `visibilitychange` 핸들러가 `refreshPrice()` 를 불러 그 자리에서 바뀌었다 — **사이트 데이터는 이미 09:40:49 에 준비돼 있었다.**
- 그러므로 이 표본은 「ticker 위상 0~5분」 쪽을 재지 못한다(그 경로는 여전히 미관측). +329s 는 보던 사람이 없던 시간이지 파이프라인 지연이 아니다.
- **한 표본이다 — 보장이 아니다.** 다만 이날의 경계 → 배포 완료 45s 는 09-03·09-04 로그 중앙 46s 와 같은 자리다.
- 관찰 방법(재현): 사이트를 열어 `#vpx` 를 기록 → 경계 전후로 `curl` 로 `price.json` 의 `as_of_kst` 를 4초마다 → `gh run list --workflow=pages.yml` 의
  dispatch run `createdAt`/`updatedAt` → 화면 텍스트 변화 시각. 쓰기·dispatch·알림 없음.

## ③ 주간 파수꾼 첫 `protocol_b` 생성·커밋 (2026-09-07 실측)

- 슬롯은 **월요일 09:10 KST**(`watchdog.yml` cron `10 0 * * 1`) — 일요일이 아니다. 다음 실행 2026-09-07(월).
- 세션 예약(one-shot · **세션이 살아 있을 때만** 발화 · 디스크에 남지 않음)으로 09-07 09:52 KST 읽기 전용 확인을 걸었다. 확인 항목: run 결론 · check/heartbeat/휴장일/커밋 스텝 · `origin/main:data/ops_check.json` 의 `protocol_b`·`as_of`·`heartbeat` · 점검 커밋 유무 · 첫 등장이라 「악화」 알림 없음.
**실측 결과 (2026-09-07 · 읽기 전용 관찰)**

- **실행**: run `34070225309` · `schedule` · `WEEKLY: true`(월요일 슬롯) · **success** · 00:34:15Z 생성 → 00:34:38Z 완료(23s). 예약은 `10 0 * * 1` = 00:10Z 인데
  실제 큐 진입이 **00:34:15Z — 24분 지연**(GitHub 예약 지연 · v190 실측과 같은 종류이며 이번 변경과 무관). 체크아웃 기준 `7752fc0`.
- **고정 SHA 가 실제로 쓰였다**: `Set up job` 로그에 checkout `3d3c42e5…` · setup-python `5fda3b95…` · github-script `f28e40c7…` 다운로드 — 2026-09-06 고정 뒤 첫 예약 실행이 정상.
- **커밋 `04bff72`** 「ops: 자동 점검 — Level 0 (+생존 기록·휴장일 표)」 — `data/ops_check.json` · `data/kr_holidays.json`.
- **`protocol_b` 첫 생성**: `verdict: ok` · `events: 0` · `line: 사건 0건 — 재검토 사유 없음` · `drift: false` · `todo: null` · `exit: 0`. **표류 없음** —
  v218 재등록이 실제 파수꾼 경로에서 확인됐다. `todo` 빈 배열 · `level` 0 · `health_errors` 빈 배열 · 첫 등장이라 「악화」 알림 없음(설계대로).
- **`heartbeat` 「2026-09」 불변**(09-01 값과 같음) → 생존 알림 **미발송**. 같은 달 한 번 규약(v177)대로다.
- **휴장일 표 재생성**: 「근로자의날」 → 「노동절」(2026·2028~2031) + **2027-05-03·2032-05-03 노동절 대체 추가**. 그 규정은 `kr_holidays.py` 에 **18d8be7(09-04 17:34)** 로
  들어왔고 표의 직전 생성은 **09-04 02:37 UTC(=11:37 KST)** 라 규정보다 앞선다 — 이번이 그 반영의 첫 자동 적용이다(v195 「같으면 안 쓴다」가 정상 작동).
- **스키마 이동 `exec.switches` → `exec.events`**(+`health_errors` 신규): `내가_보는_것/점검.py` 의 v204 변경(`48da82d` · 09-05)이 09-07 첫 주간 실행에 반영된 것이다.
  화면은 이 키를 안 읽는다 — `signal.html` 의 `m.switches` 는 **성과표 stats** 쪽이고 `drawOpsCheck` 는 `level`·`todo`·`protocol_b` 만 읽는다. 회귀 아님.
- 느린 변수 4종 정상 · AUM 4다리 정상(418660 6,421억) · `exec.nav_days` 6 → 8.
- **스텝 결론 전수**(09:52 KST 두 번째 예약 관찰): 휴장일 표 연장 **success**(`→ data/kr_holidays.json (137일, 2025~2032)`) · 점검 결과 커밋 **success**(`[main 04bff72]` · 2 files changed) ·
  **이상이면 이슈 열기 skipped**(이슈 0건) · 알림 채널 생존 success(「카카오 access 토큰 갱신 OK · 살아 있음: 카카오톡」 — 메시지 없이 자격증명만 · v140 규약) ·
  평일 슬롯 전용 4스텝(신선도·재조정·전환일·근접)은 skipped(월요일 슬롯이라 정상).
- **알림 흔적은 로그로만 읽었다 · 이번 슬롯의 카톡 발송 0건 · 이슈 0건**: 자동 점검 「Level 0 · 정상 — 유지 · 할 일 0건 · 판정 규약 사건 0건 — 재검토 사유 없음」 →
  「상태 악화 없음 — 알림 생략」 · 살아 있음 「2026-09 은 이미 보냈다 — 생략」. 커밋 시각 **09:34:34 KST**(`github-actions[bot]`).
- **휴장일 diff 는 이름 5건 + 대체 2건**이다(관찰 지시문의 「이름 6건」과 다름): 이름 2026·2028·2029·2030·2031, 추가 2027-05-03·2032-05-03.
  **2027-05-01·2032-05-01 이 토요일**이라 그 두 해는 이름 행 자체가 없고 대체(월요일)만 생긴다 — 표는 거래일 기준이다.

- 장부·원자료·secrets 무접촉 · 카톡 발송·dispatch 없음. 세션이 그 전에 끝나면 **미확인**으로 남는다 — 손으로 볼 때의 명령은 위 항목 그대로(`gh run list --workflow=watchdog.yml` · `git show origin/main:data/ops_check.json`).

## 남은 한계
- ① 은 표시 검사다. 412px 실측은 v223 행렬이 담당했고 이번엔 데스크톱 pane 폭에서 상태만 봤다(가로 스크롤은 v223 와 같은 마크업).
- ② 의 「화면 반영」은 09-07 라이브 **1건**뿐이고 그것도 **탭 복귀 경로**다 — ticker 위상(0~5분) 경로는 여전히 미관측. 보장 표현으로 쓰지 마라.
- ③ 은 2026-09-07 월요일 슬롯에서 관찰됐다(위 실측 결과). 예약 지연 24분은 그날의 값이지 상한이 아니다.

## G. 통합
- 커밋 **1cc1425**(review/guide-notes-7 → main fast-forward · 2026-09-06). CI: 「검증」 success(회귀 목록에 `audit.test_fold_anchor` 포함) · 「GitHub Pages 배포」 success. 배포 화면 실측: 도장 「전략 반영 v224 · 2026-09-06 01:15」· 노트 최신 v224 · `b_fold_v1={routine:1}` 상태로 `guide.html#order` → §③ 펼침·대상 상단(top 12px)·버튼 「접기」·저장값 그대로.
