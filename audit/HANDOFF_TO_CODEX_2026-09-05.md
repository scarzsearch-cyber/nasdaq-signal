# 코드리뷰 전수 순회 보고서 — Codex(Astra) 인수인계용 (2026-09-05)

> **이 문서의 용도**: 소유자가 다른 AI(Codex Astra / ChatGPT)에게 그대로 붙여 넣는 **보고서 겸 지시문**이다.
> §1~§7 은 사실 보고, §8 은 소유자 결정 항목, §9 는 받는 쪽에 요청하는 검토 작업(프롬프트)이다.
> 작성자: Claude Fable 5.1 · 기준 커밋 `35b1313` (main) · 상세 대장: `audit/CODE_REVIEW_SWEEP_2026-09-05.md` §7·§8·§9.

---

## 1. 한눈에

- 저장소 `나스닥_레버리지_전환전략_v18` 의 **운영·자동화·검증·엔진·화면·문서·연구·데이터** 파일 약 210개(약 58,000줄)를 15배치로 나눠 **혼자 순서대로 전문 판독 + 실행**했다.
- 발견 **114건 = P1 1 · P2 6 · P3 22 · P4 85**. P1·P2 는 전부 수정, P3 는 전부 병기·정정 표시 또는 계산화, P4 는 주석 수정 또는 기록.
- **전략 B 동결값(−16/−16 · 룩백 252 · 방어 40/40/20) · 판정 로직 · 실측 장부(freeze.json · oos_log.csv · nav_history.csv) · 공표 4시나리오는 어디에서도 바꾸지 않았다.**
- 어긋난 수치의 원인은 거의 전부 **v210(2026-09-05, FRED 빈 가격 행 114개를 거래일에서 제거)** 하나였다. 이것을 세 작업트리(v203 `d9606ed` · v209 `4c01375` · v210 `330e1c7`)에서 같은 코드를 돌려 **한 변수씩** 가렸다.
- 커밋: `7163868`(B06·07) · `81ccc5e` v214(B08) · `9c16a07` v215(B09) · `66526d1`(B10~13) · `3b1a4bf` v218(B14) · `35b1313`(B15·총괄). B01~B05 는 v211·v212 와 그 앞 review 커밋. 전부 push · CI(검증·배포) 통과.

## 2. 무엇을 검토했나

| 배치 | 대상 | 파일·줄 | 읽기/실행 |
|---|---|---|---|
| B01 | `deploy/` 운영 스크립트 · `내가_보는_것/` | 17 · 5,700 | 전문 · 셀프테스트 9종(I14) |
| B02 | `.github/workflows/` · `verify_all.py` · `audit/` | 15 · 3,700 | 전문 · verify_all 전체 · audit 4종 |
| B03 | 루트 엔진(`hist_*` · `axis_*` · `reentry_lib` · `hyst_core`) | 14 · 3,800 | 전문 · 수정 전후 지문 24종 |
| B04 | 화면 3개 · 전략 문서 4개 · CLAUDE/HANDOFF/FILES | 12 · 12,179 | 전문(04 는 감사 전문+diff) · verify 전체 |
| B05 | 연구 문서 14편 | 14 · 3,338 | 전문 · `horizon_study` 재실행 |
| B06~B15 | `research/*.py` 116개 (a~w) + `data/` 최상위 14개 | 130 · ≈29,000 | 전문 · **129편 중 117편 실행** · 데이터 계약 자동 대조 |

**일부러 뺀 것**: `공유용_별도전략/`(격리 규약 — 읽지 않음) · `docs/history/`(수정 금지 — diff 만) · `data/hist/` 원자료 85개(구조·범위만 · 출처 교차검증은 04 §7 Q10 미결) · `archive/`.

## 3. 방법

1. 파일을 전문으로 읽는다 → 2. 스크립트를 실제로 돌린다(타임아웃 25분 · 출력 보관) → 3. 문서에 적힌 숫자와 하나씩 대조한다 → 4. 어긋나면 **세 작업트리에서 같은 코드를 돌려** 코드 탓인지 자료 탓인지 가른다(CLAUDE §-1 ⑧ 「한 변수만 바꾼 열」) → 5. 고친다(옛 값은 지우지 않고 「v210 재실행」 값을 옆에 병기 · 판정문은 하드코딩 대신 계산값 출력) → 6. `verify_all`(문서·py 만이면 `--fast`, 화면을 건드리면 전체) → 7. 명시 경로만 커밋 · push · CI 확인.
- 「채택」은 한 번도 쓰지 않았다. 관문을 넘은 후보는 전부 **소유자 결정 항목**(§8)으로만 적었다.
- 새 관문·정정은 **FAIL 을 실제로 띄운 뒤 PASS** 를 재현했다(§-1 ⑤).

## 4. 발견 — 심각도별

### P1 (1건) — 운영에 곧 영향
- **B14-1 B 판정 규약 기저율 표류.** v210 뒤 `research/oos_protocol_b.py --oos` 의 자기검산이 등록값(재난 지급 7/7 · 보험료 P05 −33.3%)과 역사(8/8 · −29.3%)의 어긋남을 잡아 「판정 중단」을 내고 있었다. 그대로 두면 다음 월요일(2026-09-08) 파수꾼 `check` 가 「기저율 표류」 todo·카톡을 낸다.
  **처리**: 원인이 v210 단독임을 확인(v203·v209 는 등록값과 일치) → **동결 이후 판정 사건 0건** 상태에서 같은 정의로 다시 재 `data/oos_protocol_b.json` 재등록(A 8/8 · B P05 −0.293 · R P05 −0.331 · 최악값 둘 동일 · 옛 값·옛 지문 `c7f4f90ba8340e5a` 를 `revisions` 에 보존 · 새 지문 `74387a5c73c0fc06`) · 02 §5-1 재등록 문단·표 · 04 §5-23 표시 · notes v218 · **I13 FAIL→PASS 재현** · 평가기 「판정 불가 — 정상」 복귀 · 파수꾼 `protocol_status` verdict ok 확인.

### P2 (6건) — 전부 수정
| 배치 | 내용 | 처리 |
|---|---|---|
| B03 | `axis_defmix.real_run` 이 수익 → 전환 순서라 방어 진입일 손실을 건너뜀 / `axis_lib.check_accum` 이 엔진 결과 대신 손으로 짠 사본을 검산 | 순서 교정 · 항등식 검산으로 교체 (v211) |
| B04 | 신호 화면 성과표 각주의 「168배」가 원화 B 가 아니라 원화 2배 보유 값 / 저장 날짜가 UTC | STATS 원천에서 읽게 · KST (v211) |
| B05 | 설명서 ② 지평 확률표의 원천 `horizon_study` 가 현행 코드로 재실행되지 않아 중앙값 1~8% 옛 값 | 재실행값으로 갱신 · 옛 값 등재 (v212) |
| B08 | `axis_nextgen` 의 v81 재현 앵커가 v210 뒤 사망 → 재실행하니 MIX(0.50)(B 50%+T4 50%)가 관문 통과 | 앵커를 v210 기준으로 갱신(옛 값 병기) · **채택 아님 · 소유자 결정 항목** (v214) |
| B09 | 설명서 ⑪ T4 성적표가 v80 값(최악 −53.4% · Calmar 0.392/0.461) | 재계산(−50.9% · 0.394/0.503 · 사건 방어 승률 90/62%) (v215) |

### P3 (22건) — 전부 병기·정정 표시 또는 계산화
대부분 **「v210 자료 정정 뒤 문서 숫자가 옛 값」** 유형이다. 대표:
- T4 그림자 계열이 v210 에 크게 민감: B +7.5% 인데 **T4 +57.7%**(163,161 → 257,279). 우위가 **1972~77 여섯 해**에 걸려 있고 1978~ 공통창에선 T4 = **0.50×B**(B08·B09·B12).
- 룩백 200 의 표본 내 우위: 20년 창 승률 **100% → 78%** · 갈린 독립 사건 5/8 → **3/7**(B13). 「200 이어야 할 사전 근거 없음 · 유지」 결론 동일.
- 기계 정책의 낙폭 규칙 재발견 3/45 → **14/45**(v204 라벨 정정이 옮겼는데 문서 미갱신 · B13).
- C3(÷T-bill 낙폭) 관문 수치 Calmar +13.4 → **+10.5%** · p05 +26 → **+1.7%** · 플라시보 ①② 27 → 14.5%(B10). 닫음 결론 동일.
- 「B 는 2배 보유의 38배」→ **49배**(2배 계속보유 5,760 → 4,552 — 드래그 모형이 v210 격자에 민감 · B14).
- `wfa_thresh` 문서 대조 12/12 → **7/12**(선택 중앙 −16 은 3/3 유지 · 「고정 승」 백분율만 어긋남 · B15).
- `withdraw` 「최악 연간 소득 −51.3%」가 **연 1회 관측 위상 하나**의 값 — 같은 코드가 v210 뒤 −16.0% 를 냄(위상 252개 최악 −53.6% · 중앙 −37.4%). 위상 전수·롤링 1년으로 재정의(B15). 설계 요건(1년치 현금 완충) 동일.
- `tax_us_direct` 1981~ 전체창 145,135/253,390(1.75배 · 전환 120) → **144,804/264,133(1.82배 · 124회)** · 5년 비중첩 B승 6/9 → **4/9**. **21세기 표(전략 B ISA 161.5배 vs 3배 B 직투 283.9배 = 1.76배)와 손익분기 k 는 소수점까지 동일**(화면·아티팩트 기준 불변 · B15).
- **하드코딩 판정문 3건**: `post_dotcom` [4](VR 0.57 vs 표 0.52) · `wfa_thresh` [3](「안 움직인다」 vs 값 −0.12→−0.16) · `withdraw` [6] → 전부 표의 계산값으로 출력.
- 인용 오류 정정: 미니맥스 3위 → 1위/210 · v18 미관측 28년 상위 79 → 90% · Oracle +288 → +251% · B 사건승 15/21=71% → 13/21=62% · `retired_numbers.json` 등재(B08).

### P4 (85건)
주석·docstring 의 옛 앵커(217,110.075 등) · 모듈 수준 실행 · 전역 상수 덮어쓰기 · 재실행 결론 동일 확인. 주석은 수정, 나머지 기록.

## 5. 바뀐 것 / 안 바뀐 것

**바뀐 것(동작)**: 연구 엔진 `axis_defmix.real_run` 순서 · `axis_lib.check_accum` 항등식 · `post_dotcom`·`wfa_thresh`·`withdraw` 판정문 계산화 · 재현 앵커 4개(v210 기준, 옛 값 병기) · `data/oos_protocol_b.json` 재등록 · `hyst_wfa.csv` 재생성 · `retired_numbers.json` 등재 추가.
**바뀐 것(표시만)**: 설명서 ②·⑪ 표 · 신호 화면 각주 · notes v214·v218 항목 · 문서 100여 곳의 「v210 재실행」 병기.
**안 바뀐 것**: 전략 규칙 · 판정 · 장부 · `deploy/*`(B01 cp949 단위검사 추가 외 수정 0) · 워크플로 · `내가_보는_것/점검.py`.

## 6. 확인하지 못한 것 (정직하게)

- `공유용_별도전략/` · `docs/history/*` · 원자료 85개의 **출처 교차검증**(04 §7 Q10 미결) · 외부 아티팩트 「전략 B 배합 탐색기」 원본(v205 미확인) · **카카오 실제 도착**(자동 검증 불가 · v178) · GitHub 러너 환경 차이(`source-probe.yml` 로만) · **소유자 개인의 투자금·납입액·감수 손실·세후 조건**(미확인 입력은 추측하지 않았다).
- 미실행 12편: 네트워크 다운로더 `hist_fetch` · B05 에서 재실행분을 재사용한 문서 원천 등(배치별 「읽기 vs 실행」 줄 참조).
- **동시 작업**: 순회 중 Codex 세션이 v207~v217 · F1~F4 연구 커밋을 main 에 넣었다. 순회는 그 뒤 코드를 읽었고 수정 파일은 겹치지 않았다. Codex 의 미커밋 파일(`audit/test_f4_products.py` · `research/strategy_f4_products.py`)은 어느 커밋에도 넣지 않았다.

## 7. 다음 거래일(2026-09-08 월) 확인 목록

- `daily-signal` 이 05:0x KST 에 종가를 굳혔는가(`signal.json` as_of).
- `nav_collect` 가 장 밖 슬롯에서 **직전 거래일 종가**로 적립했는가(v206 계약).
- 파수꾼 `check` 의 첫 `protocol_b` 기록이 **「사건 0건 — 재검토 사유 없음」** 인가(재등록 뒤 첫 실행 · `data/ops_check.json`).
- 방어 재조정 D-day 표시 · 자동 검증 배지.

## 8. 소유자 결정 항목 (AI 가 정하지 않는다)

1. **혼합 그림자 후보**: `axis_nextgen` MIX(0.50) · `hypo_hex` x=0.50·0.55 가 v210 자료에서 관문 통과. 비용 미교정 경로 위 값 · 2000~ 은 0.91×B. **채택 아님.** 그림자 등록 논의를 열지 여부.
2. **감시 밴드 4종**(`점검.py` · SURVIVAL §F)이 v210 전 분위(차이 ≤0.4%p · 전부 먼저 경고하는 쪽). 갱신하면 경고가 미세하게 늦어진다. 현재값은 밴드에서 멀다.
3. 04 §5-11 T4 서술을 다시 쓸 때 **창을 병기**할지(1972~ 전창 1.40×B vs 1978~ 0.50×B).
4. v210 전 수치를 든 연구 문서 5편(ENGINE·EXT_INFINITE·NEW_STRATEGY·SURVIVAL·LEVERAGE_US)은 배너만 — 전면 재생성 여부.

## 9. Codex(Astra)에게 요청하는 작업 — 지시문

> 아래를 그대로 지시로 써도 된다.

당신은 이 저장소의 **독립 검토자**다. 위 §1~§8 은 Claude 가 2026-09-05 에 마친 코드리뷰 순회의 보고다. 다음을 수행하라.

**규칙(먼저 읽을 것)**: `CLAUDE.md` 전문, 특히 §-1(오류 비용 비대칭 · 실행 체크리스트)과 §2(수정 금지 대상). 전략 B 동결값·판정 로직·실측 장부(`data/freeze.json` · `oos_log.csv` · `nav_history.csv`)는 절대 바꾸지 마라. 「채택」·「권고」를 쓰지 마라 — 관문을 넘은 후보는 소유자 결정 항목으로만 적어라. 통짜 54년 배수를 표·요약의 머리에 올리지 마라(성과 주장은 21세기 기준). 미확인 입력(투자금·납입액·감수 손실·세후 기준)은 추측하지 마라. 숫자를 인용하기 전 그 값이 **v210 재실행 값인지 옛 값인지** 원문에서 확인하라.

**작업 1 — 독립 검산(가장 중요)**: 다음 셋을 Claude 의 코드를 쓰지 말고 **직접 계산**해 일치 여부를 보고하라.
- (a) `research/oos_protocol_b.py` 기본 모드의 기저율: 독립 사건 21 · 재난(H 창 MDD ≤ −50%) 지급 8/8 · 전체 69건 프리미엄 P05 −29.3% · 최악 −41.1% · 3년 롤링 P05 −33.1% · 최악 −49.3%. `data/oos_protocol_b.json` 의 등록값과 같은가. 지문 `74387a5c73c0fc06` 이 `verify_all.py` I13 규약(fingerprint 키 제외 · sort_keys · separators (',',':') · sha256 앞 16자)으로 재계산되는가.
- (b) `research/withdraw.py` [5]: 전략 B 의 롤링 1년 수익 최악이 −53.6% 이고, 연 1회 관측 위상 252개의 최악 연간 감소가 최악 −53.6% · 중앙 −37.4% · 최선 −16.0% 인가.
- (c) `research/tax_us_direct.py --c21` 이 전략 B ISA 161.5배 · 3배 B 직투 283.9배 · 1.76배 · MDD −53.2/−61.3% 를 내는가(이 값은 v210 전후 동일해야 한다).

**작업 2 — 순회가 놓친 것 찾기**: `audit/CODE_REVIEW_SWEEP_2026-09-05.md` §8 의 발견 대장을 읽고, (i) P3 로 분류된 것 중 P2 이상이어야 할 것, (ii) 「기록만」으로 둔 P4 중 수정이 필요한 것, (iii) 순회가 전문 판독했다고 적었으나 실제로는 확인이 얕은 곳을 지목하라. 지목할 때는 파일:줄과 근거를 적어라.

**작업 3 — 동시 작업 정합성**: 당신(Codex)이 순회 중 넣은 v207~v217 · F1~F4 커밋과 Claude 의 review 커밋(v211~v218) 사이에 **같은 수치를 다르게 적은 곳**이 있는지 `research/CODE_REVIEW_2026-09-05.md` 와 `audit/CODE_REVIEW_SWEEP_2026-09-05.md` 를 대조하라. 특히 부분 비중 비용 교정(v213)과 순회의 재실행 값이 같은 엔진 경로인지 확인하라.

**작업 4 — 보고 형식**: 결론 한 문장 먼저. 그 다음 (1) 검산 일치/불일치 표, (2) 놓친 것 목록(심각도순 · 파일:줄), (3) 당신이 바꾼 것/안 바꾼 것, (4) 소유자가 정해야 할 것. 수정은 `verify_all.py --fast` 실패 0 · 화면을 건드리면 전체 모드로 검증하고, 커밋 제목에 화면 변경 시 vNN 을 붙여라. 확인하지 못한 것은 「확인하지 못했다」고 적어라.

## 10. 참조 파일

- 순회 대장(단일 진실): `audit/CODE_REVIEW_SWEEP_2026-09-05.md` (§6 배치 · §7 진행표 · §8 발견 · §9 총괄)
- 판정 규약: `data/oos_protocol_b.json` · `02_Risk_Management.md` §5-1 · `research/oos_protocol_b.py` · `verify_all.py` I13 · `deploy/watchdog.py` `protocol_status`
- 작업 규칙: `CLAUDE.md` §-1 · §2 · §3 · §4 v211~v218 항목
- 이전 리뷰(Codex 작성): `research/CODE_REVIEW_2026-09-05.md` (v203~v213)
- 폐기 수치 관문: `data/retired_numbers.json` (I9)
