# 낙폭 백분위 검증 — 생성기·JSON·화면 (2026-09-06 · 후보 ① · v230)

> 기준 ccc35c0. 회귀 `audit/test_ddpct10.py`(4 · verify.yml 등재). 생성기(`research/emit_dd_distribution.py`)·JSON 은 **재생성하지 않았다** — 정의에 맞는지 독립 검산만.
> 화면(`signal.html ddDeeperPct`)의 표시 오류 1건을 고쳤다(v230). 원자료·엔진·동결값 무접촉.

## 1. 분포·단위·보간·경계 (먼저 명시)
| 항목 | 생성기 | 화면 |
|---|---|---|
| 분포 | 엔진 chain(`hist_defensive.build('chain')`)의 **252거래일 고점 대비 낙폭** `ddv`(min_periods 252 · 시작 전 격자에서 워밍업 · v203) × 100 → **%** · 유한값 전부 | 같은 JSON 을 읽는다 |
| 표본 | n = 13,749 = 엔진 거래일 수 · start 1972-02-07 · end 2026-08-28 (JSON = 엔진 정확히 일치 · min −70.2054 · max 0 · median −4.2459 일치) | — |
| 경계 | 1~99 백분위 · `np.percentile` 기본(선형 보간 · Hyndman-Fan 7) · 소수 4자리 반올림 · 오름차순(깊은 쪽이 앞) | `edges[i]` = (i+1) 백분위 |
| 동률 | **고점 갱신일(dd=0)이 11.64%** → p89~p99 경계 11개가 정확히 0 | edges[98]=0 |
| 문구 | 생성기 자체 검산은 「이하(≤)」 비율을 찍는다(정보용) | 「54년 중 더 깊었던 날 p%」 · p=0 이면 「가장 깊은 축」 |
| 정상 시차 | 원자료 월간 연장 뒤 생성(end 2026-08-28) vs 운영 일봉(`data/qqq.csv` 매일) — 검사하지 않는다 | — |

## 2. 별도 계산으로 기존 JSON 대조 (보간식 복사 없음)
- **경험분포 괄호**: 각 경계 e_p 에 대해 share(dd < e_p) ≤ p ≤ share(dd ≤ e_p) — 순서통계 1개(1/n)와 4자리 반올림(±5e-5%p) 여유 안에서 **99/99 통과**. 즉 경계는 어떤 보간식을 썼든 경험분포와 모순이 없다.
- **정의 민감도**: 다른 정의(weibull·nearest·lower·higher·midpoint)로 뽑은 경계와의 최대 차이 **0.13%p** — 정수 % 표시에 영향 없음.
- **단조·edges[98]=0·percentiles 1~99·n·start/end·min/max/median**: 전부 일치.
- **첫 날 dd −0.082(0 아님)** → 워밍업 뒤 진짜 낙폭에서 시작(fillna 잔재 없음).

## 3. 「이보다 깊었던 날」 — 엄격/이하, 동률, 최솟값·최댓값, 정밀도
| 질문 | 확인 |
|---|---|
| 엄격 미만인가 이하인가 | **수정 전 화면은 ≤ 셈**(경계 ≤ 오늘 낙폭 개수). 문구 「더 깊었던 날」은 엄격히 더 깊은 날이다 |
| 동률 0(고점 갱신일) | 수정 전 **99%** 표시 · 정확한 「더 깊은 날」 비율 **88.4%** — 오차 10.6%p(54년 중 11.6% 의 날에 발생) → **수정: 엄격(<) 셈 → 88%** |
| 0 이 아닌 낙폭 | 동률이 사실상 없어 ≤ 와 < 가 같다(−16% → 19% · −3.83% → 52% · −0.5% → 81%) |
| 최솟값(−70.2%) | 경계 0개 < dd → 0 → 「가장 깊은 축」 ✅ (하위 1% 구간 전체가 이 문구) |
| 최댓값(0) | 엄격 셈 88%(수정 전 99%) — 100% 는 정의상 나올 수 없다(자기 자신을 세지 않는다) |
| 정밀도 | 99개 경계의 정수 % 근사 — 전 일자에서 정확 분율과의 최대 오차 **1.00%p**(수정 후) · 표시가 정수 % 라 적절 |

## 4. 결함 주입 — 검사가 잡는가
비단조 경계 · edges[98]≠0 · n+1 · end 변경 · p19 경계 3%p 이동(경험분포 밖) · median 변조 → 계약 검사 **전부 FAIL**. 화면 함수 node 하네스: 0 → 음수 경계 수 · 경계와 같은 값은 제외 · 사이값 · 최솟값 아래 0 · null/NaN/데이터 없음 → null.

## 5. 판정 요약
- **계산 일치**: JSON(경계·메타) = 정의 · 독립 검산 통과.
- **표시만의 문제 1(수정 · v230)**: 고점 갱신일에 「더 깊었던 날 99%」 → 88%. 그 외 날은 표시 불변.
- **실제 오류**: 없음(생성기). **정책 판단**: 없음. 생성기 자체 검산 문구(≤)는 정보용이라 그대로.
