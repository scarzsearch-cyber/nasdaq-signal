# 나스닥 레버리지 전환 전략

QQQ 의 252일 낙폭으로 **2배 레버리지**와 **방어 바스켓**을 오가는 전략.
신호는 매일 자동으로 갱신되고 화면에 뜬다 → **https://scarzsearch-cyber.github.io/nasdaq-signal/**

---

## 1. 전략 (이게 전부다)

| | |
|---|---|
| **신호** | QQQ **미국 종가**의 252일 낙폭 |
| **진입** | 낙폭 ≤ **−16%** → 방어로 전량 전환 |
| **복귀** | 낙폭 > **−16%**(규칙 B, 기본) 또는 **−11%**(규칙 A) → 레버리지로 전량 복귀 |
| **공격** | TIGER 미국나스닥100레버리지(합성) `418660` |
| **방어** | 배당40 `458730` / 미국채40 `305080` / 금20 `411060` — **비중 고정**, 월 1회 재조정 |
| **체결** | 전일 미국 종가 신호 → 다음 한국 거래일. 09:05~15:20 사이 아무 때나 |
| **계좌** | ISA 중개형 (일반계좌 대비 세후 +48.8%, 20년 중앙값) |

**하지 말 것**: 방어자산을 골라 담기 · 부분 비중 · 낙폭 기다리며 현금 쌓기 ·
VIX/공포탐욕 등 매크로 지표 추가 · 국내 종가로 신호 재기.
전부 시험했고 전부 기각됐다(`docs/` 참조).

---

## 2. 자동화 — 사용자가 할 일은 없다

```
매 평일 22:30 / 23:30 / 00:30 UTC   daily-signal.yml
   └ deploy/update_signal.py   QQQ 종가 → data/signal.json
   └ deploy/nav_collect.py     국내 ETF 실측 NAV 한 줄 적립
        ↓ (성공하면)
   pages.yml                   화면 재배포
        ↓
매 평일 01:00 UTC               verify.yml  ← [v37 신설]
   └ verify_all.py             불변식 8종 검사. 실패하면 **이슈 자동 생성**
   └ audit_full.py             59개 파일 전수조사 + 시점별 재계산
```

**예약 슬롯이 3개인 이유**: GitHub 은 부하가 걸리면 예약 실행을 **통째로 버린다**
(2026-08-26 에 실제로 발생, 종가가 하루 밀렸다). 한 번 놓쳐도 한국장 개장 전에 따라잡는다.

**그래도 밀리면**: 화면 맨 위에 **경고 배너**가 뜬다(3거래일 주의 / 5거래일 이상 빨간 경고).
배너의 링크를 눌러 Actions → 「일일 신호 갱신」 → Run workflow 하면 즉시 갱신된다.

### 수동으로 해야 하는 것 (모형을 바꿨을 때만)

| 명령 | 언제 |
|---|---|
| `python deploy/build_stats.py` | 방어자산·레버리지·문턱 등 **모형**을 바꿨을 때 |
| `python axis_isa.py --emit` | 세율·납입한도가 바뀌었을 때 |
| `python deploy/kr_holidays.py --emit` | 2032년이 가까워지면 (현재 커버리지 2032-12-31) |

---

## 3. 검증 — 뭔가 고쳤으면 이것부터

```bash
python verify_all.py          # 불변식 8종. 3초. 실패=0 이어야 한다
python verify_all.py --fast   # CI 가 매 push 마다 부르는 것
```

| 불변식 | 무엇을 막는가 |
|---|---|
| I1 엔진 동치 | 시뮬레이터마다 다른 답 |
| I2 미래 미참조 | **시점별 재계산** — 그날 데이터만으로 같은 값이 나오는가 |
| I3 체결 규약 | 미래를 당기면 좋아져야 한다(안 그러면 이미 보고 있다) |
| I4 모형 vs 실물 | 국내 ETF 3종 연드리프트 ±1.5%p |
| I5 채택 결정 | B>A, 40/40/20 좌측꼬리, 미국종가 신호 |
| I6 라이브 정합 | `signal.json` 이 재계산과 일치 + 신선도 |
| I7 공표 수치 | `strategy_stats.json` 이 현재 코드와 일치 |
| I8 의존성 | 공용 모형 사용처 목록 |

추가 도구: `audit_all.py`(채택 결정 재검증) · `audit_full.py`(59파일 전수조사) ·
`verify.py`(채택안 단독 검산) · `verify_volguard.py`(v32/33 관문)

---

## 4. 파일 지도

### 건드리면 안 되는 것 (라이브)

```
signal.html                 화면 전부
deploy/update_signal.py     매일 도는 신호 생성기
deploy/build_stats.py       성과표 생성 (수동)
deploy/nav_collect.py       실측 NAV 적립
deploy/kr_holidays.py       한국 휴장일 (수동)
data/*.json, data/qqq.csv   화면이 읽는 것
.github/workflows/*.yml     자동화
```

### 공용 엔진 — 고치면 사용처를 전부 재실행할 것

| 파일 | 무엇 | 고치면 |
|---|---|---|
| `reentry_lib.py` | 체결·비용 규약의 **단일 원천** | 전부 |
| `axis_lib.py` | `rule_w` `sim` `accumulate` `lev_r` `check` | 전부 |
| `hist_defasset.py` | `ust_tr` `gold_r` `mix_monthly` | 방어자산 쓰는 것 전부 |
| `hist_defensive.py` | 배당체인, `build()` | 전부 |
| `hist_data.py` | 1972- 나스닥 3구간 접합 | 전부 |
| `hist_korea.py` `hist_krfinal.py` `hist_krreal.py` | 원화·한국 체결 | 원화 쓰는 것 |
| `axis_defmix.py` | `materials` `sim_hold` `check_hold` | 바스켓 쓰는 것 |

> **`verify_all.py` 의 I8 이 사용처 목록을 출력한다.** 공용 모형을 고쳤으면
> 그 목록을 보고 관련 raw 출력을 재생성하라. 2026-08-27 에 이걸 안 해서
> v27 의 근거 문장 하나가 뒤집힌 채 남아 있었다.

### 연구 기록 (실행할 일 없음 — 기각 근거의 증거)

`axis_*.py` 20여 개와 `docs/전략_v*.md` 19개, `docs/raw/*.txt` 17개.
**지우지 않는 이유**: 각 기각 판정의 재현 코드이자 증거다.
같은 축을 또 파지 않으려면 이 기록이 있어야 한다.
`archive/` 는 v19~v20 시절 폐기본 15개.

---

## 5. 왜 오류가 났는가 — 2026-08-27 의 12건 분석

하루에 12건을 찾았다. 무작위가 아니라 **구조적 패턴**이 있었다.

| 원인 | 건수 | 무엇 |
|---|---:|---|
| **① 검산 함수가 없던 곳** | **2** | `mix_monthly_parts`(비용 분모) · `accumulate`(2일 지연) |
| ② 엔진을 안 쓰고 새로 짬 | 2 | `axis_macro.bt()` 직접 구현 · 검증 하네스가 `verify.py` 구성 미재현 |
| ③ 모형이 실제 상품과 다름 | 1 | 「미국채10년**선물**」을 현물로 모형화 |
| ④ 검증 설계 오류 | 5 | 격자 단면만 봄 · 수익 기준 워크포워드 · MDD 초기허수 · 최악값 인용 · 하드코딩 판정문 |
| ⑤ 의존성 추적 실패 | 1 | 공용 모형 변경 후 사용처 6개 미재실행 |
| ⑥ 플랫폼 | 1 | GitHub 예약 실행 누락 |

### 가장 중요한 발견

> **검산 함수가 있던 코드는 12건 중 0건이 틀렸다.**
> `run` / `sim` / `sim_hold` / `after_tax` 는 `check()` 가 매번 대조했고 전부 정확했다.
> `accumulate` / `mix_monthly` 는 검산이 없었고 **둘 다 틀렸다.**

**파일이 많아서 못 찾은 게 아니다.** ⑤ 한 건만 파일 수와 관련 있고,
그것도 `grep -rl` 로 즉시 해결됐다. 진짜 원인은 **불변식을 코드로 못박지 않은 것**이다.

### 그래서 무엇을 바꿨나

| 원인 | 대책 | 상태 |
|---|---|---|
| ① | `verify_all.py` I1 — 모든 시뮬레이터 동치 검사 | ✅ 자동 |
| ① | `axis_lib.check_accum()` — 납입 1회면 거치식과 오차 0 | ✅ 자동 |
| ② | I1 이 새 구현을 엔진과 대조 | ✅ 자동 |
| ③ | I4 — 국내 ETF 실물과 연드리프트 ±1.5%p | ✅ 자동 |
| ④ | 이 README §6 의 체크리스트 | ⚠ 사람이 봐야 함 |
| ⑤ | I8 — 공용 모형 사용처 목록 출력 | ✅ 반자동 |
| ⑥ | 예약 슬롯 3개 + 화면 신선도 경고 | ✅ 자동 |

---

## 6. 새 분석을 할 때의 체크리스트 (④ 를 막는 유일한 방법)

자동화로 못 막는 것들이다. 반드시 사람이 확인해야 한다.

- [ ] **엔진을 재구현하지 말 것.** `axis_lib.sim` / `sim_def` / `sim_hold` 를 써라.
      새로 짜야 한다면 기존 엔진과 오차 0 인지 먼저 검산하라.
- [ ] **격자는 전 방향으로 넓혀 볼 것.** 최적점이 경계에 붙어 있으면 격자 밖까지 늘려라.
      v31 이 단면 하나만 보고 "첨탑"이라 오판했다.
- [ ] **워크포워드는 Calmar 로 고를 것.** 수익으로 고르면 잡음을 고른다.
      **더 나은 건 아예 안 고르고 못박는 것**이다(v32: 고정 8/9 vs 선택 5/9).
- [ ] **적립식 MDD 는 납입 종료 후 또는 원금 대비로 잴 것.** 초기 잔고가 1단위일 때의
      등락이 낙폭으로 잡힌다(−23% 가 −70% 로 부풀었다).
- [ ] **중앙값과 최악값을 구분해 인용할 것.**
- [ ] **판정문에 숫자를 하드코딩하지 말 것.** 계산 결과에서 만들어라.
      `audit_full.py` 의 스캐너도 서술형 문장은 못 잡는다.
- [ ] **달러와 원화가 갈리면 원화가 기준.** 실제로 사는 게 원화 상품이다.
- [ ] **전구간 배수를 믿지 말 것.** 데이터 출처별로 쪼개라
      (1972-1985 나스닥종합 합성 / 1985-1999 NDX / 1999- QQQ 실물).
- [ ] **한국 ETF 는 일간 상관으로 검증하지 말 것.** 세션 시차 때문에 낮게 나온다.
      주간/월간 또는 shift+1 로 봐라.
- [ ] **`pct_change` 는 교집합 후에 계산할 것.** 먼저 하면 드리프트가 부풀려진다.

---

## 7. 한계 (버그가 아니라 데이터의 한계)

| | |
|---|---|
| 1972-1985 | NDX 가 없어 **나스닥 종합지수**로 대체. 2배 상품도 합성 |
| 국내 ETF 이전 | 「원화 환산 미국 자산」 시뮬레이션이지 TIGER 실제 성과가 아니다 |
| 실물 대조 표본 | 배당 2.9년 / 국채 7.4년 / 금 4.3년 — 전부 짧다 |
| 세션 시차 | 한국장이 미국 i−1일을 반영하는 구조를 일간 단위로 완전히 재현 못 함 |
| 국채 보수 0.29% | 회귀 절편에서 나온 값. 실제 상품 보수와 대조 필요 |

**세무 자문이 아니다.** ISA 자격·세율은 가입 전 증권사·국세청에서 직접 확인할 것.

---

## 8. 문서

- `HANDOFF.md` — 다음 세션이 읽어야 할 것 (하지 마라 목록 포함)
- `FILES.md` — 파일별 역할
- `docs/전략_v36.md` — 최신 판정
- `docs/전략_v34.md` `v35.md` — 감사 기록
